template: angular
preamble: >-
  You are working inside an existing Angular project skeleton already present in
  your working directory. Build it with `npm run build` and start the dev server
  with `npm start`; it serves on port 4200.
install: npm install
build: npm run build
start: npm start
port: 4200
viewport:
  width: 1280
  height: 800
