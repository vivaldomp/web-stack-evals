---
phase: 03-evaluation-pipeline-scorer
plan: 06
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - src/eval/registry.ts
  - tests/registry.test.ts
autonomous: true
requirements: [EVAL-05]

must_haves:
  truths:
    - "pixelmatch, axe, and judge always run in v1, unconditionally (D3-16)"
    - "the DOM evaluator is included only when the scenario declares a non-empty expectedElements list; when absent, it is omitted entirely — never scored 1.0, 0.0, or persisted as a dropped row (D3-09)"
    - "adding a fifth evaluator to the registry requires editing only this file, never src/pipeline/evaluate.ts (D3-15)"
  artifacts:
    - "src/eval/registry.ts exporting buildRegistry(deps): EvaluatorPort[]"
  key_links:
    - "buildRegistry -> createPixelMatchEvaluator/createJudgeEvaluator (03-03-PLAN.md)"
    - "buildRegistry -> createDomEvaluator/createAxeEvaluator (03-04-PLAN.md)"
    - "buildRegistry's judge wiring -> lookupCachedJudgeVerdict (03-01-PLAN.md), closed over the caller's db"
---

<objective>
Compose the four real evaluator factories from 03-03/03-04-PLAN.md into the one `buildRegistry()` function EVAL-05 requires — a plain array-builder, not a plugin framework, per 03-RESEARCH.md's explicit "Pattern 2: Registry as a plain map, not a framework" guidance and D3-16's "all four always run in v1" lock.

Purpose: this is the single place D3-09's "no expectedElements → DOM evaluator dropped via omission" and D3-16's "all four always run" rules are encoded, and the single place a future evaluator gets added (per D3-15) — everything downstream (`evaluate.ts`, already built in 03-05-PLAN.md and untouched by this plan) only ever sees the resulting `EvaluatorPort[]`.
Output: `src/eval/registry.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-evaluation-pipeline-scorer/03-CONTEXT.md
@.planning/phases/03-evaluation-pipeline-scorer/03-RESEARCH.md
@src/core/ports.ts
@src/eval/pixelmatchEvaluator.ts
@src/eval/judgeEvaluator.ts
@src/eval/domEvaluator.ts
@src/eval/axeEvaluator.ts
@src/storage/evaluations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildRegistry() — always-on pixelmatch/axe/judge, conditional dom (D3-09, D3-15, D3-16)</name>
  <files>src/eval/registry.ts, tests/registry.test.ts</files>
  <read_first>src/eval/pixelmatchEvaluator.ts, src/eval/judgeEvaluator.ts, src/eval/domEvaluator.ts, src/eval/axeEvaluator.ts (the four `create*Evaluator` factory signatures, exactly as 03-03/03-04-PLAN.md built them — read the actual files, do not assume the signatures from RESEARCH.md's draft snippets); src/storage/evaluations.ts (`lookupCachedJudgeVerdict(db, fingerprint): {rawScore, detail} | null` — synchronous; the judge evaluator's constructor param expects an async function, so this task wraps it); 03-RESEARCH.md "Pattern 2: Registry as a plain map, not a framework"</read_first>
  <behavior>
    - `buildRegistry({db, models, expectedElements: undefined, judgeModel: DEFAULT_JUDGE_MODEL})` returns an array whose `.map(e => e.name).sort()` equals exactly `["axe", "judge", "pixelmatch"]` — no `"dom"` entry when `expectedElements` is absent (D3-09).
    - `buildRegistry({db, models, expectedElements: [], judgeModel: DEFAULT_JUDGE_MODEL})` (an explicitly empty array, not `undefined`) also produces no `"dom"` entry — an empty declared-elements list is treated the same as an absent one.
    - `buildRegistry({db, models, expectedElements: ["nav"], judgeModel: DEFAULT_JUDGE_MODEL})` returns exactly `["axe", "dom", "judge", "pixelmatch"]`.
    - Every returned entry satisfies the `EvaluatorPort` shape (`typeof entry.name === "string"`, `typeof entry.evaluate === "function"`) — a structural check, not a behavioral one (registry.ts composes, it does not execute).
  </behavior>
  <action>
    Create `src/eval/registry.ts` importing `Database` as a type from `"better-sqlite3"`, `Models` as a type from `"@earendil-works/pi-ai"`, `EvaluatorPort` as a type from `../core/ports.js`, `createPixelMatchEvaluator` from `./pixelmatchEvaluator.js`, `createJudgeEvaluator, DEFAULT_JUDGE_MODEL` and `JudgeModelSpec` from `./judgeEvaluator.js`, `createDomEvaluator` from `./domEvaluator.js`, `createAxeEvaluator` from `./axeEvaluator.js`, and `lookupCachedJudgeVerdict` from `../storage/evaluations.js`. Export `interface RegistryDeps { db: Database.Database; models: Models; expectedElements?: string[]; judgeModel?: JudgeModelSpec; }` and `function buildRegistry(deps: RegistryDeps): EvaluatorPort[]`. Build a `lookupCachedVerdict` closure: `(fingerprint: string) => Promise.resolve(lookupCachedJudgeVerdict(deps.db, fingerprint))` (wraps the synchronous storage function to match `createJudgeEvaluator`'s async-returning parameter). Construct `const evaluators: EvaluatorPort[] = [createPixelMatchEvaluator(), createAxeEvaluator(), createJudgeEvaluator(deps.models, deps.judgeModel ?? DEFAULT_JUDGE_MODEL, lookupCachedVerdict)];` (D3-16: these three are unconditional, no `if` guarding any of them). Then `if (deps.expectedElements && deps.expectedElements.length > 0) { evaluators.push(createDomEvaluator(deps.expectedElements)); }` (D3-09: the ONLY conditional in this function — this single line is the entire mechanism by which "no expectedElements → DOM evaluator dropped" is implemented; it is intentionally NOT a special case inside `composeScore` or `evaluate.ts`, both of which remain completely unaware that a fifth or sixth evaluator could exist). Return `evaluators`. Write `tests/registry.test.ts` as a fast unit test (no real Chromium navigation, no real network call — the evaluators are only constructed and inspected structurally, never `.evaluate()`d in this test; use a real temp-file `better-sqlite3` db via `openDb()` since `createJudgeEvaluator`'s constructor-time model-lookup needs a real `Models` instance — obtain one via whatever pi-ai export 03-03-PLAN.md's `judgeEvaluator.ts` itself used to resolve `models.getModel`, so this test stays consistent with the real construction path) covering all four `<behavior>` cases.
  </action>
  <verify>
    <automated>npx vitest run tests/registry.test.ts</automated>
  </verify>
  <done>All four behavior cases pass; `src/eval/registry.ts` contains exactly one `if` statement (the D3-09 dom-inclusion guard) and no other conditional branching on evaluator identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — `registry.ts` performs pure in-process composition (constructs `EvaluatorPort` objects, does no I/O of its own; matches 03-RESEARCH.md's Architectural Responsibility Map row "Evaluator registry — API/Backend — in-process module, no I/O of its own"). It inherits, but does not add to, the trust boundaries already mitigated in 03-01/03-03/03-04-PLAN.md's threat registers (T-3-01, T-3-03, T-3-04, T-3-06, T-3-09, T-3-10).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-12 | Tampering (accept — no new surface) | `src/eval/registry.ts` | low | accept | No I/O, no untrusted input parsing, no new network/filesystem/DOM boundary — this file only calls four already-mitigated `create*Evaluator()` factories and pushes their results into an array |

</threat_model>

<artifacts_produced>
- `src/eval/registry.ts` — `buildRegistry(deps): EvaluatorPort[]`, `RegistryDeps`
</artifacts_produced>

<verification>
`npx vitest run tests/registry.test.ts` green — structural composition proven for both the with- and without-`expectedElements` cases.
</verification>

<success_criteria>
`buildRegistry()` is ready for 03-07-PLAN.md's full-pipeline integration test to call once, real evaluators and all, feeding its result straight into `evaluateRun()` (03-05-PLAN.md) unmodified.
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-pipeline-scorer/03-06-SUMMARY.md` when done
</output>
