---
phase: 03-evaluation-pipeline-scorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/specs/schema.ts
  - src/storage/evaluations.ts
  - tests/specs.test.ts
  - tests/evaluationsPersistence.test.ts
autonomous: true
requirements: [SCORE-02]

must_haves:
  truths:
    - "scenario.yaml can declare expectedElements + evaluatorWeights and unknown keys are still rejected (D-08 preserved)"
    - "a raw sub-score and a dropped-evaluator's reason both persist as their own evaluations row, never silently collapsed to 0 (D-20/D3-04)"
    - "the composite score + the weights actually used can be written onto an existing runs row, independent of and after the raw evaluations rows (D-21/SCORE-02)"
    - "an identical-fingerprint judge verdict can be looked up from prior evaluations rows without a new table (D3-14)"
  artifacts:
    - "src/storage/evaluations.ts exporting insertEvaluation, updateRunComposite, linkDiffScreenshot, lookupCachedJudgeVerdict"
    - "ScenarioSchema.expectedElements: string[] optional"
    - "ScenarioSchema.evaluatorWeights: Record<string, number> optional"
    - "sharp@0.35.3, @axe-core/playwright@4.12.1, @earendil-works/pi-ai@0.80.3 present in package.json dependencies"
  key_links:
    - "insertEvaluation -> evaluations table (D-20, one row per run/rep/evaluator)"
    - "updateRunComposite -> runs.composite_score + runs.composite_weights (D-21)"
    - "linkDiffScreenshot -> artifacts row (kind=screenshot) + screenshots row (role=diff) (D-25/D3-07)"
    - "lookupCachedJudgeVerdict -> SELECT over evaluations WHERE evaluator_name='judge', keyed by detail.fingerprint (D3-14)"
---

<objective>
Lay the three pieces of Phase 3 groundwork every other plan in this phase imports: (1) the three new production dependencies the evaluators need, (2) the `ScenarioSchema` fields that let a scenario declare DOM expected-elements and per-evaluator weight overrides, and (3) the raw-storage functions that let the composite scorer and the four evaluators persist and read back rows in the already-locked `evaluations`/`runs`/`screenshots` schema (D-20/D-21/D-25) without any migration.

Purpose: every downstream Phase 3 plan (pixelmatch/judge/dom/axe evaluators, the composite scorer, the orchestrator) either imports one of these new packages, reads/writes a `Scenario.expectedElements`/`evaluatorWeights` field, or calls one of these storage functions. Getting the shapes right here, in one place, means no later plan touches `package.json` or `src/specs/schema.ts` again.
Output: `sharp`/`@axe-core/playwright`/`@earendil-works/pi-ai` installed; `ScenarioSchema` extended; `src/storage/evaluations.ts` with four exported functions, unit-tested against a real temp `better-sqlite3` file (same pattern as `tests/storagePort.test.ts`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evaluation-pipeline-scorer/03-CONTEXT.md
@.planning/phases/03-evaluation-pipeline-scorer/03-RESEARCH.md
@.claude/CLAUDE.md
@src/specs/schema.ts
@src/storage/artifacts.ts
@src/storage/schema.sql.ts
@src/storage/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sharp, @axe-core/playwright, @earendil-works/pi-ai as production dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json; .claude/CLAUDE.md (locked versions section); 03-RESEARCH.md "Package Legitimacy Audit" table (all three packages already verdict=Approved, no [ASSUMED]/[SUS]/[SLOP] — no blocking human-verify checkpoint required)</read_first>
  <action>
    Run `npm install sharp@0.35.3 @axe-core/playwright@4.12.1 @earendil-works/pi-ai@0.80.3` (production dependencies, not devDependencies — matches the existing `pixelmatch`/`pngjs`/`playwright` placement in package.json, since the runtime evaluators import these directly). Do NOT add `@types/sharp` or a separate types package for either — `sharp`, `@axe-core/playwright`, and `@earendil-works/pi-ai` all ship their own bundled `.d.ts` files (confirmed by 03-RESEARCH.md's direct tarball inspection of `@earendil-works/pi-ai`; `sharp` and `@axe-core/playwright` have shipped bundled types for years). Do not touch `devDependencies`, `scripts`, or any other package.json field.
  </action>
  <verify>
    <automated>node -e "require('sharp'); require('@axe-core/playwright'); require('@earendil-works/pi-ai'); console.log('ok')"</automated>
  </verify>
  <done>`npm ls sharp @axe-core/playwright @earendil-works/pi-ai` reports exactly `sharp@0.35.3`, `@axe-core/playwright@4.12.1`, `@earendil-works/pi-ai@0.80.3` with no `UNMET DEPENDENCY` warnings; `package.json` `dependencies` (not `devDependencies`) lists all three.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ScenarioSchema with expectedElements + evaluatorWeights (D3-08, D3-02)</name>
  <files>src/specs/schema.ts, tests/specs.test.ts</files>
  <read_first>src/specs/schema.ts (current ScenarioSchema/StackSchema/ModelSchema `.strictObject()` pattern); tests/specs.test.ts (existing `loadScenario` test); tests/fixtures/scenarios/dashboard/dashboard.yaml (a real scenario fixture that must keep parsing after this change, since both new fields are optional)</read_first>
  <behavior>
    - `ScenarioSchema.safeParse` on the existing `tests/fixtures/scenarios/dashboard/dashboard.yaml` shape (no `expectedElements`/`evaluatorWeights` keys) still succeeds — both fields are optional, no breaking change to an existing fixture.
    - `ScenarioSchema.safeParse` succeeds when `expectedElements: ["nav[role=navigation]", ".dashboard-card"]` is present, and the parsed value's `expectedElements` array equals the input.
    - `ScenarioSchema.safeParse` succeeds when `evaluatorWeights: { pixelmatch: 0.4, dom: 0.2, axe: 0.2, judge: 0.2 }` is present, and the parsed value's `evaluatorWeights` record equals the input.
    - `ScenarioSchema.safeParse` still rejects an unrelated unknown top-level key (e.g. `notAField: true`) — `.strict()` behavior (D-08) is preserved after the extension.
  </behavior>
  <action>
    In `src/specs/schema.ts`, add two optional fields to the existing `ScenarioSchema` `z.strictObject({...})` call: `expectedElements: z.array(z.string()).optional()` (D3-08 — plain CSS selector strings, e.g. `[role="navigation"]`, no separate ARIA-role DSL per 03-RESEARCH.md Assumption A3) and `evaluatorWeights: z.record(z.string(), z.number()).optional()` (D3-02 — keys are evaluator names, values are the weight; no cross-validation against known evaluator names here — `src/specs/schema.ts` must not import anything from `src/eval/`, that coupling stays one-directional). Do not add a `.refine()` requiring weights to sum to 1 — `composeScore` (03-02-PLAN.md) renormalizes regardless, so a stricter constraint here is unrequested. Add the four test cases from `<behavior>` to `tests/specs.test.ts`'s existing `describe("loadScenario", ...)` block (or a new adjacent `describe`), reusing the `VALID_STACK`-style inline literal pattern already used for `StackSchema` in that file rather than adding a new fixture YAML.
  </action>
  <verify>
    <automated>npx vitest run tests/specs.test.ts</automated>
  </verify>
  <done>All four behavior cases pass; the existing `loadScenario`/`loadStack`/`loadModel` tests in `tests/specs.test.ts` still pass unmodified (extension is additive-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Storage functions for evaluations, composite, diff-screenshot linking, and judge-cache lookup (D-20, D-21, D3-07, D3-14)</name>
  <files>src/storage/evaluations.ts, tests/evaluationsPersistence.test.ts</files>
  <read_first>src/storage/artifacts.ts (writeArtifact/getArtifactPath pattern — prepared statements, bound params only, T-1-SQL rule); src/storage/schema.sql.ts (evaluations/runs/screenshots/artifacts column names — do not invent columns); tests/storagePort.test.ts (tmp-dir + openDb + afterEach cleanup pattern to reuse verbatim)</read_first>
  <behavior>
    - `insertEvaluation(db, runId, repIndex, "pixelmatch", 0.87, {mismatchedPixels: 130})` then a raw `SELECT * FROM evaluations WHERE run_id=? AND evaluator_name='pixelmatch'` returns one row with `raw_score = 0.87` and `detail` JSON-parseable back to `{mismatchedPixels: 130}`.
    - `insertEvaluation(db, runId, repIndex, "judge", null, {dropped: true, reason: "timeout"})` persists a row with `raw_score IS NULL` and `detail.dropped === true` — a dropped evaluator is never silently written as `0` (D3-04).
    - `updateRunComposite(db, runId, 0.91, {pixelmatch: 0.5, axe: 0.5})` on a `runs` row pre-seeded with a minimal `INSERT INTO runs (run_id) VALUES (?)` updates that row's `composite_score` to `0.91` and `composite_weights` to the JSON-parseable weights object, leaving every other `runs` column untouched.
    - `linkDiffScreenshot(db, runId, pngBuffer, {width: 20, height: 20})` writes the bytes via the same `results/<runId>/...` convention as `writeArtifact` (reuse it, don't reimplement), returns a numeric artifact id, and a raw `SELECT role, viewport FROM screenshots WHERE artifact_id=?` returns exactly one row with `role='diff'`.
    - `lookupCachedJudgeVerdict(db, fingerprint)` returns `null` when no prior `evaluations` row has that fingerprint in `detail.fingerprint`, and returns `{rawScore, detail}` for the most recent matching non-null-score row when one exists (proves the cache actually short-circuits on a second call with the same fingerprint).
  </behavior>
  <action>
    Create `src/storage/evaluations.ts` following the exact style of `src/storage/artifacts.ts` (module-level prepared-SQL string constants, functions taking `db: Database.Database` as the first param — this module is NOT `core/ports.ts` and is NOT gated by D-23's "imports nothing concrete" rule, same as `artifacts.ts`/`db.ts`/`storagePort.ts` already aren't). Export four functions: `insertEvaluation(db, runId: string, repIndex: number, evaluatorName: string, rawScore: number | null, detail: unknown): void` — `INSERT INTO evaluations (run_id, rep_index, evaluator_name, raw_score, detail) VALUES (...)` with `detail` `JSON.stringify`'d, bound params only (no plain `INSERT` — reuse the pattern, not a fresh design). `updateRunComposite(db, runId: string, compositeScore: number, weightsUsed: Record<string, number>): void` — `UPDATE runs SET composite_score = @score, composite_weights = @weights WHERE run_id = @run_id`. `linkDiffScreenshot(db, runId: string, diffPng: Buffer, viewport: { width: number; height: number }, resultsRoot?: string): number` — calls the existing `writeArtifact(db, runId, "screenshot", "diff.png", diffPng, resultsRoot)` from `src/storage/artifacts.ts` (import it, do not duplicate the mkdir/write/sha256 logic) to get an artifact id, then `INSERT INTO screenshots (artifact_id, role, viewport, dpr) VALUES (@artifact_id, 'diff', @viewport, 1)` with `viewport` JSON-stringified, and returns the artifact id. `lookupCachedJudgeVerdict(db, fingerprint: string): { rawScore: number; detail: unknown } | null` — `SELECT raw_score, detail FROM evaluations WHERE evaluator_name = 'judge' AND raw_score IS NOT NULL AND json_extract(detail, '$.fingerprint') = @fingerprint ORDER BY id DESC LIMIT 1`, returns `null` when no row matches, otherwise `{ rawScore: row.raw_score, detail: JSON.parse(row.detail) }`. No `INSERT OR REPLACE`/upsert logic anywhere in this file — v1 is single-rep/single-run scope (D3-18), a plain insert per evaluator per run is sufficient; do not add conflict-handling that nobody asked for. Write `tests/evaluationsPersistence.test.ts` against a real temp-file `better-sqlite3` DB via `openDb()`, mirroring `tests/storagePort.test.ts`'s `mkdtempSync`/`afterEach(rmSync)` setup exactly, covering all five `<behavior>` cases above.
  </action>
  <verify>
    <automated>npx vitest run tests/evaluationsPersistence.test.ts</automated>
  </verify>
  <done>All five behavior cases pass against a real `better-sqlite3` file; no test touches `src/storage/artifacts.ts`'s internals directly (only via its exported `writeArtifact`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hand-edited `scenario.yaml` on disk → `ScenarioSchema.safeParse` | A scenario author (or a corrupted/typo'd file) supplies `expectedElements`/`evaluatorWeights`; these values later drive live `page.locator()` calls (03-04) and composite math (03-02) |
| npm registry → `node_modules` | Three new third-party packages enter the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | `ScenarioSchema.expectedElements` / `evaluatorWeights` | medium | mitigate | `.strict()` zod validation on the whole `ScenarioSchema` object (unchanged behavior, D-08) rejects any unrecognized key before a run starts; the two new fields are typed (`string[]` / `Record<string,number>`) so a malformed value (wrong type) is rejected at load time rather than failing later inside a live-page evaluator |
| T-3-SC | Tampering (supply chain) | `npm install sharp @axe-core/playwright @earendil-works/pi-ai` | high | mitigate | All three packages carry an `Approved` verdict with no `[ASSUMED]`/`[SUS]`/`[SLOP]` flags in 03-RESEARCH.md's Package Legitimacy Audit (versions/registry age/repo cross-checked directly); per the package-legitimacy gate this means no blocking `checkpoint:human-verify` is required before this install task |

</threat_model>

<artifacts_produced>
- `src/storage/evaluations.ts` — `insertEvaluation`, `updateRunComposite`, `linkDiffScreenshot`, `lookupCachedJudgeVerdict`
- `ScenarioSchema.expectedElements: string[] | undefined` (`src/specs/schema.ts`)
- `ScenarioSchema.evaluatorWeights: Record<string, number> | undefined` (`src/specs/schema.ts`)
- `sharp@0.35.3`, `@axe-core/playwright@4.12.1`, `@earendil-works/pi-ai@0.80.3` in `package.json` dependencies
</artifacts_produced>

<verification>
Run `npx vitest run tests/specs.test.ts tests/evaluationsPersistence.test.ts` — both green. `npm ls sharp @axe-core/playwright @earendil-works/pi-ai` — all three resolved at the pinned versions.
</verification>

<success_criteria>
Every downstream Phase 3 plan can `import sharp`/`import { AxeBuilder } from "@axe-core/playwright"`/`import ... from "@earendil-works/pi-ai"`, read `scenario.expectedElements`/`scenario.evaluatorWeights` off a loaded `Scenario`, and call `insertEvaluation`/`updateRunComposite`/`linkDiffScreenshot`/`lookupCachedJudgeVerdict` against a real SQLite file with no further schema or package changes.
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-pipeline-scorer/03-01-SUMMARY.md` when done
</output>
