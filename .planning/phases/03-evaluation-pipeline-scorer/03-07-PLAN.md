---
phase: 03-evaluation-pipeline-scorer
plan: 07
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - tests/evalPipeline.integration.test.ts
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05, SCORE-01, SCORE-02]

must_haves:
  truths:
    - "feeding a matched expected/generated pair through the real pipeline yields four sub-scores: PixelMatch %, DOM structural presence, axe-core accessibility, and LLM-judge verdict (ROADMAP Phase 3 Success Criterion 1)"
    - "the axe and DOM evaluators score from the SAME shared render pass — one renderWithPage() call, one page, closed exactly once after both finish (D3-17)"
    - "each run persists raw sub-scores separately from the normalized composite score (ROADMAP Phase 3 Success Criterion 4)"
    - "the full evaluation pipeline runs green end-to-end on fixture screenshots with no agent, no dev server, and no live network call present (ROADMAP Phase 3 Success Criterion 5)"
  artifacts:
    - "tests/evalPipeline.integration.test.ts — the ROADMAP SC5 proof"
  key_links:
    - "renderWithPage (03-02) -> buildRegistry (03-06) -> evaluateRun (03-05) -> real evaluations rows + runs.composite_score, all in one test"
---

<objective>
Wire every piece Phase 3 built — `renderWithPage()` (03-02), the four real evaluators (03-03/03-04), `buildRegistry()` (03-06), and `evaluateRun()` (03-05) — into one real, no-mocks integration test that proves ROADMAP Phase 3's Success Criterion 5 ("the full evaluation pipeline runs green end-to-end on fixture screenshots with no agent"). This plan adds no production code — it is the phase's own checkpoint proof.

Purpose: every earlier plan in this phase tested its own piece in isolation (fake evaluators for `evaluate.ts`, `fauxProvider()` for the judge, structural-only checks for the registry). Nothing until now has proven the pieces actually fit together — that a page opened once by `renderWithPage()` survives being handed to both the DOM and axe evaluators before `close()`, that `buildRegistry()`'s real evaluator instances satisfy `evaluateRun()`'s generic loop, and that a full run really does persist four separate `evaluations` rows plus one composite on the `runs` row.
Output: `tests/evalPipeline.integration.test.ts` — the definitive, CI-safe (no `ANTHROPIC_API_KEY` required — the judge evaluator is wired to pi-ai's `fauxProvider()` here too) proof this phase's checkpoint is met.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-evaluation-pipeline-scorer/03-CONTEXT.md
@.planning/phases/03-evaluation-pipeline-scorer/03-RESEARCH.md
@src/pipeline/evaluate.ts
@src/eval/registry.ts
@src/render/renderWithPage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end fixture pipeline — real renderWithPage + real registry + real evaluateRun (ROADMAP SC5)</name>
  <files>tests/evalPipeline.integration.test.ts</files>
  <read_first>src/pipeline/evaluate.ts (`EvaluateRunInput` — the exact shape this test must assemble); src/eval/registry.ts (`RegistryDeps` — real `db`/`models`/`expectedElements`/`judgeModel`); src/render/renderWithPage.ts (`LiveRenderResult` — `page`/`close`); tests/fixtures/eval/app.html + tests/fixtures/eval/pngFixtures.ts (03-04/03-03-PLAN.md's fixtures — reused here as-is, not recreated); tests/evaluateRun.test.ts and tests/registry.test.ts (the real-`better-sqlite3`-tmp-db + faux-provider-`Models` construction patterns to reuse, so this test's setup is consistent with the rest of the phase rather than inventing a third pattern)</read_first>
  <action>
    Write `tests/evalPipeline.integration.test.ts` against `vitest.integration.config.ts` (real Chromium via `renderWithPage`, no network — the judge evaluator's `Models` instance is constructed the same faux-provider way `tests/judgeEvaluator.test.ts` and `tests/registry.test.ts` already do, so this test never requires `ANTHROPIC_API_KEY` and never makes a live HTTP call). Set up: a real temp-file `better-sqlite3` DB via `openDb()`, seeded with a minimal `INSERT INTO runs (run_id) VALUES (?)` row (same seeding approach as `tests/evaluateRun.test.ts`). Call `renderWithPage({ url: "file://" + resolved path to tests/fixtures/eval/app.html, viewport: { width: 400, height: 300 } })` exactly ONCE, keeping the returned `page` open. Build `expectedPng`/`generatedPng` from `pngFixtures.makeExpectedPng()`/`pngFixtures.makeGeneratedMatchPng()` (03-03-PLAN.md's procedural fixtures — a matched, near-identical pair, satisfying D3-04's "agent-caused degradation" contrast case is optional here since that specific behavior is already proven in `tests/pixelmatchEvaluator.test.ts`; this test's job is end-to-end wiring, not re-proving each evaluator's internal math). Call `buildRegistry({ db, models: <the faux-provider Models instance>, expectedElements: ["nav[role='navigation']", ".dashboard-card", "button[type='submit']"], judgeModel: DEFAULT_JUDGE_MODEL })` (uses `app.html`'s real elements, so the DOM evaluator scores `1`). Call `evaluateRun({ db, runId, repIndex: 0, expectedPng, generatedPng, viewport: { width: 400, height: 300 }, page: liveRenderResult.page, registry, defaultWeights: DEFAULT_EVALUATOR_WEIGHTS })`. AFTER `evaluateRun` resolves, call `liveRenderResult.close()` — proving the page stayed open across both the DOM and axe evaluator calls inside `evaluateRun`'s loop and was only closed here, by the top-level caller, matching D3-17's "caller runs axe + DOM-presence, then calls close() after both finish" lifecycle. Assert: exactly four rows exist in `evaluations` for this `run_id` (a raw `SELECT COUNT(*) FROM evaluations WHERE run_id = ?`), with `evaluator_name` values `pixelmatch`, `dom`, `axe`, `judge` (ROADMAP SC1); each row's `raw_score` is non-null and in `[0,1]` (this fixture pairing is designed to have every evaluator survive, not drop); the `runs` row's `composite_score` is non-null and in `[0,1]`, and its `composite_weights` JSON-parses to a record whose values sum to `1` (within floating-point tolerance) — proving raw sub-scores and the composite are stored separately and the composite is re-derivable from the persisted weights (ROADMAP SC4, SCORE-02); and a `screenshots` row with `role='diff'` exists (D3-07). The whole test never starts a dev server, never imports `src/pipeline/runStack.ts`, and never spawns the Pi SDK — proving ROADMAP SC5's "no agent" checkpoint.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/evalPipeline.integration.test.ts</automated>
  </verify>
  <done>The test passes with: 4 `evaluations` rows (pixelmatch/dom/axe/judge, all non-dropped), a non-null `runs.composite_score` whose recorded `composite_weights` sum to 1, and a `screenshots.role='diff'` row — all produced by ONE `renderWithPage()` call whose `page` is shared by both live evaluators and closed exactly once, at the end, by the test itself (not inside `evaluateRun`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan adds only a test wiring already-mitigated components together (renderWithPage's bounded navigation from 03-02, the judge's zod-validated verdict from 03-03, the DOM/axe evaluators from 03-04, the persistence/composite logic from 03-01/03-02/03-05, the registry composition from 03-06). It inherits every threat disposition already recorded in those plans' registers.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-13 | Repudiation (accept — test-only) | `tests/evalPipeline.integration.test.ts` | low | accept | This file is test code, not a shipped runtime path; it exists solely to prove the already-mitigated production components (T-3-01 through T-3-12) compose correctly end-to-end |

</threat_model>

<artifacts_produced>
- `tests/evalPipeline.integration.test.ts` — the ROADMAP Phase 3 Success Criterion 5 proof
</artifacts_produced>

<verification>
`npx vitest run --config vitest.integration.config.ts tests/evalPipeline.integration.test.ts` green, with zero network calls and zero dev-server/agent involvement.
</verification>

<success_criteria>
ROADMAP Phase 3's five Success Criteria are all demonstrably true against real (fixture-driven) code: four sub-scores from a matched pair (SC1), a new evaluator registers via `EvaluatorPort` + `buildRegistry` with no core/orchestrator edit (SC2, proven structurally by 03-06's tests + this plan's use of the same unmodified `evaluate.ts`), the judge runs an independent model family at temp=0, images-only, structured rubric (SC3, proven in 03-03's tests, exercised again here), raw sub-scores persist separately from the composite (SC4), and the full pipeline runs green with no agent present (SC5).
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-pipeline-scorer/03-07-SUMMARY.md` when done
</output>
