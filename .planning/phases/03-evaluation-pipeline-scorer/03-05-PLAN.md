---
phase: 03-evaluation-pipeline-scorer
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/pipeline/evaluate.ts
  - tests/evaluateRun.test.ts
autonomous: true
requirements: [SCORE-02]

must_haves:
  truths:
    - "every evaluator in the injected registry runs the same shared input — evaluateRun never special-cases a call by evaluator name to decide WHAT to pass it (EVAL-05: adding an evaluator needs no orchestrator edit)"
    - "a dropped evaluator's reason is persisted; its raw_score column is NULL, never 0 (D3-04)"
    - "the pixelmatch evaluator's diff image is persisted as screenshots.role='diff' by the orchestrator, not by the evaluator itself (D3-07)"
    - "the composite score + weights are written to the runs row only when at least one evaluator survived (SCORE-02, Pitfall 5)"
  artifacts:
    - "src/pipeline/evaluate.ts exporting evaluateRun(input): Promise<EvaluateRunResult>"
  key_links:
    - "evaluateRun -> registry: EvaluatorPort[] (injected, built by 03-06-PLAN.md's buildRegistry — evaluate.ts never imports registry.ts)"
    - "evaluateRun -> insertEvaluation/linkDiffScreenshot/updateRunComposite (03-01-PLAN.md)"
    - "evaluateRun -> composeScore (03-02-PLAN.md)"
---

<objective>
Implement `evaluateRun()` — the single orchestration function that turns "a registry of evaluators + a shared input (two PNGs, a viewport, a live page)" into "N persisted `evaluations` rows + one composite score written to the `runs` row". This is Phase 3's EVAL-05 composition point and its SCORE-02 persistence flow, deliberately built and tested BEFORE any real evaluator exists, using fake `EvaluatorPort` stand-ins — proving by construction that the orchestrator has zero knowledge of which four (or five, or three) evaluators it's driving.

Purpose: this is the concrete demonstration of D3-15 ("adding an evaluator = a registry entry with no core/orchestrator edits") — `evaluateRun` takes `registry: EvaluatorPort[]` as a plain injected parameter, never imports `registry.ts` or any concrete evaluator module, so this plan can be written, tested, and reviewed in parallel with the actual evaluator implementations (03-03/03-04-PLAN.md).
Output: `src/pipeline/evaluate.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-evaluation-pipeline-scorer/03-CONTEXT.md
@.planning/phases/03-evaluation-pipeline-scorer/03-RESEARCH.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@src/core/ports.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluateRun() — registry-driven evaluation with drop/renormalize persistence (EVAL-05 composition point, SCORE-02)</name>
  <files>src/pipeline/evaluate.ts, tests/evaluateRun.test.ts</files>
  <read_first>src/core/ports.ts (`EvaluatorPort.evaluate(input: unknown): Promise<{rawScore, detail}>` — the ONLY shape this task's loop is allowed to assume about any evaluator); tests/storagePort.test.ts / tests/evaluationsPersistence.test.ts (03-01-PLAN.md's real-`better-sqlite3`-tmp-file test setup pattern to reuse); 01-CONTEXT.md D-14/D-18 (rep_index is an explicit, never-hardcoded param — v1 callers pass `0`, per D3-18)</read_first>
  <behavior>
    - Given a `registry` of three fake `EvaluatorPort`s — `{name:"fakeA", evaluate: async () => ({rawScore:0.8, detail:{}})}`, `{name:"fakeB", evaluate: async () => ({rawScore:0.6, detail:{}})}`, `{name:"fakeDropped", evaluate: async () => ({rawScore:0, detail:{dropped:true, reason:"boom"}})}` — and `defaultWeights = {fakeA:1/3, fakeB:1/3, fakeDropped:1/3}`, `evaluateRun(...)` persists exactly three `evaluations` rows (`fakeDropped`'s `raw_score` is `NULL`, its `detail.reason` is `"boom"`), and the returned `compositeScore` equals the two-survivor renormalized weighted mean of `0.8` and `0.6` (matches `composeScore`'s own renormalization behavior — this task doesn't reimplement that math, it calls `composeScore`).
    - The `runs` row (pre-seeded with a minimal `INSERT INTO runs (run_id) VALUES (?)`) has its `composite_score`/`composite_weights` columns updated to match the returned result after `evaluateRun` completes.
    - Given a registry entry named exactly `"pixelmatch"` whose `evaluate()` resolves with `detail.diffPng` set to a real `Buffer`, `evaluateRun` writes that buffer as a `screenshots.role='diff'` artifact (assert via a raw `SELECT role FROM screenshots WHERE artifact_id = (SELECT id FROM artifacts WHERE run_id = ? AND kind = 'screenshot' ORDER BY id DESC LIMIT 1)` returning `'diff'`) — no other evaluator name triggers this branch.
    - Given a registry where every entry resolves `detail.dropped === true`, `evaluateRun`'s returned `compositeScore` is `null` and the `runs` row's `composite_score` column is left untouched (never overwritten with `null` or `0`) — matches `composeScore`'s all-dropped short-circuit (Pitfall 5) and D3-04's "escalate to eval_error only when systemic" note (the actual `runs.status` escalation is explicitly out of this phase's scope — Phase 5's orchestrator wiring — this task only proves the composite write is correctly skipped).
    - `evaluateRun` calls every registry entry's `evaluate()` with the exact same input object (`{expectedPng, generatedPng, viewport, page}`) — assert this by having one fake evaluator record the object identity/shape it received and comparing across two different fake evaluator names, proving no per-name special-casing of the INPUT (the diffPng persistence branch keys off the evaluator's OUTPUT/name for where to WRITE the result, not what it's given to evaluate).
  </behavior>
  <action>
    Create `src/pipeline/evaluate.ts` importing `insertEvaluation`/`updateRunComposite`/`linkDiffScreenshot` from `../storage/evaluations.js` (03-01-PLAN.md) and `composeScore`/`DEFAULT_EVALUATOR_WEIGHTS`/`EvaluatorOutcome` from `./composite.js` (03-02-PLAN.md), plus `EvaluatorPort` as a type from `../core/ports.js`. Export `interface EvaluateRunInput { db: Database.Database; runId: string; repIndex: number; expectedPng: Buffer; generatedPng: Buffer; viewport: { width: number; height: number }; page: unknown; registry: EvaluatorPort[]; defaultWeights?: Record<string, number>; }` (`page` is typed `unknown` here deliberately — `evaluate.ts` never imports Playwright's `Page` type; it only forwards whatever the caller gave it into each evaluator's `unknown` input, exactly like `EvaluatorPort.evaluate` itself does) and `interface EvaluateRunResult { compositeScore: number | null; weightsUsed: Record<string, number>; outcomes: EvaluatorOutcome[]; }`. Export `async function evaluateRun(input: EvaluateRunInput): Promise<EvaluateRunResult>`: build the shared per-evaluator input object once (`{expectedPng: input.expectedPng, generatedPng: input.generatedPng, viewport: input.viewport, page: input.page}`); for each `evaluator` of `input.registry`, `await evaluator.evaluate(sharedInput)`, then classify: if `(result.detail as any)?.dropped === true`, this is a `DroppedResult` (`reason: String((result.detail as any).reason ?? "unknown")`) and `insertEvaluation(input.db, input.runId, input.repIndex, evaluator.name, null, result.detail)`; otherwise it's an `EvalResult` and `insertEvaluation(input.db, input.runId, input.repIndex, evaluator.name, result.rawScore, result.detail)`. Immediately after a successful (non-dropped) `"pixelmatch"`-named evaluator's insert, if `(result.detail as any)?.diffPng` is present, call `linkDiffScreenshot(input.db, input.runId, (result.detail as any).diffPng, input.viewport)` — this is the one, explicitly-named, evaluator-specific branch in an otherwise fully-generic loop (D3-07 requires it; it does not violate D3-15's "no orchestrator edit to ADD an evaluator" because this branch is keyed to a specific evaluator's OWN documented output shape, not a structural requirement every future evaluator must satisfy). Collect all outcomes into an array, call `composeScore(outcomes, input.defaultWeights ?? DEFAULT_EVALUATOR_WEIGHTS)`, and if the resulting `compositeScore !== null`, call `updateRunComposite(input.db, input.runId, compositeScore, weightsUsed)` — if it IS `null`, skip that call entirely (do not write `null`/`0` to `runs.composite_score`). Return `{ compositeScore, weightsUsed, outcomes }`. Write `tests/evaluateRun.test.ts` against a real temp-file `better-sqlite3` DB (same `mkdtempSync`/`openDb`/`afterEach(rmSync)` pattern as `tests/evaluationsPersistence.test.ts`), covering all five `<behavior>` cases with fake `EvaluatorPort` arrays — this test file never imports a real evaluator module or Playwright.
  </action>
  <verify>
    <automated>npx vitest run tests/evaluateRun.test.ts</automated>
  </verify>
  <done>All five behavior cases pass against a real SQLite file; `src/pipeline/evaluate.ts` has no import from `src/eval/registry.ts` or any concrete evaluator module, and no import from `"playwright"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Evaluator `detail` objects (of unknown shape, `unknown`-typed by `EvaluatorPort`) → `evaluate.ts`'s classification logic | `evaluate.ts` reads `detail.dropped`/`detail.diffPng` off an `unknown`-typed value returned by whatever evaluator is in the registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-07 | Repudiation / Integrity | `src/pipeline/evaluate.ts` persistence loop | medium | mitigate | Every registry entry's outcome — survivor or dropped — is persisted as its own `evaluations` row via `insertEvaluation`'s bound-parameter INSERT (never string-concatenated SQL); a dropped evaluator's `detail.reason` is always recorded, never silently discarded, so the full audit trail of what ran and what didn't survives even when the composite excludes it |
| T-3-11 | Tampering (defensive typing) | `evaluate.ts` reading `detail.dropped`/`detail.diffPng` off an `unknown` value | low | accept | `detail` is `unknown` by `EvaluatorPort`'s own contract (D-23); `evaluate.ts` reads these two optional fields defensively (`(result.detail as any)?.dropped`) and never assumes their presence — an evaluator that omits both fields is simply treated as a normal survivor, which is the correct fallback, not a crash |

</threat_model>

<artifacts_produced>
- `src/pipeline/evaluate.ts` — `evaluateRun(input): Promise<EvaluateRunResult>`, `EvaluateRunInput`, `EvaluateRunResult`
</artifacts_produced>

<verification>
`npx vitest run tests/evaluateRun.test.ts` green against a real SQLite temp file, using only fake evaluators.
</verification>

<success_criteria>
`evaluateRun()` is ready for 03-07-PLAN.md's full-pipeline integration test to call with a REAL registry (built by 03-06-PLAN.md) and REAL evaluators (03-03/03-04-PLAN.md), with zero changes to `evaluate.ts` itself.
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-pipeline-scorer/03-05-SUMMARY.md` when done
</output>
