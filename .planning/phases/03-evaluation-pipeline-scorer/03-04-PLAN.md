---
phase: 03-evaluation-pipeline-scorer
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/eval/domEvaluator.ts
  - tests/domEvaluator.integration.test.ts
  - src/eval/axeEvaluator.ts
  - tests/axeEvaluator.integration.test.ts
  - tests/fixtures/eval/app.html
  - tests/fixtures/eval/app-clean.html
autonomous: true
requirements: [EVAL-02, EVAL-03]

must_haves:
  truths:
    - "DOM structural presence is a gradient (found/declared), not pass/fail, and runs against the rendered DOM, not the screenshot (D3-08)"
    - "axe accessibility scoring starts at 1.0 and subtracts severity-weighted per-node penalties, floored at 0 (D3-10)"
    - "both evaluators consume an already-open Playwright Page from renderWithPage — neither one navigates or manages browser lifecycle itself"
  artifacts:
    - "src/eval/domEvaluator.ts exporting createDomEvaluator(expectedElements): EvaluatorPort"
    - "src/eval/axeEvaluator.ts exporting createAxeEvaluator(): EvaluatorPort"
    - "tests/fixtures/eval/app.html — elements matching a sample expectedElements list + one deliberate a11y violation"
    - "tests/fixtures/eval/app-clean.html — same element shape, a11y-clean"
  key_links:
    - "renderWithPage (03-02-PLAN.md) -> page -> createDomEvaluator/createAxeEvaluator evaluate({page}) (D3-17's shared render pass)"
    - "createDomEvaluator -> conditionally included in the registry only when scenario.expectedElements is non-empty (D3-09, wired in 03-06-PLAN.md)"
---

<objective>
Implement the two "live-page" evaluators — the two of Phase 3's four evaluators that need a still-open Playwright `Page` rather than PNG bytes: `createDomEvaluator()` (EVAL-02, structural presence) and `createAxeEvaluator()` (EVAL-03, accessibility).

Purpose: both evaluators exist specifically because `renderWithPage()` (03-02-PLAN.md) now exposes a live page instead of closing it after a screenshot. Building them together in one plan keeps the two "drives Chromium, evaluator code runs in Node" evaluators (per 03-RESEARCH.md's Architectural Responsibility Map) as a matched pair sharing one fixture HTML file, and proves Pitfall 2 ("calling AxeBuilder/page.locator after the browser has closed throws") never triggers because neither evaluator ever calls `close()` itself.
Output: `src/eval/domEvaluator.ts`, `src/eval/axeEvaluator.ts`, and two static HTML fixtures (`app.html`, `app-clean.html`) served via `file://` in tests — no dev server needed, keeping this phase's checkpoint agent-free.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-evaluation-pipeline-scorer/03-CONTEXT.md
@.planning/phases/03-evaluation-pipeline-scorer/03-RESEARCH.md
@src/core/ports.ts
@src/render/renderWithPage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DOM structural-presence evaluator + shared fixtures (EVAL-02, D3-08/D3-09)</name>
  <files>src/eval/domEvaluator.ts, tests/domEvaluator.integration.test.ts, tests/fixtures/eval/app.html, tests/fixtures/eval/app-clean.html</files>
  <read_first>src/core/ports.ts (`EvaluatorPort`); src/render/renderWithPage.ts (`renderWithPage(input): Promise<LiveRenderResult>` — this task's test is the first real consumer of it besides its own integration test in 03-02-PLAN.md); 03-RESEARCH.md "DOM structural presence" code example and Assumption A3 (CSS selector strings, no separate ARIA-role DSL)</read_first>
  <behavior>
    - `createDomEvaluator(["nav[role='navigation']", ".dashboard-card", "button[type='submit']", ".does-not-exist"]).evaluate({page})` against `tests/fixtures/eval/app.html` (which contains the first three selectors' matching elements but not the fourth) returns `rawScore === 0.75` and `detail.missing` containing exactly `.does-not-exist`.
    - `createDomEvaluator(selectors).evaluate({page})` where every declared selector matches at least one element returns `rawScore === 1`.
    - The evaluator never calls `page.goto`/`page.close`/any browser-lifecycle method — it only reads from the `page` it's handed.
  </behavior>
  <action>
    Create `tests/fixtures/eval/app.html` — a small static HTML document (no external assets, no `<script>` needed) containing at least: a `<nav role="navigation">`, an element with class `dashboard-card`, a `<button type="submit">`, and one deliberate accessibility violation — an `<img>` with a `src` but no `alt` attribute (the fixture both plans in this phase's Wave 2 share: this task's DOM-presence test and this plan's own axe test in Task 2). Create `tests/fixtures/eval/app-clean.html` with the same navigable/card/button structure but the `<img>` has a proper `alt` attribute and no other a11y issues — used by Task 2's "clean fixture scores near-1.0" case. Create `src/eval/domEvaluator.ts` exporting `createDomEvaluator(expectedElements: string[]): EvaluatorPort` with `name: "dom"`. Inside `evaluate(rawInput: unknown)`, cast to `{ page: Page }` (import `Page` as a type from `"playwright"` — this file, like `axeEvaluator.ts`, is one of the small explicitly-named set of files besides `renderWithPage.ts`/`playwrightRenderer.ts` allowed to import Playwright's `Page` type for its own input typing, per 03-RESEARCH.md's Pattern 1 rationale; `core/ports.ts` never learns about this), loop `for (const selector of expectedElements) { const count = await page.locator(selector).count(); ... }`, tracking `found`/`missing`. Return `{ rawScore: found / expectedElements.length, detail: { found, declared: expectedElements.length, missing } }` (D3-08's gradient scoring — never pass/fail). Write `tests/domEvaluator.integration.test.ts` against `vitest.integration.config.ts`: serve `app.html` via a `file://` URL (`fileURLToPath`/`join(__dirname, "fixtures/eval/app.html")`), call `renderWithPage({url: "file://" + resolvedPath, viewport: {width: 400, height: 300}})`, evaluate against the returned `page`, then `close()` — covering both `<behavior>` cases.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/domEvaluator.integration.test.ts</automated>
  </verify>
  <done>Both behavior cases pass against the real fixture via a real (headless, `file://`) Playwright page — no mocked `Page` object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Axe accessibility evaluator with severity-weighted penalty (EVAL-03, D3-10)</name>
  <files>src/eval/axeEvaluator.ts, tests/axeEvaluator.integration.test.ts</files>
  <read_first>node_modules/@axe-core/playwright's shipped README (installed by 03-01-PLAN.md's Task 1) confirming `new AxeBuilder({page}).analyze()` returns `.violations[]` with `impact: "critical"|"serious"|"moderate"|"minor"` and `.nodes[]`; 03-RESEARCH.md "Axe severity-weighted scoring" code example (the exact `IMPACT_PENALTY` table and per-node rationale) and its "Assumptions Log" entry A1 (per-node, not per-rule, is the adopted interpretation)</read_first>
  <behavior>
    - `createAxeEvaluator().evaluate({page})` against `tests/fixtures/eval/app-clean.html` (rendered via `renderWithPage`) returns `rawScore === 1` and `detail.violationCount === 0`.
    - `createAxeEvaluator().evaluate({page})` against `tests/fixtures/eval/app.html` (has the deliberate missing-`alt` violation) returns `0 <= rawScore < 1` and `detail.violationCount >= 1` — asserts the score is strictly lower than the clean fixture's, without hardcoding axe-core's exact impact classification for `image-alt` (that classification is axe-core's own taxonomy, not this evaluator's to assume).
    - The penalty is never negative — `rawScore` is always in `[0, 1]` even in a hypothetical high-violation-count case (assert via a direct unit-level call to the penalty math with a synthetic violations array, not requiring a real page for this specific case).
  </behavior>
  <action>
    Create `src/eval/axeEvaluator.ts` exporting `createAxeEvaluator(): EvaluatorPort` with `name: "axe"`. Define the module-level penalty table exactly as 03-RESEARCH.md specifies: `const IMPACT_PENALTY: Record<string, number> = { critical: 0.5, serious: 0.25, moderate: 0.1, minor: 0.05 }` (D3-10 — per-node, not per-rule: a rule violated on 5 elements penalizes 5x). Inside `evaluate(rawInput: unknown)`, cast to `{ page: Page }` (same Playwright-`Page`-typing allowance as `domEvaluator.ts`), run `const results = await new AxeBuilder({ page }).analyze();` (import `AxeBuilder` from `"@axe-core/playwright"`), then for each `violation` in `results.violations`, add `(IMPACT_PENALTY[violation.impact ?? "minor"] ?? IMPACT_PENALTY.minor) * violation.nodes.length` to a running `penalty` total, and accumulate a `byImpact` breakdown keyed by impact level (node counts, for `detail`). Return `{ rawScore: Math.max(0, 1 - penalty), detail: { violationCount: results.violations.length, byImpact, penalty } }` — the `Math.max(0, ...)` is the floor. Write `tests/axeEvaluator.integration.test.ts` against `vitest.integration.config.ts`, reusing the same `renderWithPage` + `file://` pattern as Task 1's DOM test (against `app-clean.html` and `app.html` respectively), plus one plain unit-level assertion (no page/browser needed) that directly exercises the `Math.max(0, ...)` floor logic against a large synthetic violations count to prove the score can never go negative.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/axeEvaluator.integration.test.ts</automated>
  </verify>
  <done>All three behavior cases pass; the clean fixture scores exactly `1`, the violating fixture scores strictly less than `1`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `expectedElements` selector strings (already zod-validated in 03-01-PLAN.md) → `page.locator(selector)` | A malformed CSS selector reaching `page.locator()` |
| axe-core's injected JS runs inside the rendered page | Standard, well-audited third-party accessibility scanner, not this evaluator's own code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-06 | Tampering (indirect) | `src/eval/domEvaluator.ts` selector handling | low | accept | `expectedElements` values already pass `.strict()` zod validation as plain strings at scenario-load time (03-01-PLAN.md); a syntactically invalid CSS selector at worst throws inside `page.locator().count()`, which is a per-evaluator failure the orchestrator (03-05-PLAN.md) already classifies as a drop, not a security boundary break |
| T-3-10 | Tampering (accept — third-party scanner) | `src/eval/axeEvaluator.ts` via `@axe-core/playwright` | low | accept | `axe-core` is Deque's official, widely-audited accessibility engine (Approved in 03-01-PLAN.md's Package Legitimacy Audit); this evaluator only reads its structured `.violations[]` output, it does not execute arbitrary axe-core-supplied code beyond the library's own documented API |

</threat_model>

<artifacts_produced>
- `src/eval/domEvaluator.ts` — `createDomEvaluator(expectedElements): EvaluatorPort`
- `src/eval/axeEvaluator.ts` — `createAxeEvaluator(): EvaluatorPort`, `IMPACT_PENALTY`
- `tests/fixtures/eval/app.html`, `tests/fixtures/eval/app-clean.html`
</artifacts_produced>

<verification>
`npx vitest run --config vitest.integration.config.ts tests/domEvaluator.integration.test.ts tests/axeEvaluator.integration.test.ts` — both green against real headless Chromium.
</verification>

<success_criteria>
`createDomEvaluator()` and `createAxeEvaluator()` both satisfy `EvaluatorPort`, both consume (never manage the lifecycle of) a `renderWithPage()`-produced `page`, and both are ready for `registry.ts` (03-06-PLAN.md) to compose.
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-pipeline-scorer/03-04-SUMMARY.md` when done
</output>
