---
phase: 01-foundations-contracts
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/storage/artifacts.ts
  - tests/artifacts.test.ts
autonomous: true
requirements: [STORE-03]
must_haves:
  truths:
    - "An artifact written to the on-disk store under results/<run_id>/ is retrievable via a relative-path link stored in the DB artifacts table"
    - "The DB stores relative paths only, never artifact blobs"
    - "An artifact path that would escape results/<run_id>/ (path traversal) is rejected before any write"
  artifacts:
    - path: src/storage/artifacts.ts
      provides: "writeArtifact (bytes -> results/<run_id>/ + artifacts row) and getArtifactPath (id -> relative path)"
      exports: ["writeArtifact", "getArtifactPath"]
      contains: "results"
  key_links:
    - from: "src/storage/artifacts.ts writeArtifact"
      to: "artifacts table + results/<run_id>/ on disk"
      via: "write bytes to disk, INSERT relative path into artifacts (prepared stmt), return the row id/path"
      pattern: "artifacts"
    - from: "src/storage/artifacts.ts getArtifactPath"
      to: "on-disk file"
      via: "SELECT path from artifacts by id -> the stored relative path resolves to the written bytes"
      pattern: "prepare\\("
---

<objective>
Build the on-disk artifact store (STORE-03, D-15/25): write artifact bytes under `results/<run_id>/`, record a relative-path link (never a blob) in the `artifacts` table, and retrieve the bytes back through the DB link — with path-containment so no filename can escape the run's results directory (V12).

Purpose: Makes Success Criterion #5 TRUE — an artifact written to the on-disk store is retrievable via a link stored in the DB. This is what the Phase 5 HTML report browses.
Output: `src/storage/artifacts.ts` + `tests/artifacts.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@src/storage/db.ts
@src/storage/schema.sql.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: On-disk artifact store with DB link + path containment</name>
  <files>src/storage/artifacts.ts, tests/artifacts.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-15 (artifacts on disk under results/<run_id>/; DB stores relative paths only, never blobs), D-25 (screenshots specializes artifacts; artifacts is the generic store kind/path/run_id/sha), D-22 (run_id reused verbatim as the results/<run_id> dir name).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Security Domain" (path traversal into results/<...> — join+normalize and assert the path stays within results/<run_id>/ before write), "Pattern 4" (sha256 via node:crypto for the artifact sha).
    - src/storage/db.ts + src/storage/schema.sql.ts (Plan 03) — openDb and the artifacts table columns (id, run_id, kind, path, sha, created_at).
  </read_first>
  <behavior>
    - writeArtifact(db, runId, kind, filename, bytes) writes bytes to results/<runId>/<filename>, inserts an artifacts row with the relative path (relative to the results root) and a sha256 of the bytes, and returns the new row id.
    - getArtifactPath(db, id) returns the stored relative path; reading that path (resolved against the results root) yields bytes deep-equal to what was written.
    - The artifacts row stores a relative path string, not the bytes (no BLOB column populated).
    - writeArtifact called with a traversing filename (containing ../ that would escape results/<runId>/) throws before writing anything to disk or the DB.
  </behavior>
  <action>
    src/storage/artifacts.ts: export writeArtifact(db, runId, kind, filename, bytes) and getArtifactPath(db, id). Resolve the run dir as results/<runId> under a results root; build the target with node:path join then normalize/resolve and assert the resolved absolute path is still inside the resolved run dir (reject with a thrown Error otherwise — V12 containment) before any mkdir/write. Create the dir recursively, writeFileSync the bytes, compute sha = sha256(bytes) via node:crypto, and INSERT into artifacts (run_id, kind, path=the path relative to the results root, sha, created_at=Date.now()) via a prepared statement with bound params; return the inserted row id. getArtifactPath runs a prepared SELECT path FROM artifacts WHERE id = ? and returns the relative path (or null). Keep run_id trusted (it is generated per D-22, not user input) but still enforce containment on the filename.
    tests/artifacts.test.ts: open a tmp-file DB (openDb) with the schema, write bytes to results/<runId>/x.png, assert the artifacts row holds a relative path (not the bytes), read the path back via getArtifactPath and assert the on-disk bytes deep-equal the input; assert a filename with ../ throws and leaves no file/row behind. Point the results root at a tmp dir for the test and clean it up.
  </action>
  <verify>
    <automated>npx vitest run tests/artifacts.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/artifacts.test.ts` exits 0.
    - src/storage/artifacts.ts uses `prepare(` for both the INSERT and SELECT (bound params).
    - The artifacts row stores a relative path string; the test asserts the DB value is a path, not the byte buffer (D-15).
    - A traversing filename throws before write; the round-trip (write -> DB link -> read) returns identical bytes (SC#5).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>writeArtifact persists bytes under results/<run_id>/ + a relative-path artifacts row, getArtifactPath retrieves it, path traversal is rejected, and artifacts.test.ts is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files:** `src/storage/artifacts.ts`, `tests/artifacts.test.ts`
- **Functions:** `writeArtifact()`, `getArtifactPath()`
- **On-disk layout:** `results/<run_id>/<filename>` (bytes on disk; DB holds relative-path link only)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact filename → filesystem path under results/<run_id>/ | A filename resolves to a path where bytes are written on disk |

ASVS L1 scope: this plan is the phase's V12 (Files & Resources) control point. No auth, network, or secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-V12-01 | Tampering | src/storage/artifacts.ts writeArtifact | high | mitigate | Join+normalize the target path and assert it stays inside the resolved results/<run_id>/ dir before any mkdir/write; reject traversal (../) with a thrown Error. run_id is generated (D-22), not user input, but the filename is still contained. |
| T-1-V12-02 | Tampering | artifacts persistence | medium | mitigate | Store a relative path + sha256 only (D-15); no blobs in SQLite. The sha lets a later phase detect on-disk tampering/drift. INSERT via prepared statement (bound params). |
</threat_model>

<verification>
- `npx vitest run tests/artifacts.test.ts` green (write -> DB link -> read round-trip; traversal rejected).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
An artifact written to the on-disk store is retrievable via a relative-path link stored in the DB, and a traversing path is rejected (SC#5, V12).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-04-SUMMARY.md` when done.
</output>
