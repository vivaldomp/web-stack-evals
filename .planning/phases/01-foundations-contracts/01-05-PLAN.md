---
phase: 01-foundations-contracts
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/manifest/fingerprint.ts
  - src/manifest/manifest.ts
  - tests/manifest.test.ts
autonomous: true
requirements: [SPEC-04, STORE-02, TEL-01]
must_haves:
  truths:
    - "Starting a run produces a stamped manifest: spec snapshot + version stamps + input fingerprint, persisted to the runs row"
    - "The input fingerprint is a content-hash of everything the agent sees — resolved spec values PLUS actual asset bytes — so a swapped mockup changes the fingerprint"
    - "The fingerprint is a top-level hash plus per-component hashes, so a mismatch names which input changed"
    - "Version stamps (node/dep/model, with browser+live-model injected later) are stored separately from the fingerprint"
  artifacts:
    - path: src/manifest/fingerprint.ts
      provides: "canonicalJSON, sha256, fingerprint() -> { top, components }"
      exports: ["fingerprint", "canonicalJSON"]
      contains: "createHash"
    - path: src/manifest/manifest.ts
      provides: "VersionStamp type, buildManifest(), persistManifest()"
      exports: ["buildManifest", "persistManifest", "VersionStamp"]
      contains: "VersionStamp"
  key_links:
    - from: "src/manifest/manifest.ts buildManifest"
      to: "src/manifest/fingerprint.ts + injected VersionStamp"
      via: "fingerprint(resolved specs + asset bytes) folded into the manifest alongside the injected version stamp"
      pattern: "fingerprint\\("
    - from: "src/manifest/manifest.ts persistManifest"
      to: "runs table (Plan 03 schema)"
      via: "prepared INSERT of run_id + manifest JSON + fingerprint + fingerprint_components + version_stamp"
      pattern: "prepare\\("
---

<objective>
Build the run manifest + input fingerprint (SPEC-04, STORE-02, D-10/11/12): a content-hash over everything the agent sees (resolved spec values PLUS raw asset bytes) with a top-level + per-component structure, plus version stamps captured separately, all folded into a stamped manifest persisted to the `runs` row.

Purpose: Makes Success Criterion #3 TRUE — starting a run produces a stamped run manifest persisted to the runs row. This is the reproducibility spine: two runs with an equal fingerprint provably saw identical inputs; a version change is attributable separately from an input change.
Output: `src/manifest/fingerprint.ts` + `src/manifest/manifest.ts` + `tests/manifest.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@src/specs/types.ts
@src/storage/db.ts
@src/storage/schema.sql.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Input fingerprint over spec values + asset bytes</name>
  <files>src/manifest/fingerprint.ts, tests/manifest.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-10 (hash everything the agent sees: resolved spec values + actual asset bytes; path-only hashing forbidden), D-11 (top-level fingerprint + per-component hashes so a mismatch names which input changed).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Pattern 4" (stdlib node:crypto sha256; canonicalJSON = recursive sorted-key JSON.stringify), "Open Questions" #2 (canonical serialization recommendation), "Anti-Patterns" (path-only fingerprint).
    - tests/fixtures/scenarios/dashboard/{mockup.png,expected.png} (Plan 02) — real bytes to hash and to mutate for the change-detection test.
  </read_first>
  <behavior>
    - fingerprint(inputs) is stable: two calls over identical resolved specs + identical asset bytes produce an equal top-level hash and equal per-component hashes.
    - Flipping one byte of the mockup changes the top-level hash AND the mockup component hash, while the stack/model component hashes stay equal (D-11 names which input changed).
    - canonicalJSON produces the same string regardless of input key order (recursive sorted keys), so two specs equal-by-value hash equally.
  </behavior>
  <action>
    src/manifest/fingerprint.ts: export sha256(buf|string) using createHash("sha256") from node:crypto (hex digest); export canonicalJSON(value) doing a recursive sorted-key JSON.stringify (no dependency — ~10 lines); export fingerprint(inputs) returning { top, components } where components is a map of per-item hashes: stack=sha256(canonicalJSON(resolvedStack)), model=sha256(canonicalJSON(resolvedModel)), scenario=sha256(canonicalJSON(scenario spec values)), prompt=sha256(prompt text), mockup=sha256(mockupBytes), expected=sha256(expectedBytes), and skills=sha256 over the concatenated sorted skill-file bytes when present (D-10). top = sha256 of the sorted list of "name:hash" component entries (D-11). Document the algorithm name/version inline so a future change is a methodology bump.
    tests/manifest.test.ts (fingerprint section): load the Plan 02 fixtures, build fingerprint twice and assert deep equality; then pass a mockup buffer with one byte flipped and assert top differs and components.mockup differs while components.stack/model stay equal.
  </action>
  <verify>
    <automated>npx vitest run tests/manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/manifest.test.ts` exits 0.
    - src/manifest/fingerprint.ts references `createHash` and exports fingerprint + canonicalJSON.
    - Test proves: identical inputs -> equal top+components; one flipped mockup byte -> different top AND different components.mockup, same components.stack.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>fingerprint() content-hashes resolved spec values + raw asset bytes into a stable top-level + per-component structure that pinpoints which input changed; fingerprint tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildManifest (injected VersionStamp) + persist to runs row</name>
  <files>src/manifest/manifest.ts, tests/manifest.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-12 (stamp versions explicitly + separately from the fingerprint: node, dep versions/lockfile hash, Playwright+Chromium revision, model id+params), D-18 (runs row stores an immutable manifest snapshot), D-22 (run_id reused as the runs PK), D-26 (epoch ms / decimal USD units).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Open Questions" #1 (buildManifest accepts version data as an injected VersionStamp param so Phase 1 defines the shape and captures node/dep/model statically; Playwright/Chromium + live-model fields are filled by later phases — keeps Phase 1 free of Playwright/Pi imports), Assumptions A2/A3.
    - src/manifest/fingerprint.ts (Task 1) — fingerprint() output folded into the manifest.
    - src/storage/db.ts + src/storage/schema.sql.ts (Plan 03) — openDb + the runs columns (run_id, manifest, fingerprint, fingerprint_components, version_stamp, status, started_at).
    - src/specs/types.ts (Plan 02) — Stack/Scenario/ModelConfig typed inputs.
  </read_first>
  <behavior>
    - buildManifest(input) returns a manifest with runId, a specSnapshot (resolved stack+scenario+model values, D-18), the fingerprint { top, components } from Task 1, and the injected versionStamp; browser/live-model fields on the stamp may be null in Phase 1.
    - persistManifest(db, manifest) inserts a runs row keyed by runId with manifest JSON, fingerprint, fingerprint_components JSON, version_stamp JSON, and started_at (epoch ms); reading the runs row back yields the same fingerprint and a JSON.parse'd manifest deep-equal to the built one.
    - Two buildManifest calls over identical inputs yield the same fingerprint.top (SPEC-04 reproducibility).
  </behavior>
  <action>
    src/manifest/manifest.ts: export a VersionStamp type = { node: string; dependencies: Record<string,string> | { lockfileHash: string }; playwright: string | null; chromium: string | null; modelId: string; modelParams: Record<string, unknown> } (D-12; Playwright/Chromium injected later per Open Question 1). Export buildManifest(input) taking the resolved specs (Stack, Scenario, ModelConfig), the asset bytes (prompt text, mockup, expected, skills), a runId, and a VersionStamp; it calls fingerprint() from Task 1, assembles { runId, specSnapshot, fingerprint, versionStamp, createdAt: Date.now() }, and returns it (do NOT import Playwright or the Pi SDK — the caller injects those versions). Export persistManifest(db, manifest) running a prepared INSERT into runs binding run_id, manifest=JSON.stringify(manifest), fingerprint=manifest.fingerprint.top, fingerprint_components=JSON.stringify(manifest.fingerprint.components), version_stamp=JSON.stringify(manifest.versionStamp), status='completed' or a starting status, started_at=manifest.createdAt (bound params only). For Phase 1 the node field is process.version and modelId/modelParams come from the loaded ModelConfig (SPEC-03); playwright/chromium are null.
    tests/manifest.test.ts (manifest section): build a manifest from the Plan 02 fixtures + a stub VersionStamp with null browser fields, persist it into a tmp-file DB (openDb), SELECT the runs row back and assert the stored fingerprint equals manifest.fingerprint.top and JSON.parse(row.manifest) deep-equals the built manifest; assert a second buildManifest over the same inputs has an equal fingerprint.top.
  </action>
  <verify>
    <automated>npx vitest run tests/manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/manifest.test.ts` exits 0.
    - src/manifest/manifest.ts exports a VersionStamp type with playwright and chromium fields (injected-later shape) and does NOT import playwright or @earendil-works.
    - `grep -cE "from ['\"](playwright|@earendil-works)" src/manifest/manifest.ts` returns 0.
    - persistManifest uses `prepare(` and writes to the runs row; reading it back returns a fingerprint equal to manifest.fingerprint.top and a manifest deep-equal after JSON.parse.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>buildManifest folds the fingerprint + injected VersionStamp + spec snapshot into a manifest; persistManifest stamps it onto the runs row and it reads back identically; version stamps are stored separately from the fingerprint; manifest tests green.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files:** `src/manifest/fingerprint.ts`, `src/manifest/manifest.ts`, `tests/manifest.test.ts`
- **Types:** `VersionStamp`, `Manifest` (the buildManifest return shape)
- **Functions:** `sha256()`, `canonicalJSON()`, `fingerprint()`, `buildManifest()`, `persistManifest()`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved specs + asset bytes → fingerprint/manifest | Inputs are hashed for reproducibility integrity and snapshotted to the runs row |

ASVS L1 scope: V6 (partial — non-secret integrity hashing) + parameterized persistence. No auth, network, or secrets in this phase (model API keys enter only in Phase 4).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-REPRO-01 | Tampering / Repudiation | src/manifest/fingerprint.ts | high | mitigate | Fingerprint the actual asset BYTES (not paths) plus resolved spec values (D-10); a swapped mockup/prompt changes the hash, closing the reproducibility-illusion gap. sha256 via node:crypto — never hand-roll a hash. |
| T-1-REPRO-02 | Repudiation | version stamps vs fingerprint | medium | mitigate | Stamp node/dep/model (and injected browser/live-model) versions separately from the fingerprint (D-12) so a score change is attributable to an env change vs an input change. |
| T-1-SQL-03 | Tampering | persistManifest | medium | mitigate | INSERT into runs via prepared statement with bound params (no string-concatenated SQL). |
</threat_model>

<verification>
- `npx vitest run tests/manifest.test.ts` green (stable fingerprint; byte-change detection; persist + read-back identical).
- `grep -cE "from ['\"](playwright|@earendil-works)" src/manifest/manifest.ts` == 0 (no Playwright/Pi imports — D-23).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Starting a run produces a stamped manifest (spec snapshot + version stamps + input fingerprint) persisted to the runs row; the fingerprint hashes bytes the agent sees and names which component changed (SC#3, SPEC-04, STORE-02).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-05-SUMMARY.md` when done.
</output>
