---
phase: 01-foundations-contracts
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/storage/schema.sql.ts
  - src/storage/db.ts
  - tests/db.test.ts
autonomous: true
requirements: [STORE-01, TEL-01]
must_haves:
  truths:
    - "Opening the DB initializes the full rep-keyed schema (runs, stacks, artifacts, events, metrics, screenshots, tool_calls, iterations, evaluations) in WAL mode"
    - "Schema init is idempotent — re-opening an existing DB does not error or duplicate tables (guarded by user_version)"
    - "An event appended to the events table reads back byte-identically after JSON round-trip"
    - "Writes go through prepared statements with bound params (no string-concatenated SQL)"
  artifacts:
    - path: src/storage/schema.sql.ts
      provides: "SCHEMA_SQL (CREATE TABLE IF NOT EXISTS x all tables) + SCHEMA_VERSION const"
      exports: ["SCHEMA_SQL", "SCHEMA_VERSION"]
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: src/storage/db.ts
      provides: "openDb (WAL + idempotent init), appendEvent, readEvents"
      exports: ["openDb", "appendEvent", "readEvents"]
      contains: "journal_mode = WAL"
  key_links:
    - from: "src/storage/db.ts"
      to: "src/storage/schema.sql.ts"
      via: "openDb execs SCHEMA_SQL once when user_version < SCHEMA_VERSION"
      pattern: "user_version"
    - from: "src/storage/db.ts appendEvent/readEvents"
      to: "events table"
      via: "prepared statement INSERT/SELECT with bound params; payload = JSON.stringify(event), read back via JSON.parse"
      pattern: "prepare\\("
---

<objective>
Create the SQLite results DB: the full rep-keyed 9+ table schema (STORE-01, D-13/14/18/19/20/21/25), an idempotent WAL init guarded by `user_version` (D-16/17), and the append-only event log (TEL-01, D-16) with append + read-back-identically over prepared statements.

Purpose: Makes Success Criterion #4 TRUE — the DB initializes with the full schema in WAL/single-writer mode and an appended event reads back identically. The `events` table IS the canonical append-only log every downstream projection folds from.
Output: `src/storage/schema.sql.ts` + `src/storage/db.ts` + `tests/db.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@src/core/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the rep-keyed SQL schema</name>
  <files>src/storage/schema.sql.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-13 (generic events table + promoted tool_name), D-14 (rep-keyed: every run carries stack/model/scenario/rep_index, v1 rep_index=0), D-18 (stacks/models/scenarios registries + immutable manifest snapshot on runs), D-19 (runs.status enum + failed_stage), D-20 (evaluations one row per run/rep/evaluator + detail JSON), D-21 (composite stored with its weights, separate from raw), D-24 (metrics/tool_calls/iterations projection tables), D-25 (screenshots specializes artifacts), D-26 (integer epoch ms / integer ms / decimal USD).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — Assumptions Log A1 (table-to-decision mapping; exact column names are the executor's discretion), "Recommended Project Structure".
    - .planning/ROADMAP.md — Phase 1 Success Criterion #4 (exact required table names).
  </read_first>
  <action>
    Export SCHEMA_VERSION = 1 and SCHEMA_SQL, a single string of CREATE TABLE IF NOT EXISTS statements (static DDL only). Create the 9 tables named in SC#4 — runs, stacks, artifacts, events, metrics, screenshots, tool_calls, iterations, evaluations — PLUS the models and scenarios registry tables required by D-18 (same registry pattern as stacks). Key shapes: stacks/models/scenarios = registries with a name + the resolved spec fields + created_at (D-18). runs = run_id TEXT PRIMARY KEY (the D-22 id), nullable FKs stack_id/model_id/scenario_id, rep_index INTEGER NOT NULL DEFAULT 0 (D-14), status TEXT (D-19 enum values) + failed_stage TEXT NULL, manifest JSON (immutable snapshot, D-18), fingerprint TEXT + fingerprint_components JSON (D-11), version_stamp JSON (D-12), composite_score REAL NULL + composite_weights JSON NULL (D-21), started_at + finished_at INTEGER (epoch ms, D-26). events = (run_id, seq, type, ts, tool_name, payload) with PRIMARY KEY (run_id, seq) (D-04 append order authoritative), tool_name promoted+nullable (D-13). artifacts = (id, run_id, kind, path, sha, created_at) storing a relative path only (D-15). screenshots specializes artifacts via artifact_id FK + role (expected|generated|diff) + viewport + dpr (D-25). evaluations = (run_id, rep_index, evaluator_name, raw_score, detail JSON) UNIQUE(run_id, rep_index, evaluator_name) (D-20). metrics/tool_calls/iterations = projection tables FK'd to run_id (D-24). Add indexes on events(run_id, type) and events(tool_name) for hot metric folds (RESEARCH Open Question 3). FKs to registries are nullable so Phase 1 (no run executes yet) can persist a manifest without populating registries.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "CREATE TABLE IF NOT EXISTS" src/storage/schema.sql.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS" src/storage/schema.sql.ts` returns 11 or more (9 SC#4 tables + models + scenarios).
    - SCHEMA_SQL text contains each of: runs, stacks, models, scenarios, artifacts, events, metrics, screenshots, tool_calls, iterations, evaluations.
    - runs table declares rep_index with DEFAULT 0 (D-14) and columns manifest, fingerprint, version_stamp.
    - events declares PRIMARY KEY over (run_id, seq) and a tool_name column (D-13).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>SCHEMA_SQL creates all SC#4 tables plus the models/scenarios registries, rep-keyed, with the generic events log + promoted tool_name and the projection/evaluation tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: openDb (WAL + idempotent init) + append-only event log</name>
  <files>src/storage/db.ts, tests/db.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-16 (events table IS the log; single-writer, WAL, seq-ordered), D-17 (single idempotent init guarded by user_version; NO migration framework).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Pattern 2" (WAL + user_version idempotent init), "Pattern 3" (append event, read back identically), "Pitfall 4" (set busy_timeout defensively; single writer).
    - src/storage/schema.sql.ts (Task 1) — SCHEMA_SQL + SCHEMA_VERSION.
    - src/core/events.ts — the AgentEvent union whose values are appended/read.
  </read_first>
  <behavior>
    - openDb(file) on a fresh path sets journal_mode to wal, then all SC#4 tables exist; a second openDb(file) on the same path does not re-run DDL (user_version already at SCHEMA_VERSION) and does not throw.
    - appendEvent then readEvents(runId) returns events in seq order; each returned event deep-equals the original AgentEvent (JSON.stringify -> stored payload -> JSON.parse round-trip is lossless).
    - Inserting two events with the same (run_id, seq) violates the primary key (append order is authoritative).
  </behavior>
  <action>
    src/storage/db.ts: export openDb(file) that news a better-sqlite3 Database, runs pragma journal_mode = WAL, pragma foreign_keys = ON, pragma busy_timeout = a few seconds (D-16/Pitfall 4), reads pragma user_version (simple), and when it is below SCHEMA_VERSION execs SCHEMA_SQL then sets user_version = SCHEMA_VERSION (D-17 idempotent init). Export appendEvent(db, event) using a prepared INSERT into events binding run_id/seq/type/ts/tool_name (null unless the event has toolName)/payload=JSON.stringify(event) — bound params only, never string-concatenated SQL. Export readEvents(db, runId) using a prepared SELECT ordered by seq that JSON.parse's each payload back into an AgentEvent. Wrap a batch append in db.transaction() so append-and-read-back is atomic (SC#4).
    tests/db.test.ts: use a tmp file DB (node:os tmpdir + a run id, not :memory:, so WAL is real). Assert pragma journal_mode === wal; assert every SC#4 table exists (query sqlite_master); assert a second openDb does not throw and leaves tables intact; append a sample event of each variant and assert readEvents returns them seq-ordered and deep-equal to the originals.
  </action>
  <verify>
    <automated>npx vitest run tests/db.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/db.test.ts` exits 0.
    - src/storage/db.ts contains `journal_mode = WAL` and `user_version`.
    - src/storage/db.ts uses `prepare(` for both the event INSERT and SELECT (bound params; no string-concatenated SQL).
    - db.test.ts proves journal_mode=wal, all 9 SC#4 tables present, idempotent re-open, and lossless event round-trip.
  </acceptance_criteria>
  <done>openDb enables WAL and idempotently inits the schema; appendEvent/readEvents round-trip an event losslessly in seq order via prepared statements; db.test.ts green.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files:** `src/storage/schema.sql.ts`, `src/storage/db.ts`, `tests/db.test.ts`
- **Constants:** `SCHEMA_SQL`, `SCHEMA_VERSION`
- **Functions:** `openDb()`, `appendEvent()`, `readEvents()`
- **SQLite tables:** runs, stacks, models, scenarios, artifacts, events, screenshots, evaluations, metrics, tool_calls, iterations (rep-keyed; `events` = the canonical append-only log)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event payloads / row values → SQLite | Structured data is written to the results DB (the product's canonical output) |

ASVS L1 scope: parameterized persistence (V5-adjacent). No auth, network, or secrets in this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SQL-01 | Tampering | src/storage/db.ts appendEvent/readEvents | high | mitigate | All row writes/reads use better-sqlite3 prepared statements with bound params; the only exec of raw SQL is the static DDL SCHEMA_SQL (no interpolated values). Prevents SQL injection into the append-only log. |
| T-1-SQL-02 | Tampering | schema init | medium | mitigate | Idempotent CREATE TABLE IF NOT EXISTS guarded by user_version (D-17) — re-opening never corrupts or duplicates the canonical DB. |
</threat_model>

<verification>
- `npx vitest run tests/db.test.ts` green (WAL on, all SC#4 tables, idempotent re-open, lossless event round-trip).
- `grep -c "CREATE TABLE IF NOT EXISTS" src/storage/schema.sql.ts` >= 11.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
DB initializes the full rep-keyed schema in WAL/single-writer mode; an event appended to the log reads back identically (SC#4).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-03-SUMMARY.md` when done.
</output>
