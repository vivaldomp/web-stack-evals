---
phase: 01-foundations-contracts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/specs/schema.ts
  - src/specs/types.ts
  - src/specs/load.ts
  - tests/specs.test.ts
  - tests/fixtures/stacks/angular.yaml
  - tests/fixtures/stacks/angular.bad.yaml
  - tests/fixtures/scenarios/dashboard/dashboard.yaml
  - tests/fixtures/scenarios/dashboard/mockup.png
  - tests/fixtures/scenarios/dashboard/expected.png
  - tests/fixtures/models/deepseek4pro.json
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03]
must_haves:
  truths:
    - "A malformed stack.yaml / scenario.yaml / model config (unknown or typo'd key) is rejected with a clear multi-line error naming the offending key, before any run starts"
    - "Loading a valid stack.yaml + scenario.yaml + model config yields typed spec objects the rest of the system consumes with no hardcoded stack/model/scenario in core"
    - "The expected-screenshot in scenario.yaml carries a structured provenance block (source, tool, version, date)"
  artifacts:
    - path: src/specs/schema.ts
      provides: "z.strictObject schemas: StackSchema, ScenarioSchema, ModelSchema (+ ProvenanceSchema)"
      contains: "z.strictObject("
      exports: ["StackSchema", "ScenarioSchema", "ModelSchema"]
    - path: src/specs/types.ts
      provides: "z.infer typed exports consumed downstream (SPEC-02)"
      exports: ["Stack", "Scenario", "ModelConfig"]
    - path: src/specs/load.ts
      provides: "loadStack/loadScenario/loadModel: parse -> safeParse -> z.prettifyError throw"
      exports: ["loadStack", "loadScenario", "loadModel"]
    - path: tests/fixtures/stacks/angular.yaml
      provides: "Valid v1-row stack fixture (Angular @ 4200)"
    - path: tests/fixtures/models/deepseek4pro.json
      provides: "Valid v1-row model config fixture (DeepSeek 4 Pro)"
  key_links:
    - from: "src/specs/load.ts"
      to: "src/specs/schema.ts"
      via: "safeParse against the strictObject schema; z.prettifyError(result.error) on failure"
      pattern: "prettifyError"
    - from: "src/specs/types.ts"
      to: "src/specs/schema.ts"
      via: "z.infer<typeof StackSchema> etc."
      pattern: "z\\.infer"
---

<objective>
Build the declarative, zod-validated spec loaders (SPEC-01/02/03): strict schemas for `stack.yaml`, `scenario.yaml`, and the `models/<name>.json` config; loaders that parse-then-validate and throw a clear multi-line error on failure; and test fixtures shaped to the fixed v1 row (Angular @ 4200 + DeepSeek 4 Pro + "dashboard").

Purpose: Makes Success Criterion #1 (malformed spec rejected with a clear zod error before any run) and #2 (valid specs -> typed objects, nothing hardcoded in core) TRUE. Every later phase resolves inputs through these loaders.
Output: `src/specs/{schema,types,load}.ts` + the fixture tree (valid + one deliberately malformed variant).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1-row spec fixtures (valid + malformed)</name>
  <files>tests/fixtures/stacks/angular.yaml, tests/fixtures/stacks/angular.bad.yaml, tests/fixtures/scenarios/dashboard/dashboard.yaml, tests/fixtures/scenarios/dashboard/mockup.png, tests/fixtures/scenarios/dashboard/expected.png, tests/fixtures/models/deepseek4pro.json</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-07 (layout: stacks/name.yaml, models/name.json, scenarios/name/ with mockup + expected screenshot alongside its yaml), D-09 (provenance block), "Specific Ideas" (fixed v1 row: Angular @ port 4200 + DeepSeek 4 Pro + dashboard).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Pattern 1" field set each schema expects.
  </read_first>
  <action>
    Mirror the D-07 on-disk layout inside tests/fixtures/. angular.yaml carries template, install, build, start, port set to 4200, and viewport with width and height. angular.bad.yaml is identical to angular.yaml but adds ONE extra unknown/typo'd key (e.g. a mistyped viewport key) so z.strictObject rejects it — this drives the SPEC-01 rejection test. dashboard.yaml carries prompt (dashboard build instructions), an expected block of path (expected.png) plus a provenance object of source, tool, version, date per D-09, viewport with width and height, and an empty skills list. deepseek4pro.json carries provider, modelId, and a params object (temperature and any model params) — declarative model config, no model hardcoded in core (SPEC-03). Write tiny valid placeholder PNG bytes for mockup.png and expected.png (a minimal valid PNG signature plus a few pixels is enough — they exist so the fingerprint plan can hash real bytes; they need not be visually meaningful).
  </action>
  <verify>
    <automated>test -s tests/fixtures/stacks/angular.yaml && test -s tests/fixtures/stacks/angular.bad.yaml && test -s tests/fixtures/scenarios/dashboard/dashboard.yaml && test -s tests/fixtures/models/deepseek4pro.json && test -s tests/fixtures/scenarios/dashboard/mockup.png && test -s tests/fixtures/scenarios/dashboard/expected.png</automated>
  </verify>
  <acceptance_criteria>
    - All six fixture files exist and are non-empty.
    - dashboard.yaml contains a provenance sub-block with keys source, tool, version, date (D-09).
    - angular.bad.yaml differs from angular.yaml by exactly one extra/typo'd key (the strict-rejection trigger).
    - The fixture tree mirrors the D-07 layout (stacks/, scenarios/dashboard/, models/).
  </acceptance_criteria>
  <done>Valid v1-row fixtures plus one malformed stack variant and two placeholder PNGs exist under tests/fixtures/ in the D-07 layout.</done>
</task>

<task type="auto">
  <name>Task 2: Define strict zod schemas + z.infer types</name>
  <files>src/specs/schema.ts, src/specs/types.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-08 (strict, unknown keys rejected), D-09 (provenance block), "Claude's Discretion" (exact field names are the executor's to choose).
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Pattern 1" and "Pitfall 1 / State of the Art" (Zod 4: use z.strictObject; the deprecated Zod-3 chained-strict form on a plain z.object is forbidden).
    - tests/fixtures/stacks/angular.yaml + scenarios/dashboard/dashboard.yaml + models/deepseek4pro.json (Task 1) — schemas must accept the valid fixtures and reject the bad one.
  </read_first>
  <action>
    src/specs/schema.ts: define StackSchema, ScenarioSchema, ModelSchema, and a shared ProvenanceSchema using z.strictObject (D-08 — the Zod-4 strict-object constructor; the deprecated Zod-3 chained-strict form on a plain z.object is forbidden). StackSchema fields: template string, install string, build string, start string, port an int positive, viewport a strictObject of width int and height int. ProvenanceSchema fields: source, tool, version, date (D-09). ScenarioSchema fields: prompt string, expected a strictObject of path string plus provenance ProvenanceSchema, viewport (reuse the viewport shape), skills an array of string. ModelSchema fields: provider string, modelId string, params a record of model params. Every object uses z.strictObject so an unknown key yields an unrecognized_keys issue.
    src/specs/types.ts: export type Stack = z.infer of StackSchema, Scenario = z.infer of ScenarioSchema, ModelConfig = z.infer of ModelSchema (SPEC-02 — the typed objects the rest of the system consumes).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c strictObject src/specs/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/specs/schema.ts contains `z.strictObject(` for stack, scenario, model, and provenance.
    - `grep -c strictObject src/specs/schema.ts` returns 4 or more (a chained-strict form on a plain z.object would leave 0 strictObject matches and fail this check — the Zod-3 drift guard).
    - src/specs/types.ts exports Stack, Scenario, ModelConfig via z.infer; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Strict schemas for stack/scenario/model + provenance exist (Zod 4 z.strictObject), and z.infer typed exports compile clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement loaders with clear validation errors</name>
  <files>src/specs/load.ts, tests/specs.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md — D-08 (fail loudly before any run), Success Criterion #1 wording.
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md — "Pattern 1" (yaml.parse -> safeParse -> z.prettifyError throw), "Don't Hand-Roll" (use z.prettifyError, not a manual issues walk).
    - src/specs/schema.ts + src/specs/types.ts (Task 2) — the schemas the loaders validate against.
  </read_first>
  <behavior>
    - loadStack(path) on tests/fixtures/stacks/angular.yaml returns a typed Stack whose port === 4200.
    - loadStack(path) on tests/fixtures/stacks/angular.bad.yaml throws, and the thrown message contains the offending key name (the strict rejection) and the file path.
    - loadScenario(path) returns a Scenario whose expected.provenance.source is defined (D-09).
    - loadModel(path) on tests/fixtures/models/deepseek4pro.json returns a ModelConfig with provider and modelId (SPEC-03).
  </behavior>
  <action>
    src/specs/load.ts: export loadStack, loadScenario, loadModel. Each reads the file with readFileSync(path, "utf8"), parses (YAML.parse for the .yaml loaders, JSON.parse for the model .json loader), runs schema.safeParse on the parsed object, and on failure throws an Error whose message embeds the path plus z.prettifyError(result.error) (the Zod 4 multi-line formatter — SC#1); on success returns result.data typed via the src/specs/types.ts exports. No run may proceed on invalid input — the loader throws before returning.
    tests/specs.test.ts: cover the four behaviors above with vitest — a valid load per spec type asserting typed fields, and an expect(() => loadStack(bad)).toThrow(...) asserting the message contains the bad key.
  </action>
  <verify>
    <automated>npx vitest run tests/specs.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/specs.test.ts` exits 0.
    - src/specs/load.ts references `prettifyError` (clear-error requirement, SC#1).
    - Loading angular.bad.yaml throws an error whose message contains the offending key path; loading angular.yaml returns port 4200.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Loaders parse-then-validate all three spec types, return typed objects on success, and throw a path-prefixed z.prettifyError message on any unknown/typo'd key; specs.test.ts green.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files:** `src/specs/schema.ts`, `src/specs/types.ts`, `src/specs/load.ts`, `tests/specs.test.ts`, and the `tests/fixtures/` v1-row tree (stacks/angular.yaml + angular.bad.yaml, scenarios/dashboard/{dashboard.yaml, mockup.png, expected.png}, models/deepseek4pro.json)
- **Zod schemas:** `StackSchema`, `ScenarioSchema`, `ModelSchema`, `ProvenanceSchema`
- **Types:** `Stack`, `Scenario`, `ModelConfig`
- **Functions:** `loadStack()`, `loadScenario()`, `loadModel()`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec files (stack.yaml / scenario.yaml / model.json) → loader | Author-controlled config text is parsed and validated before any downstream use |

ASVS L1 scope: this plan is the phase's V5 (Input Validation) control point. No auth, network, or secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-V5-01 | Tampering | src/specs/load.ts + schema.ts | high | mitigate | z.strictObject rejects unknown/typo'd keys and safeParse-then-throw stops the run before any spec value is used (D-08, SC#1). A silently-accepted bad key would corrupt a benchmark row's inputs. |
| T-1-V5-02 | DoS / Tampering | YAML parsing of spec files | low | mitigate | Use the `yaml` (eemeli) parser which does not execute arbitrary tags by default; parse to plain data, then zod-validate. Specs are author-controlled in v1. |
</threat_model>

<verification>
- `npx vitest run tests/specs.test.ts` green (valid loads + malformed rejection).
- `grep -c strictObject src/specs/schema.ts` >= 4 (proves the Zod-4 strict-object API is used, not the deprecated Zod-3 chained form).
- `npx tsc --noEmit` clean (proves the z.infer typed-object contract, SPEC-02).
</verification>

<success_criteria>
Malformed stack/scenario/model rejected with a clear multi-line error naming the offending key before any run (SC#1); valid specs load into typed objects with no hardcoded stack/model/scenario in core (SC#2).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-02-SUMMARY.md` when done.
</output>
