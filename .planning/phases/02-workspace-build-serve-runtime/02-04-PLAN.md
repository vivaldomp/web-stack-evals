---
phase: 02-workspace-build-serve-runtime
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/render/determinism.ts
  - src/render/playwrightRenderer.ts
  - tests/fixtures/render/index.html
  - tests/determinism.selftest.test.ts
autonomous: true
requirements: [BUILD-03, BUILD-04]
must_haves:
  truths:
    - "A screenshot taken via the RenderPort implementation is exactly the declared viewport's pixel dimensions at deviceScaleFactor 1"
    - "Screenshotting the same fixture page twice yields <=0.1% differing pixels (BUILD-04 determinism self-test)"
    - "Console errors, uncaught page exceptions, and failed requests during rendering are captured as a non-fatal signal alongside a successful screenshot (D2-15), never blocking the screenshot"
    - "Only src/render/playwrightRenderer.ts imports the playwright package in this plan (D-23/D2-21)"
  artifacts:
    - path: src/render/determinism.ts
      provides: "installDeterminismControls(context) — freeze Date/Math.random, kill CSS motion+caret, block external font CDNs"
      exports: ["installDeterminismControls"]
    - path: src/render/playwrightRenderer.ts
      provides: "createPlaywrightRenderer(): RenderPort — the only playwright import in this plan"
      exports: ["createPlaywrightRenderer"]
    - path: tests/determinism.selftest.test.ts
      provides: "BUILD-04's screenshot-twice pixelmatch self-test"
  key_links:
    - from: "src/render/playwrightRenderer.ts createPlaywrightRenderer"
      to: "src/core/ports.ts RenderPort (Plan 02-01)"
      via: "the returned object's screenshot() method satisfies RenderPort exactly, so src/pipeline/runStack.ts (Plan 02-05) can depend on the interface alone"
      pattern: "RenderPort"
    - from: "src/render/determinism.ts installDeterminismControls"
      to: "tests/determinism.selftest.test.ts"
      via: "the same controls applied on every real screenshot are what make the self-test's <=0.1% threshold achievable at all"
      pattern: "reducedMotion"
---

<objective>
Implement the only module in this phase that imports `playwright` (D-23/D2-21): a `RenderPort` implementation that launches headless Chromium, applies the D2-11 determinism controls, captures the D2-15 non-fatal runtime page-error signals, and takes a fixed-viewport `deviceScaleFactor:1` screenshot — proven deterministic by its own self-test (BUILD-04, ROADMAP Success Criterion 5).

Purpose: Makes ROADMAP Success Criteria 4 and 5 TRUE (Playwright screenshot at declared viewport/dpr; determinism self-test passes) in isolation from the Angular pipeline, using a minimal static fixture so this plan does not depend on the (slower, heavier) template scaffold.
Output: `src/render/{determinism,playwrightRenderer}.ts`, `tests/fixtures/render/index.html`, `tests/determinism.selftest.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/02-workspace-build-serve-runtime/02-CONTEXT.md
@.planning/phases/02-workspace-build-serve-runtime/02-RESEARCH.md
@src/core/ports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determinism controls (D2-11)</name>
  <files>src/render/determinism.ts</files>
  <read_first>
    - 02-CONTEXT.md D2-11 (all four determinism controls: viewport-clip, kill motion, font stability, freeze time/random) and D2-12 (`deviceScaleFactor: 1`, `reducedMotion: 'reduce'` — these two are context-creation options, not part of this file; this file only covers the `addInitScript`/CSS/route pieces).
    - 02-RESEARCH.md Pattern 4 "Determinism Controls" code example — `context.addInitScript` for the frozen `Date`/`Math.random` stub, the CSS-injection `addInitScript` for killing animations/transitions/caret, and `page.route(/^https:\/\/fonts\.(googleapis|gstatic)\.com\/.*/, route => route.abort())` for blocking external font CDNs. Copy these patterns' exact mechanics (timing guarantee of `addInitScript` running "after the document was created but before any of its scripts were run").
    - CONTEXT.md's Assumption A2 (the fixed-epoch + simple LCG `Math.random` stub misses `crypto.getRandomValues` — acceptable, the fixture this plan controls only uses `Math.random`).
  </read_first>
  <action>
    Create src/render/determinism.ts exporting `async function installDeterminismControls(context: import("playwright").BrowserContext): Promise&lt;void&gt;`. Inside, call `context.addInitScript(...)` once with a function that freezes `Date`/`Date.now` to a fixed constant epoch (e.g. `1735689600000`) and overrides `Math.random` with a small deterministic LCG (e.g. seed `42`, matching 02-RESEARCH.md's Pattern 4 exactly) — both are page-side overrides evaluated before any page script runs. Call `context.addInitScript(...)` a second time with a function that injects a `&lt;style&gt;` element zeroing `animation-duration`/`animation-delay`/`transition-duration`/`transition-delay` and setting `caret-color: transparent` on `*, *::before, *::after` (kills CSS motion + hides the text caret, per D2-11). This function is `context`-scoped (applies to every page created in that context), not `page`-scoped — do not also expose a `page`-level variant; one context-level call covers every page.
    Export a second function `async function blockExternalFonts(page: import("playwright").Page): Promise&lt;void&gt;` calling `page.route(/^https:\/\/fonts\.(googleapis|gstatic)\.com\/.*/, (route) => route.abort())` — kept as a separate exported function (not folded into `installDeterminismControls`) because it is `page`-scoped, not `context`-scoped, and Plan 02-05's `playwrightRenderer.ts` calls it once per page after creating the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/render/determinism.ts exports exactly `installDeterminismControls` and `blockExternalFonts`.
    - `installDeterminismControls` calls `context.addInitScript` at least twice (Date/Math.random freeze + CSS injection) and never calls `page.route` itself (that responsibility is `blockExternalFonts`'s alone).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>installDeterminismControls freezes Date/Math.random and kills CSS motion/caret at the context level; blockExternalFonts aborts font-CDN requests at the page level — verified only by type-checking here, exercised for real by Task 2/3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RenderPort implementation (BUILD-03, D2-12/D2-15)</name>
  <files>src/render/playwrightRenderer.ts</files>
  <read_first>
    - src/core/ports.ts (post Plan 02-01) — the exact `RenderPort`/`RenderInput`/`RenderResult` shapes this implementation must satisfy.
    - 02-CONTEXT.md D2-10 (layered readiness gate — this file owns only the Playwright-side navigate+settle layer: `waitUntil: 'networkidle'` → `document.fonts.ready` → short fixed settle; the HTTP-poll layer is Plan 02-05's `src/runtime/readiness.ts`, called by the pipeline BEFORE this renderer is invoked), D2-12 (`channel: "chromium"`, `headless: true`, `deviceScaleFactor: 1`, `reducedMotion: "reduce"`), D2-15 (console errors + uncaught exceptions + failed requests captured as a non-fatal signal, screenshot still proceeds).
    - 02-RESEARCH.md Pattern 3 "Layered Readiness Gate" (the `navigateAndSettle` half only — the `waitForHttp200` half belongs to Plan 02-05, not here), Pattern 4 (`chromium.launch`/`browser.newContext` option shapes), "Runtime page-error capture" code example (`page.on('console'|'requestfailed')`, `context.on('weberror')` — prefer `weberror` over the older `page.on('pageerror')`), Pitfall 4 (never let `networkidle` alone be the gate — wrap it with its own short, independently-bounded race, since this layer runs only after Plan 02-05's HTTP-poll already proved the server is up).
    - src/render/determinism.ts (Task 1) — `installDeterminismControls`/`blockExternalFonts` signatures this file calls.
  </read_first>
  <behavior>
    - `createPlaywrightRenderer().screenshot({ url, viewport: {width, height} })` resolves to a `RenderResult` whose `png` buffer, decoded, has pixel width/height exactly equal to `viewport.width`/`viewport.height`.
    - Two calls to `screenshot()` against the same static URL (no navigation errors) each resolve successfully and close their own browser instance (no leaked browser process between calls — v1 is single-sequential, so a fresh launch+close per call is correct, not a shared pooled browser).
    - When the target page logs a console error or has an uncaught exception, `screenshot()` still resolves (does not reject) and the result's `consoleErrors`/`uncaughtExceptions` arrays are non-empty.
    - When navigation itself fails to ever reach the page (e.g. the URL is unreachable), `screenshot()` rejects (this is the one case Plan 02-05 must catch and map to a fatal outcome).
  </behavior>
  <action>
    Create src/render/playwrightRenderer.ts exporting `function createPlaywrightRenderer(): RenderPort` (import `RenderPort`/`RenderInput`/`RenderResult` type-only from "../core/ports.js"; import `chromium` as a value from "playwright" — this is the only file in the whole phase permitted to do so). The returned object's `screenshot(input: RenderInput): Promise&lt;RenderResult&gt;` method: launch `chromium.launch({ channel: "chromium", headless: true })`; create `browser.newContext({ viewport: input.viewport, deviceScaleFactor: 1, reducedMotion: "reduce" })`; call `installDeterminismControls(context)`; create `const page = await context.newPage()`; call `blockExternalFonts(page)`; register listeners before navigating — `page.on("console", msg => { if (msg.type() === "error") consoleErrors.push(msg.text()); })`, `context.on("weberror", (e) => uncaughtExceptions.push(String(e.error())))`, `page.on("requestfailed", (req) => failedRequests.push(\`${req.url()} ${req.failure()?.errorText ?? ""}\`))` (collect into three local arrays that become the result's fields). Navigate: `await page.goto(input.url, { waitUntil: "networkidle" })` wrapped so it cannot exceed roughly 12 seconds on its own (e.g. race it against a timeout promise) — per Pitfall 4, this is a bounded refinement layer, not the sole readiness gate, since Plan 02-05 already confirmed HTTP 200 before calling this. After navigation, `await page.evaluate(() => document.fonts.ready)`, then a short fixed settle (`await new Promise(r => setTimeout(r, 250))`). Take the screenshot: `const png = await page.screenshot({ type: "png" })` — no `fullPage` flag and no explicit `clip` object; Playwright's default (viewport-bounds-only capture) already satisfies D2-11's "viewport-clip at the declared size", since the context's own `viewport` option fixes the capture bounds. Always close the context/browser in a `finally` block (even on navigation failure) before the method's promise settles, so no browser process leaks past a single `screenshot()` call. Return `{ png, consoleErrors, uncaughtExceptions, failedRequests }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "playwright"' src/render/playwrightRenderer.ts` returns at least 1, and `grep -rlc 'from "playwright"' src/` (excluding this file) returns 0 — only this file imports playwright.
    - `createPlaywrightRenderer()` returns an object whose shape satisfies `RenderPort` (the file type-checks assigning it to a `const r: RenderPort = createPlaywrightRenderer()`).
    - The implementation closes the browser/context in a `finally` block (grep the function body for a `finally` clause wrapping the launch/navigate/screenshot sequence).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>createPlaywrightRenderer implements RenderPort with fixed viewport/dpr/reducedMotion, applies the Task-1 determinism controls, captures D2-15 page-error signals non-fatally, and always tears down its own browser instance — proven end-to-end by Task 3's self-test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Determinism self-test (BUILD-04) + minimal fixture</name>
  <files>tests/fixtures/render/index.html, tests/determinism.selftest.test.ts</files>
  <read_first>
    - 02-RESEARCH.md "Determinism self-test (BUILD-04, 'screenshot twice' self-test)" code example (`pixelmatch(imgA.data, imgB.data, null, width, height, { threshold: 0.1, includeAA: false })`, dimension-mismatch guard, `diffPct <= 0.1` assertion) and "Open Question 1" resolution (use a minimal static HTML fixture — not the real Angular template — so this self-test isolates Playwright's own determinism controls from Angular build variance and runs in milliseconds).
    - 02-CONTEXT.md D2-08 ("sirv-cli... dev-dependency in the template, not core") — confirms `sirv` must NOT be added as a platform-level dependency just for this test; serve the static fixture via Node's built-in `node:http` instead (no new dependency).
    - src/render/playwrightRenderer.ts (Task 2) — `createPlaywrightRenderer().screenshot(...)` is the exact function under test here; this self-test does not reimplement rendering, it calls the real renderer twice.
  </read_first>
  <behavior>
    - Screenshotting `tests/fixtures/render/index.html` (served locally) twice via `createPlaywrightRenderer().screenshot(...)` and diffing the two PNGs with pixelmatch yields <=0.1% differing pixels.
    - The fixture page itself contains a CSS animation, a webfont `&lt;link&gt;` (to an external font CDN host, to exercise `blockExternalFonts`), and one `Math.random()`-seeded element (to exercise the Date/Math.random freeze) — so a passing self-test is actually exercising all of Task 1's determinism controls, not merely a page too simple to reveal nondeterminism.
  </behavior>
  <action>
    Create tests/fixtures/render/index.html: a minimal static page with (a) an element using a CSS `@keyframes` animation with a multi-second duration, (b) a `&lt;link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto"&gt;` (or similar googleapis/gstatic URL — must match `blockExternalFonts`'s route pattern so the test also proves the block works), and (c) a small inline `&lt;script&gt;` that writes `Math.random()`'s value into the DOM on load.
    Create tests/determinism.selftest.test.ts: start a plain `node:http.createServer` in the test's `beforeAll` that serves this single fixture file's bytes (read once via `readFileSync`) for any request, listening on an ephemeral port (`server.listen(0)`, read the assigned port from `server.address()`); this avoids adding `sirv`/`sirv-cli` as a platform-level dependency (D2-08 scopes that to the template only). Call `createPlaywrightRenderer().screenshot({ url: \`http://localhost:${port}/\`, viewport: { width: 400, height: 300 } })` twice, decode both `png` buffers with `PNG.sync.read` (from "pngjs"), assert equal width/height, run `pixelmatch(a.data, b.data, null, width, height, { threshold: 0.1, includeAA: false })`, and assert the resulting `(numDiffPixels / (width*height)) * 100 &lt;= 0.1`. Close the http server in `afterAll`.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/determinism.selftest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.integration.config.ts tests/determinism.selftest.test.ts` exits 0.
    - The fixture HTML file contains a CSS `@keyframes`/`animation` rule, a `fonts.googleapis.com` or `fonts.gstatic.com` link/URL reference, and a `Math.random()` call.
    - The test asserts `diffPct &lt;= 0.1`, not exact pixel equality (the one legitimate threshold check in this phase, per VALIDATION.md's held-out-vs-direct-assertion split).
    - The test does NOT import or spawn `sirv`/`sirv-cli` (grep the test file: 0 occurrences) — it serves the fixture via `node:http` only.
  </acceptance_criteria>
  <done>tests/determinism.selftest.test.ts proves BUILD-04's determinism guarantee end-to-end through the real RenderPort implementation and a fixture exercising every Task-1 control; ROADMAP Success Criterion 5's determinism half is now provably TRUE.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files (this plan):** `src/render/determinism.ts`, `src/render/playwrightRenderer.ts`, `tests/fixtures/render/index.html`, `tests/determinism.selftest.test.ts`.
- **Functions:** `installDeterminismControls()`, `blockExternalFonts()`, `createPlaywrightRenderer()` (implements `RenderPort` from Plan 02-01).

**Explicit scope note on D2-12's "recorded in the manifest" clause:** D2-12 says the Chromium revision is "recorded in the manifest (D-12)". This plan implements the browser CHOICE (bundled Chromium, new-headless, pinned by the `playwright` version in package-lock.json) but does not itself call `buildManifest`/`persistManifest` — `runStack` (Plan 02-05) has no `scenario`/`model`/`prompt`/asset inputs to build a manifest from (D2-20's 3-arg signature). Stamping the Playwright/Chromium version into a run's manifest is Phase 5's job when it actually calls `persistManifest` for a real row, following the same precedent Phase 1 already set for this exact field (Phase 1's `VersionStamp.playwright`/`.chromium` were left `null`, "injected by later phases"). Phase 5 can read the version via `require("playwright/package.json").version` — no new export is needed from this plan for that.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered page (fixture today, agent-generated app in Phase 4) → this process | The only place this phase runs untrusted-in-the-future HTML/JS inside a real browser |
| External font CDN requests | The one outbound network call this module would otherwise make on every screenshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | Denial of Service | src/render/playwrightRenderer.ts navigation | medium | mitigate | The `page.goto(..., { waitUntil: "networkidle" })` call is wrapped in its own bounded race (~12s) per Pitfall 4 — a page with a perpetual background poll/beacon cannot hang this layer past its own short budget (the HTTP-poll layer in Plan 02-05 already bounds overall start-readiness separately). |
| T-2-07 | Information Disclosure | src/render/determinism.ts blockExternalFonts | low | mitigate | Aborting font-CDN requests prevents the rendered page from making any outbound network call during a screenshot — no external service ever sees a benchmark run's traffic, and no external content can vary the render non-deterministically. |
| T-2-08 | Resource Leak (Denial of Service, self-inflicted) | src/render/playwrightRenderer.ts browser lifecycle | low | mitigate | Browser/context close in a `finally` block on every `screenshot()` call (success or failure) — no orphaned Chromium process survives a single render, verified by the acceptance criterion requiring a `finally` clause. |
</threat_model>

<verification>
- `npx vitest run --config vitest.integration.config.ts tests/determinism.selftest.test.ts` green (BUILD-04).
- `npx tsc --noEmit` clean.
- Only `src/render/playwrightRenderer.ts` imports "playwright" anywhere under `src/`.
</verification>

<success_criteria>
A working `RenderPort` implementation exists, producing exact-viewport, dpr-1 screenshots with D2-15 non-fatal page-error capture, and its own self-test proves the determinism controls hold to <=0.1% pixel drift — independent of the Angular template, ready for Plan 02-05 to consume through the interface.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-build-serve-runtime/02-04-SUMMARY.md` when done.
</output>
</content>
