---
phase: 02-workspace-build-serve-runtime
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - tests/runStack.integration.test.ts
  - tests/isolation.selftest.test.ts
autonomous: true
requirements: [WORK-01, WORK-02, WORK-04, BUILD-01, BUILD-02, BUILD-03]
must_haves:
  truths:
    - "runStack against the real committed Angular template, run end-to-end, produces a screenshot artifact whose PNG dimensions equal the declared viewport at dpr=1 (BUILD-03, ROADMAP SC#4)"
    - "A forced install/build failure, start failure, and timeout each yield the correct distinct RunStatus + failedStage (BUILD-01, ROADMAP SC#3), proven against the real pipeline, not a mock"
    - "After runStack returns from a real start-stage run, port 4200 is confirmed free and the server process is confirmed gone (WORK-04, ROADMAP SC#5's teardown half)"
    - "Running runStack against the real template leaves the main project tree byte-identical before and after (WORK-02, ROADMAP SC#1's isolation half)"
  artifacts:
    - path: tests/runStack.integration.test.ts
      provides: "End-to-end proof of WORK-01/04, BUILD-01/02/03 against the real stacks/angular.yaml + stacks/angular/template/"
    - path: tests/isolation.selftest.test.ts
      provides: "Hash-tree-before/after proof of WORK-02 against the real copyWorkspace + runStack path"
  key_links:
    - from: "tests/runStack.integration.test.ts forced-timeout cases"
      to: "src/specs/schema.ts StackSchema.*TimeoutMs override fields (Plan 02-01)"
      via: "each forced-timeout test constructs an in-memory Stack with a tiny *TimeoutMs override + a deliberately slow command, forcing the branch in seconds instead of D2-17's multi-minute production defaults"
      pattern: "TimeoutMs"
    - from: "tests/isolation.selftest.test.ts"
      to: "src/workspace/copy.ts copyWorkspace (Plan 02-03)"
      via: "hashes the project tree (excluding tmp/results/node_modules/.git) before and after a real runStack call and asserts equality"
      pattern: "hashTree"
---

<objective>
Prove Phase 2's five ROADMAP success criteria end-to-end against the real, committed Angular template (Plan 02-02) and the assembled `runStack` pipeline (Plan 02-05) — not stubs. This is the phase's final wave: everything before it is a prerequisite for these tests actually running green.

Purpose: Nyquist's Wave-0 gap list names these two files explicitly (`tests/runStack.integration.test.ts` covering WORK-01/02/04 + BUILD-01/02/03; `tests/isolation.selftest.test.ts` covering WORK-02) as the phase's held-out proof; VALIDATION.md's per-task verification map cites their exact automated commands.
Output: `tests/runStack.integration.test.ts`, `tests/isolation.selftest.test.ts` — both run via `vitest.integration.config.ts` (Plan 02-01), never the fast default suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-workspace-build-serve-runtime/02-CONTEXT.md
@.planning/phases/02-workspace-build-serve-runtime/02-RESEARCH.md
@.planning/phases/02-workspace-build-serve-runtime/02-VALIDATION.md
@src/pipeline/runStack.ts
@src/specs/load.ts
@src/specs/types.ts
@src/storage/storagePort.ts
@stacks/angular.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Happy-path integration test (workspace, screenshot dims, non-fatal stages, dist size)</name>
  <files>tests/runStack.integration.test.ts</files>
  <read_first>
    - 02-VALIDATION.md "Per-Task Verification Map" — the exact automated commands this file must satisfy: `npx vitest run tests/runStack.integration.test.ts -t "creates workspace"`, `-t "non-fatal stages"`, `-t "screenshot dimensions"`.
    - src/pipeline/runStack.ts (Plan 02-05) — `runStack(stack, runId, storage)` signature and `RunOutcome` shape.
    - src/specs/load.ts — `loadStack("stacks/angular.yaml")` (Plan 02-02's real production spec).
    - src/storage/storagePort.ts (Plan 02-03) — `createStoragePort(db, resultsRoot)`.
    - src/storage/db.ts — `openDb(file)` (reuse the existing Phase-1 test convention of a `mkdtempSync`-based tmp DB file, matching tests/artifacts.test.ts's `setup()` helper style).
    - tests/artifacts.test.ts, tests/manifest.test.ts — existing test setup/teardown conventions (tmp dir via `mkdtempSync`, `afterEach` cleanup) — match this style rather than inventing a new one.
  </read_first>
  <action>
    Import `runStack` from "../src/pipeline/runStack.js", `loadStack` from "../src/specs/load.js", `openDb` from "../src/storage/db.js", `createStoragePort` from "../src/storage/storagePort.js", `newRunId` from "../src/core/ids.js", and `PNG` from "pngjs". In a `beforeAll`/`beforeEach`, `const stack = loadStack("stacks/angular.yaml")` (the real production spec) and set up a tmp-file DB + `resultsRoot` via `mkdtempSync` (results are written under this tmp root, not the repo's real `results/` dir, so the test suite stays self-contained and cleans up after itself — pass this `resultsRoot` into `createStoragePort(db, resultsRoot)`).
    Test "creates workspace and completes the happy path" (this is the slow one — a real `npm ci` + `ng build` + `ng lint` + `ng test` + `sirv` + Playwright screenshot against the committed template; rely on `vitest.integration.config.ts`'s raised `testTimeout` for headroom): call `const runId = newRunId(); const outcome = await runStack(stack, runId, storage)`; assert `outcome.status === "completed"`, `outcome.failedStage === null`, `outcome.screenshotArtifactId` is a non-null string; assert `existsSync(resolve("tmp", runId))` is `false` (D2-05 — deleted on success, this is the one place this test touches the real project-root `tmp/`, since `copyWorkspace`'s `tmpRoot` is not parameterized by `runStack` itself — clean it up defensively in an `afterEach` regardless of outcome, in case an assertion fails mid-test).
    Test "screenshot dimensions equal the declared viewport at dpr=1": from the same happy-path run (or a fresh one, executor's call whichever keeps the suite fastest without losing independence), resolve the written PNG bytes via `getArtifactPath`-equivalent (use the `db` directly: `SELECT path FROM artifacts WHERE id = ?` bound to the numeric form of `outcome.screenshotArtifactId`, or add a small local helper reading the file at `resultsRoot/<relative path>` — reuse the pattern from tests/artifacts.test.ts's round-trip assertion), decode with `PNG.sync.read`, and assert `width === stack.viewport.width && height === stack.viewport.height`.
    Test "non-fatal stages: lint/test do not block the screenshot": assert (from the happy-path outcome or by inspecting `storage.readEvents(runId)`) that `stage_started`/`stage_completed`/`stage_failed` events exist for `"lint"` and `"test"` (since `stacks/angular.yaml` declares both), and that the overall `outcome.status` is still `"completed"` regardless of whether the recorded lint/test exit codes happened to be zero or non-zero — the assertion is about the STATUS not being gated by lint/test, not about forcing a specific lint/test outcome (the template's actual lint/test result on a fresh scaffold is not this test's concern).
    Test "dist size is captured in meta.json": read back the `"meta"`-kind artifact written for this run (same DB-lookup pattern as the screenshot dims test, filtered by `kind = 'meta'`), `JSON.parse` its bytes, and assert `typeof meta.distBytes === "number" && meta.distBytes > 0`.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/runStack.integration.test.ts -t "creates workspace"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.integration.config.ts tests/runStack.integration.test.ts` exits 0 for the happy-path, screenshot-dimensions, non-fatal-stages, and dist-size tests.
    - The happy-path test asserts `outcome.status === "completed"` and `outcome.failedStage === null` against a REAL `runStack` call (no stubbed `RenderPort`/`execa` — this is the one test in the whole phase that exercises the true, unstubbed pipeline end-to-end).
    - The screenshot-dimensions assertion decodes the actual written PNG bytes and compares against `stack.viewport` read from the real `stacks/angular.yaml`, not a hardcoded `1280`/`800` literal (so the test stays correct if the spec's viewport ever changes).
    - `tmp/&lt;run_id&gt;/` does not exist after the happy-path test (D2-05 success-path deletion, confirmed on the real project-root `tmp/` — not a test-tmp-dir stand-in).
  </acceptance_criteria>
  <done>The real committed Angular template, run through the real runStack pipeline, produces a completed outcome with a correctly-dimensioned screenshot, a captured dist size, and lint/test recorded as non-fatal — ROADMAP Success Criteria 3 and 4 are now provably TRUE end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Forced-failure variants + teardown/port-free verification</name>
  <files>tests/runStack.integration.test.ts</files>
  <read_first>
    - 02-VALIDATION.md's exact command: `npx vitest run tests/runStack.integration.test.ts -t "teardown"`, plus the forced-failure rows (`BUILD-01` happy+3 forced-failure variants).
    - src/specs/schema.ts (Plan 02-01) — the six `*TimeoutMs` optional override fields this task's forced-timeout case relies on.
    - src/specs/types.ts — `Stack` type (the forced-failure tests construct in-memory `Stack` objects by spreading the real loaded spec and overriding one or two fields, never editing `stacks/angular.yaml` itself).
    - 02-RESEARCH.md "Verification after teardown" note (a fresh `fetch("http://localhost:4200")` after teardown must reject/refuse — proves the port is free).
  </read_first>
  <action>
    Test "install failure yields build_failed + failedStage install": construct `const badStack = { ...stack, install: "false" }` (the `false` program is on `PATH` inside the allowlisted env and always exits 1 immediately — no need to touch the committed template or wait out a real `npm ci` failure) and assert `runStack(badStack, newRunId(), storage)` resolves (never rejects) to `{ status: "build_failed", failedStage: "install" }`.
    Test "build timeout yields timeout + failedStage build": construct `const slowBuildStack = { ...stack, build: "node -e \"setTimeout(()=>{}, 5000)\"", buildTimeoutMs: 200 }` (a command that outlives a 200ms budget by design — using the `buildTimeoutMs` override from Plan 02-01's schema field is what makes this test complete in about a fifth of a second instead of waiting out the real 5-minute default) and assert the outcome is `{ status: "timeout", failedStage: "build" }`; also assert `runStack` still completed the preceding `install` stage for real (or stub install too, executor's call, whichever keeps this test's own runtime smallest while still proving the timeout branch specifically — e.g. `install: "true"` is a valid substitute that exits 0 instantly).
    Test "start failure (process exits before ready) yields start_failed + failedStage start": construct a stack whose `start` field is a command that exits non-zero almost immediately without ever binding the port (e.g. `start: "false"`, with `install`/`build` stubbed to instant-exit commands like `"true"` so this test does not pay for a real Angular build) and assert the outcome is `{ status: "start_failed", failedStage: "start" }`.
    Test "start/ready timeout yields timeout + failedStage start, and teardown leaves the port free": construct a stack whose `start` field binds nothing and simply sleeps past a tiny `startTimeoutMs` override (e.g. `start: "node -e \"setTimeout(()=>{}, 5000)\"", startTimeoutMs: 300`, `install`/`build` stubbed to `"true"`) and assert the outcome is `{ status: "timeout", failedStage: "start" }`. Immediately after, attempt `await fetch("http://localhost:" + stack.port)` wrapped in a try/catch and assert it throws/rejects (connection refused) — this is WORK-04's port-free proof, run against the real port the spec declares.
    For every forced-failure/timeout case above, also assert `tmp/&lt;run_id&gt;/` still exists after the call (D2-05 — kept on any non-`"completed"` outcome for post-mortem), and clean it up manually in the test's own `afterEach` (do not rely on `runStack` to have deleted it, since keeping it on failure is the very behavior under test).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/runStack.integration.test.ts -t "teardown"</automated>
  </verify>
  <acceptance_criteria>
    - Each forced-failure/timeout test asserts `runStack(...)` resolves (never rejects/throws) to the exact `{ status, failedStage }` pair specified in its own test name above.
    - The build-timeout and start/ready-timeout tests each set a `*TimeoutMs` override in the low hundreds of milliseconds and complete in well under 5 seconds of real wall time — none of them waits out a production-scale (multi-minute) timeout.
    - The start/ready-timeout test's post-teardown `fetch` attempt is asserted to throw/reject (port confirmed free) — this is the literal WORK-04 proof, not an inference from the returned status alone.
    - Every forced-failure/timeout test confirms `tmp/&lt;run_id&gt;/` still exists immediately after the call (D2-05 retention-on-failure), and removes it itself afterward.
    - None of these four tests touches `stacks/angular/template/` or `stacks/angular.yaml` on disk — every override is an in-memory `Stack` object built by spreading the real loaded spec.
  </acceptance_criteria>
  <done>All three fatal-stage classifications (build_failed/timeout at install-or-build, start_failed, timeout-at-start) are proven against the real runStack pipeline in well under a second of combined test time each, and WORK-04's port-free teardown guarantee is confirmed by a real post-teardown connection attempt — ROADMAP Success Criteria 2, 3, and 5's teardown half are now provably TRUE.</done>
</task>

<task type="auto">
  <name>Task 3: Isolation self-test (WORK-02)</name>
  <files>tests/isolation.selftest.test.ts</files>
  <read_first>
    - 02-CONTEXT.md D2-06 ("Verified by a dedicated self-test that hashes the project tree before/after a run... No before/after guard on real runs" — this is test-only logic, no `src/` production code needed for the hash itself).
    - 02-RESEARCH.md "Isolation self-test (WORK-02, hash-before/after)" code example — the exact `hashTree(root)` walk (sorted directory entries for stable cross-OS ordering; skip `node_modules`, `.git`, `tmp`, `results`).
    - 02-VALIDATION.md's exact command: `npx vitest run tests/isolation.selftest.test.ts`.
  </read_first>
  <action>
    Create tests/isolation.selftest.test.ts implementing `hashTree(root: string): string` exactly per 02-RESEARCH.md's example (a `node:crypto` sha256 over sorted, recursively-walked relative paths + file bytes, explicitly skipping `node_modules`, `.git`, `tmp`, `results` directory names at any depth) — this function lives only in this test file, not in `src/`, per D2-06's explicit "test-only, no runtime code" framing. Point `root` at the repository root (`process.cwd()`, or `resolve(__dirname, "..")` from `tests/`, whichever resolves to the actual project root when the test runs).
    Test "runStack never mutates the main project tree": compute `const before = hashTree(root)`, run a real `runStack(loadStack("stacks/angular.yaml"), newRunId(), storage)` to completion (reuse Task 1's DB/storage setup pattern), compute `const after = hashTree(root)`, and assert `before === after`. This single assertion is WORK-02's proof — it necessarily also covers a full happy-path run, so this test may be slow like Task 1's; that is expected and is exactly why it lives in the integration tier, not the fast default suite.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts tests/isolation.selftest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.integration.config.ts tests/isolation.selftest.test.ts` exits 0.
    - `hashTree`'s walk explicitly excludes `node_modules`, `.git`, `tmp`, and `results` by directory name (grep the test file for all four literal exclusions).
    - The test asserts `before === after` on the hash strings themselves (not a weaker proxy like file count) — a single byte changed anywhere in the tracked tree would flip the hash.
    - This test file contains no export consumed by any `src/` file — `hashTree` is test-only per D2-06.
  </acceptance_criteria>
  <done>tests/isolation.selftest.test.ts proves WORK-02's byte-identical-main-tree guarantee against a real end-to-end runStack call — ROADMAP Success Criterion 1's isolation half is now provably TRUE.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files (this plan):** `tests/runStack.integration.test.ts`, `tests/isolation.selftest.test.ts`.
- **No new `src/` symbols** — this plan is entirely test code proving the prior five plans' deliverables end-to-end. `hashTree()` is a test-local helper, not a `src/` export (D2-06).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-constructed `Stack` overrides (forced-failure cases) → runStack | Same trust level as `stacks/angular.yaml` itself — author-controlled test fixtures, not external input |
| This test suite's own tmp/results writes → the real project tree | Must never leave residue that corrupts a later test run or the isolation self-test's own before/after comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Denial of Service (self-inflicted) | tests/runStack.integration.test.ts forced-timeout/failure cases | medium | mitigate | Every forced case cleans up its own `tmp/&lt;run_id&gt;/` in `afterEach`, and the start/ready-timeout case explicitly proves port 4200 is free post-teardown via a real connection attempt — this test suite cannot itself leave an orphaned server across a CI run. |
| T-2-04 | Tampering | tests/isolation.selftest.test.ts hashTree | low | mitigate | The hash walk explicitly excludes `tmp`/`results` (this test suite's own transient output) alongside `node_modules`/`.git`, so the self-test's own artifacts never cause a false-positive "main tree mutated" failure. |
</threat_model>

<verification>
- `npx vitest run --config vitest.integration.config.ts tests/runStack.integration.test.ts tests/isolation.selftest.test.ts` — full phase integration suite green.
- Every `<automated>` command in this plan's tasks individually green (targeted `-t` filters + the isolation self-test).
- `npm test` (fast default suite) remains green and does not pick up either file (Plan 02-01's `vitest.config.ts` exclusion).
</verification>

<success_criteria>
All five ROADMAP Phase-2 success criteria are proven true end-to-end against the real committed Angular template and the real `runStack` pipeline — not mocks. This closes out Phase 2.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-build-serve-runtime/02-06-SUMMARY.md` when done.
</output>
