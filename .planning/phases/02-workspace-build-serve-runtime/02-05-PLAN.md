---
phase: 02-workspace-build-serve-runtime
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/runtime/readiness.ts
  - src/pipeline/runStack.ts
autonomous: true
requirements: [BUILD-01, BUILD-02, WORK-04]
must_haves:
  truths:
    - "runStack(stack, runId, storage) is a pure 3-argument function matching D2-20 exactly, callable directly by an integration test with no CLI"
    - "An install, build, or start failure (or per-stage timeout) yields a returned RunOutcome with a scored status + failedStage, never an uncaught exception out of runStack, and no screenshot is attempted"
    - "lint/test failures never block the screenshot when build+start succeed (D2-14) — they are recorded as non-fatal stage events"
    - "dist/ build output size is captured as a meta.json artifact (not a new event field) whenever the build stage succeeds"
    - "The start-stage process (and its descendants) is always killed, whether the run finished, failed, or timed out"
    - "tmp/<run_id>/ is deleted when the run completes and kept when it does not (D2-05); results/<run_id>/ is always kept"
  artifacts:
    - path: src/runtime/readiness.ts
      provides: "waitForHttp200(url, timeoutMs) — the HTTP-poll layer of D2-10's readiness gate"
      exports: ["waitForHttp200"]
    - path: src/pipeline/runStack.ts
      provides: "runStack(stack, runId, storage): Promise<RunOutcome> — the D2-20 pure entrypoint"
      exports: ["runStack", "RunOutcome"]
  key_links:
    - from: "src/pipeline/runStack.ts"
      to: "src/workspace/{copy,teardown}.ts, src/runtime/{env,stage,readiness}.ts, src/render/playwrightRenderer.ts, src/storage/storagePort.ts's StoragePort"
      via: "runStack is pure orchestration wiring every Plan 02-02/02-03/02-04 primitive together through their exported functions/interfaces only"
      pattern: "runStack"
    - from: "src/pipeline/runStack.ts BenchmarkFinishedEvent emission"
      to: "storage.appendEvent (StoragePort)"
      via: "the terminal event is the same status/failedStage the function also returns as RunOutcome, so a caller never needs to re-read the event log just to know how the run ended"
      pattern: "benchmark_finished"
---

<objective>
Assemble the full deterministic pipeline (D2-20's `runStack(stackSpec, runId, storage)`): copy → install → build → (lint/test, non-fatal) → start → wait-ready → screenshot → teardown, wiring together every primitive from Plans 02-02/02-03/02-04, emitting the D-06/D-19 event shapes, and returning a structured `RunOutcome` — with every fatal-stage failure and timeout mapped to a scored `RunStatus` + `failedStage`, never an uncaught crash (BUILD-01), and lint/test/dist-size captured as non-fatal metrics (BUILD-02).

Purpose: This is the single seam Plan 02-06's integration tests exercise directly and Phase 5's orchestrator (CLI-01) will call unchanged later (D2-20) — it makes ROADMAP Success Criteria 1-3 provably TRUE.
Output: `src/runtime/readiness.ts`, `src/pipeline/runStack.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/02-workspace-build-serve-runtime/02-CONTEXT.md
@.planning/phases/02-workspace-build-serve-runtime/02-RESEARCH.md
@src/core/events.ts
@src/core/ports.ts
@src/specs/types.ts
@src/workspace/copy.ts
@src/workspace/teardown.ts
@src/runtime/env.ts
@src/runtime/stage.ts
@src/storage/storagePort.ts
@src/render/playwrightRenderer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Happy-path + fatal-stage short-circuit (install/build)</name>
  <files>src/runtime/readiness.ts, src/pipeline/runStack.ts</files>
  <read_first>
    - 02-CONTEXT.md D2-13 (fatal stages = install/build/start + any per-stage timeout; scored outcome, no screenshot), D2-17 (timeouts declared in stack.yaml with generous built-in fallbacks: install 5m, build 5m, start/ready 90s, screenshot 30s — read `stack.installTimeoutMs ?? 300000` etc., per Plan 02-01's Task-2 StackSchema fields; this is what lets Plan 02-06 force a fast, deterministic timeout instead of waiting minutes), D2-20 (the exact 3-argument `runStack(stackSpec, runId, storage)` signature — no 4th param; internal constants for tmp/results roots and npm cache dir only, not per-stage timeouts, which come from the spec).
    - src/core/events.ts — `Stage`, `StageStartedEvent`/`StageCompletedEvent`/`StageFailedEvent`, `RunStatus`, `BenchmarkFinishedEvent`. `RunStatus` has no dedicated "install_failed" value — both install and build fatal failures map to `"build_failed"`; only a timeout on either maps to `"timeout"` instead.
    - src/core/ports.ts — `StoragePort` (`appendEvent`, `writeArtifact`) is the only storage surface this file touches; never import `better-sqlite3` here.
    - src/workspace/copy.ts, src/runtime/env.ts, src/runtime/stage.ts (Plan 02-03) — `copyWorkspace`, `buildAllowlistedEnv`, `runStage` exact signatures.
    - 02-RESEARCH.md Pattern 3 "Layered Readiness Gate" (the `waitForHttp200` half only — native `fetch` + `AbortSignal.timeout`, no library).
  </read_first>
  <behavior>
    - `runStack(stack, runId, storage)` with an install command that exits non-zero returns `{ status: "build_failed", failedStage: "install", screenshotArtifactId: null }` and `storage.appendEvent` was called with a terminal `BenchmarkFinishedEvent` matching that status/failedStage — the promise never rejects.
    - `runStack` with an install command that exceeds its timeout returns `{ status: "timeout", failedStage: "install", ... }`.
    - `runStack` with a build command that exits non-zero (install succeeded) returns `{ status: "build_failed", failedStage: "build", ... }`.
    - For every stage that runs, `storage.appendEvent` receives a `StageStartedEvent` before it and either a `StageCompletedEvent` or `StageFailedEvent` after it, each carrying `durationMs`/`exitCode` (D-06).
  </behavior>
  <action>
    Create src/runtime/readiness.ts exporting `async function waitForHttp200(url: string, timeoutMs: number): Promise&lt;void&gt;` — poll `fetch(url, { signal: AbortSignal.timeout(2000) })` in a loop bounded by `Date.now() + timeoutMs`, resolving as soon as `res.status === 200`, backing off `250ms` between attempts, and throwing an `Error` naming the last failure once the deadline passes (native `fetch`/`AbortSignal.timeout` only, no library — per 02-RESEARCH.md's "Don't Hand-Roll" table this is intentionally ~15 lines, not a dependency).
    Create src/pipeline/runStack.ts exporting `interface RunOutcome { runId: string; status: RunStatus; failedStage: Stage | null; screenshotArtifactId: string | null }` and `async function runStack(stack: Stack, runId: string, storage: StoragePort): Promise&lt;RunOutcome&gt;` (import `Stack` type-only from "../specs/types.js", `RunStatus`/`Stage`/event types type-only from "../core/events.js", `StoragePort` type-only from "../core/ports.js"). Internal constants (not parameters, per D2-20's fixed 3-arg signature): `const TMP_ROOT = "tmp"`, `const NPM_CACHE_DIR = resolve("tmp/.npm-cache")`. Per-stage timeouts are NOT hardcoded constants — resolve each from the spec with a fallback, per D2-17: `const installTimeoutMs = stack.installTimeoutMs ?? 300000`, `const buildTimeoutMs = stack.buildTimeoutMs ?? 300000`, `const lintTimeoutMs = stack.lintTimeoutMs ?? 300000`, `const testTimeoutMs = stack.testTimeoutMs ?? 300000`, `const startTimeoutMs = stack.startTimeoutMs ?? 90000`, `const screenshotTimeoutMs = stack.screenshotTimeoutMs ?? 30000` — this is the seam Plan 02-06 uses to force a fast timeout in its tests instead of waiting on the real multi-minute defaults. Maintain a local `let seq = 0` incremented before every `storage.appendEvent` call (D-04 — append order authoritative within this run).
    Sequence: `const appDir = copyWorkspace(stack.template, runId, TMP_ROOT)`; `const env = buildAllowlistedEnv(NPM_CACHE_DIR)`. For "install" then "build" (in that order, using `installTimeoutMs`/`buildTimeoutMs` respectively): emit `StageStartedEvent`, call `runStage(stageName, stack[stageName], { cwd: appDir, env, timeoutMs })`, then emit `StageCompletedEvent` (exitCode 0, not timed out) or `StageFailedEvent` (otherwise) carrying `durationMs`/`exitCode`; write that stage's log via `storage.writeArtifact(runId, "log", \`${stageName}.log\`, Buffer.from(outcome.logTail))`. On a fatal outcome (`exitCode !== 0` or `timedOut`) for either stage: emit `BenchmarkFinishedEvent` with `status: outcome.timedOut ? "timeout" : "build_failed"` and `failedStage: stageName`; call `cleanupWorkspace(runId, /* keep */ true, TMP_ROOT)` (D2-05 — keep the workspace for post-mortem on any non-completed outcome); return `{ runId, status, failedStage: stageName, screenshotArtifactId: null }` immediately — do not proceed to lint/test/start/screenshot.
    This task's scope stops after a successful build (or an install/build fatal exit as above) — Task 2 in this same plan continues from lint/test through screenshot/teardown, in the same file, appended to this sequence.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/runStack.ts` exports `runStack` with exactly 3 parameters (`stack`, `runId`, `storage`) and `RunOutcome`.
    - The install/build failure branches emit a `BenchmarkFinishedEvent` via `storage.appendEvent` with `status` in `{"build_failed","timeout"}` and `failedStage` matching the stage that failed, before `runStack`'s promise resolves.
    - `cleanupWorkspace(runId, true, ...)` is called (workspace kept) on every fatal-install/build return path.
    - `npx tsc --noEmit` exits 0 (Task 2 completes the file to a fully valid module before this criterion is checked in the wave's final verification, per the plan's task ordering).
  </acceptance_criteria>
  <done>runStack's copy→install→build sequence emits the correct D-06 stage events and short-circuits to a scored BenchmarkFinishedEvent + RunOutcome on any fatal install/build failure or timeout, matching D2-13 exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-fatal lint/test, dist size, readiness, screenshot, teardown</name>
  <files>src/pipeline/runStack.ts</files>
  <read_first>
    - 02-CONTEXT.md D2-14 (lint/test non-fatal — record pass/fail, never block screenshot), D2-15 (runtime page errors non-fatal, screenshot still proceeds), D2-16 ("Absent field = stage skipped" — only run lint/test when `stack.lint`/`stack.test` is defined), D2-18 (dist/ build output size in bytes, captured as a raw value — this plan writes it to a `meta.json` artifact per Plan 02-01's Task-2 design note, not as a new event field), D2-05 (delete tmp/&lt;run_id&gt;/ on success, keep on failure; results/&lt;run_id&gt;/ always kept), WORK-04 (process-group kill always runs, whatever the outcome).
    - src/runtime/stage.ts (Plan 02-03) — `startServer`/`killProcessTree` exact signatures (the "start" stage does not go through `runStage`, it is long-running).
    - src/render/playwrightRenderer.ts / src/core/ports.ts `RenderPort` (Plan 02-01/02-04) — `screenshot({url, viewport}): Promise&lt;RenderResult&gt;`.
    - src/runtime/readiness.ts (this plan's Task 1) — `waitForHttp200`.
  </read_first>
  <behavior>
    - When `stack.lint`/`stack.test` are both undefined, no lint/test `StageStarted`/`Completed`/`Failed` events are emitted at all (stage skipped, not "skipped" recorded as a special value).
    - A lint or test command that exits non-zero still allows the pipeline to reach the screenshot step (build+start having succeeded) — the final `status` is `"completed"`, not a lint/test-derived failure status.
    - When the served app never answers HTTP 200 within the start timeout, `runStack` returns `{ status: "timeout", failedStage: "start", screenshotArtifactId: null }` and the start subprocess (and any descendant) is confirmed killed before `runStack` resolves.
    - When the start subprocess itself exits before HTTP 200 is ever observed, `runStack` returns `{ status: "start_failed", failedStage: "start", ... }` (distinct from the timeout case).
    - On full success, `runStack` returns `{ status: "completed", failedStage: null, screenshotArtifactId: &lt;a string&gt; }`, a `results/&lt;run_id&gt;/` `meta.json` artifact exists containing at least `distBytes`, and `tmp/&lt;run_id&gt;/` no longer exists (deleted, D2-05).
  </behavior>
  <action>
    Continuing src/pipeline/runStack.ts after Task 1's successful build: compute `distBytes` by recursively summing `st.size` for every file under `${appDir}/dist/` (plain `node:fs` `readdirSync`/`statSync` walk, mirroring the walk shape in 02-RESEARCH.md's `hashTree` example minus the hashing — sum bytes, not hash) — walk `dist/` generically (not the Angular-specific `dist/angular/browser/` subpath) so this stays stack-agnostic for a future non-Angular row; if `dist/` does not exist for any reason, treat `distBytes` as `0` rather than throwing.
    For "lint" then "test" (in that order, only when `stack.lint`/`stack.test` is respectively defined — D2-16): emit `StageStartedEvent`, `runStage(stageName, stack[stageName], { cwd: appDir, env, timeoutMs: stageName === "lint" ? lintTimeoutMs : testTimeoutMs })`, emit `StageCompletedEvent`/`StageFailedEvent` regardless of exit code (both are terminal-but-non-fatal outcomes — never emit `BenchmarkFinishedEvent` or return early here), write its log via `storage.writeArtifact(runId, "log", \`${stageName}.log\`, ...)`.
    Start the server: `const { subprocess } = startServer(stack.start, { cwd: appDir, env })`. Race `waitForHttp200(\`http://localhost:${stack.port}\`, startTimeoutMs)` against the subprocess's own completion (e.g. `subprocess.catch(() => "exited")`/`subprocess.then(() => "exited")` in a `Promise.race`) to distinguish "server process died before ever answering" (`status: "start_failed"`) from "server never answered in time but may still be running" (`status: "timeout"`) — both cases: `failedStage: "start"`, `killProcessTree(subprocess)`, emit `BenchmarkFinishedEvent`, `cleanupWorkspace(runId, true, TMP_ROOT)`, write whatever partial `meta.json` data exists so far (`{ distBytes }`), return `{ runId, status, failedStage: "start", screenshotArtifactId: null }`.
    On HTTP 200 confirmed: call `renderer.screenshot({ url: \`http://localhost:${stack.port}\`, viewport: stack.viewport })` (construct `renderer` via `createPlaywrightRenderer()` from "../render/playwrightRenderer.js" — the one and only import of that module in this file; `runStack` never imports "playwright" itself, only the render module's factory) wrapped in a race against a `screenshotTimeoutMs` timer and in a try/catch. On rejection or timeout: `killProcessTree(subprocess)`, emit `BenchmarkFinishedEvent({ status: "start_failed", failedStage: "start" })`, `cleanupWorkspace(runId, true, TMP_ROOT)`, write `meta.json` with whatever partial data exists, return the corresponding `RunOutcome`.
    On a successful `RenderResult`: `killProcessTree(subprocess)` (always tear down the server once the screenshot is captured, success or not — WORK-04); `const screenshotArtifactId = storage.writeArtifact(runId, "screenshot", "generated.png", result.png)`; write `results/&lt;run_id&gt;/meta.json` via `storage.writeArtifact(runId, "meta", "meta.json", Buffer.from(JSON.stringify({ distBytes, pageErrors: { consoleErrors: result.consoleErrors, uncaughtExceptions: result.uncaughtExceptions, failedRequests: result.failedRequests } })))` (D2-15's non-fatal signal lives here, alongside the dist-size metric — not as a new event field); emit `BenchmarkFinishedEvent({ status: "completed", failedStage: null })`; `cleanupWorkspace(runId, /* keep */ false, TMP_ROOT)` (D2-05 — delete on success); return `{ runId, status: "completed", failedStage: null, screenshotArtifactId }`.
    Wrap the entire lint-through-teardown sequence so that `killProcessTree(subprocess)` is guaranteed to run exactly once on every exit path (success, start_failed, timeout, or an unexpected thrown error from any step) — prefer a single `try { ... } finally { killProcessTree(subprocess) }` around the start-through-screenshot block rather than repeating the kill call at each early-return site.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Lint/test stages are skipped entirely (no events emitted) when the corresponding `StackSchema` field is absent — grep the implementation for an `if (stack.lint)`/`if (stack.test)` guard around each.
    - A non-zero lint or test exit code does not appear anywhere in the fatal-outcome branch logic — only install/build/start feed `BenchmarkFinishedEvent`'s non-"completed" statuses.
    - `killProcessTree` appears inside a `finally` (or equivalent guaranteed-once) block covering the start-through-screenshot sequence, not duplicated across every early-return branch.
    - `cleanupWorkspace` is called with `keep: false` only on the `"completed"` path and `keep: true` on every other path.
    - `meta.json` is written via `storage.writeArtifact` with kind `"meta"` on both the success path and the start/screenshot-failure paths (partial data acceptable on failure).
    - `grep -cE '(300000|90000|30000)' src/pipeline/runStack.ts` matches only the six `?? <fallback>` default expressions from Task 1/this task (`installTimeoutMs`/`buildTimeoutMs`/`lintTimeoutMs`/`testTimeoutMs` falling back to `300000`, `startTimeoutMs` to `90000`, `screenshotTimeoutMs` to `30000`) — no other bare numeric literal is used as a timeout anywhere in the file; every `runStage`/`waitForHttp200`/screenshot-race call site passes one of the six resolved `*TimeoutMs` variables, never a re-typed literal.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>runStack fully implements D2-20's pipeline: non-fatal lint/test capture, dist-size + page-error signal recorded in meta.json, distinct start_failed-vs-timeout classification, guaranteed-once process-group teardown, and D2-05 workspace retention — ready for Plan 02-06's integration tests to exercise end-to-end.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files (this plan):** `src/runtime/readiness.ts`, `src/pipeline/runStack.ts`.
- **Functions/types:** `waitForHttp200()`, `runStack()`, `RunOutcome`.
- **Produced results/&lt;run_id&gt;/ artifacts (at runtime, not files in this repo):** `screenshots/generated.png` (kind `"screenshot"`), `logs/&lt;stage&gt;.log` per stage that ran (kind `"log"`), `meta.json` (kind `"meta"`, `{ distBytes, pageErrors }`).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runStack's internal control flow → BenchmarkFinishedEvent/RunOutcome | Every exit path, expected or not, must resolve to a scored value, never an uncaught rejection out of `runStack` |
| Started dev-server subprocess → this process's lifetime | Must never survive past `runStack`'s return, regardless of which branch returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Denial of Service (self-inflicted) | src/pipeline/runStack.ts start/screenshot sequence | high | mitigate | `killProcessTree(subprocess)` runs inside a single `finally` block guaranteed to execute on every exit path (success, start_failed, timeout, or an unexpected throw) — WORK-04's "no orphaned dev-server processes across runs" holds even when a step throws unexpectedly, not just on the happy/known-failure paths. |
| T-2-04 | Tampering | src/pipeline/runStack.ts writeArtifact calls (logs, screenshot, meta.json) | medium | mitigate | Every artifact write goes through the Plan 02-03 `StoragePort` adapter, which delegates to Phase 1's already-path-contained `writeArtifact` verbatim — this plan never constructs a filesystem path itself. |
| T-2-09 | Repudiation | src/pipeline/runStack.ts fatal-outcome branches | low | mitigate | Every fatal exit still emits a terminal `BenchmarkFinishedEvent` with `status`/`failedStage` before returning — a run's outcome is always a queryable, persisted record (D-19), never a silently-swallowed failure. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across both tasks.
- Manual code-path review against the acceptance criteria above (this plan's own automated verify is type-checking only — full behavioral proof is Plan 02-06's integration test suite, which this plan is a direct prerequisite for).
</verification>

<success_criteria>
`runStack(stack, runId, storage)` exists as the single D2-20 pipeline entrypoint, correctly classifying every fatal-stage outcome, capturing every non-fatal signal, and guaranteeing process-group teardown + workspace retention on every exit path — ready for Plan 02-06 to prove end-to-end against the real committed Angular template.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-build-serve-runtime/02-05-SUMMARY.md` when done.
</output>
</content>
