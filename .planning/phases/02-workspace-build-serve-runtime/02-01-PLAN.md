---
phase: 02-workspace-build-serve-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - vitest.integration.config.ts
  - src/core/events.ts
  - src/specs/schema.ts
  - src/core/ports.ts
autonomous: true
requirements: [WORK-03, BUILD-02, BUILD-03, BUILD-04]
must_haves:
  truths:
    - "playwright, execa, pixelmatch, and pngjs are installed and importable at their CLAUDE.md-pinned exact versions (1.61.1 / 9.6.1 / 7.2.0 / 7.0.0)"
    - "Running `npm test` executes only the fast unit suite; heavy integration/self-test files are excluded from it and run only via the dedicated integration config"
    - "StackSchema accepts optional lint/test string fields plus optional per-stage timeout overrides (installTimeoutMs/buildTimeoutMs/lintTimeoutMs/testTimeoutMs/startTimeoutMs/screenshotTimeoutMs), and still rejects any other unknown key (.strict() preserved)"
    - "The Stage type recognizes lint and test alongside install/build/start, so non-fatal stages reuse the existing StageStarted/Completed/Failed event shapes with no new event variants"
    - "RenderPort is declared in core/ports.ts as a pure-data seam, importing nothing concrete, ready for Plan 02-04 to implement"
  artifacts:
    - path: package.json
      provides: "Exact-pinned Phase-2 runtime deps: playwright 1.61.1, execa 9.6.1, pixelmatch 7.2.0, pngjs 7.0.0 (+ @types/pngjs dev dep)"
      contains: "\"playwright\": \"1.61.1\""
    - path: vitest.integration.config.ts
      provides: "Raised testTimeout (>=900000ms) for *.integration.test.ts / *.selftest.test.ts"
      contains: "testTimeout"
    - path: src/core/events.ts
      provides: "Stage widened to install|build|lint|test|start"
      contains: "\"lint\""
    - path: src/specs/schema.ts
      provides: "StackSchema.lint / StackSchema.test optional fields (D2-16) + optional per-stage timeout override fields (D2-17)"
      exports: ["StackSchema"]
    - path: src/core/ports.ts
      provides: "RenderPort/RenderInput/RenderResult seam (D2-21)"
      exports: ["RenderPort", "RenderInput", "RenderResult"]
  key_links:
    - from: "package.json dependency versions"
      to: "every later Phase-2 module's imports of playwright/execa/pixelmatch/pngjs"
      via: "npm install resolves them without error before any downstream plan writes code against them"
      pattern: "playwright"
    - from: "src/core/events.ts Stage union"
      to: "src/runtime/stage.ts runStage() (Plan 02-03) and src/pipeline/runStack.ts sequencing (Plan 02-05)"
      via: "both later modules type their stage parameter against this exact union"
      pattern: "Stage"
    - from: "src/specs/schema.ts StackSchema timeout override fields"
      to: "src/pipeline/runStack.ts stage timeout resolution (Plan 02-05) and Plan 02-06's forced-failure/timeout integration tests"
      via: "runStack reads stack.<stage>TimeoutMs with a generous built-in fallback (D2-17); tests override it to a small value to force a fast, deterministic timeout without waiting minutes"
      pattern: "TimeoutMs"
    - from: "src/core/ports.ts RenderPort"
      to: "src/render/playwrightRenderer.ts (Plan 02-04) and src/pipeline/runStack.ts (Plan 02-05)"
      via: "Plan 02-04 implements the interface; Plan 02-05 calls it through the interface only, never importing playwright directly"
      pattern: "RenderPort"
---

<objective>
Establish the shared contracts and dependencies every other Phase-2 plan builds on: pin the new runtime libraries (playwright/execa/pixelmatch/pngjs), extend the Phase-1 `Stage`/`StackSchema` contracts to cover the non-fatal lint/test stages (D2-14/D2-16) and per-stage timeout overrides (D2-17), add the `RenderPort` seam (D2-21), and split the vitest suite into a fast unit tier and a slow integration/self-test tier so `npm test` never regresses once Plans 02-04/02-06 add heavy tests.

Purpose: Nothing downstream can import playwright/execa/pixelmatch/pngjs, reference the widened `Stage`, use the new `lint`/`test`/timeout-override StackSchema fields, or implement `RenderPort` until this foundational layer exists. Root of the Phase-2 dependency graph (Wave 1). The timeout-override fields specifically exist so Plan 02-06 can force a stage to time out in milliseconds instead of waiting on D2-17's multi-minute production defaults.
Output: `package.json`/`package-lock.json` with the four exact-pinned deps, `vitest.config.ts` + `vitest.integration.config.ts` (two-tier suite), `src/core/events.ts` (widened `Stage`), `src/specs/schema.ts` (`StackSchema.lint`/`.test` + timeout overrides), `src/core/ports.ts` (`RenderPort`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/02-workspace-build-serve-runtime/02-CONTEXT.md
@.planning/phases/02-workspace-build-serve-runtime/02-RESEARCH.md
@package.json
@vitest.config.ts
@src/core/events.ts
@src/specs/schema.ts
@src/core/ports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-2 runtime dependencies + two-tier vitest config</name>
  <files>package.json, package-lock.json, vitest.config.ts, vitest.integration.config.ts</files>
  <read_first>
    - package.json — current dependencies/devDependencies (only zod/yaml/better-sqlite3 + typescript/tsx/vitest/@types exist today).
    - vitest.config.ts — current single-tier config (`include: ["tests/**/*.test.ts"]`, vitest default `testTimeout` = 5000ms).
    - .claude/CLAUDE.md — "Recommended Stack" table: playwright 1.61.1, execa 9.6.1, pixelmatch 7.2.0, pngjs 7.0.0 (exact pins, already project-locked, not to be re-decided).
    - 02-RESEARCH.md "Standard Stack" table and "Pitfall 6" (vitest's default 5000ms testTimeout is far shorter than a real `npm ci` + `ng build`; a dedicated raised-timeout config is required for the integration suite).
  </read_first>
  <action>
    Add to package.json "dependencies": "playwright": "1.61.1", "execa": "9.6.1", "pixelmatch": "7.2.0", "pngjs": "7.0.0" — all four as production dependencies, not devDependencies, because playwright/execa are used by the runtime pipeline itself (not just tests) and pixelmatch/pngjs are the same libraries Phase 3's evaluator will use at runtime (per .claude/CLAUDE.md's "Recommended Stack" categorization). Add "@types/pngjs" (current latest, 6.0.5) to "devDependencies" — pngjs ships no bundled type declarations (confirmed this session via `npm view pngjs types` returning empty), whereas playwright/execa/pixelmatch each ship their own `index.d.ts` and need no additional @types package. Run npm install to regenerate package-lock.json.
    Correction to 02-RESEARCH.md: its "Environment Availability" table claims these four packages are "already installed locally" — this is false in this repository's current state (confirmed this session: `npm ls playwright execa pixelmatch pngjs` printed "(empty)" and package-lock.json had zero entries for any of the four before this task runs). Treat that research claim as inaccurate and actually perform the install; do not skip it as "already done".
    Create vitest.integration.config.ts: import `defineConfig` from "vitest/config", extend the base config's `test` block with `include: ["tests/**/*.integration.test.ts", "tests/**/*.selftest.test.ts"]`, `testTimeout: 960000` (16 minutes — comfortably past the ~15-minute D2-17 overall run cap), and `hookTimeout: 960000`.
    Edit the existing vitest.config.ts: import `configDefaults` from "vitest/config" alongside `defineConfig`, and add `exclude: [...configDefaults.exclude, "tests/**/*.integration.test.ts", "tests/**/*.selftest.test.ts"]` to its `test` block (spreading `configDefaults.exclude` is required — setting a custom `exclude` array without it would silently drop vitest's own built-in excludes like `node_modules`/`.git`). This must land now, before Plans 02-04/02-06 create `tests/determinism.selftest.test.ts`, `tests/isolation.selftest.test.ts`, and `tests/runStack.integration.test.ts` — otherwise the base config's `include: ["tests/**/*.test.ts"]` glob would pick those files up and fail them against the 5-second default timeout.
  </action>
  <verify>
    <automated>npm test && npm ls playwright execa pixelmatch pngjs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json "dependencies" contains exactly `"playwright": "1.61.1"`, `"execa": "9.6.1"`, `"pixelmatch": "7.2.0"`, `"pngjs": "7.0.0"` (no `^`/`~` range prefixes).
    - package.json "devDependencies" contains `"@types/pngjs"`.
    - `npm ls playwright execa pixelmatch pngjs` prints all four resolved, no "(empty)"/UNMET PEER errors.
    - vitest.config.ts's `test.exclude` array textually contains both `"tests/**/*.integration.test.ts"` and `"tests/**/*.selftest.test.ts"` and also spreads `configDefaults.exclude`.
    - vitest.integration.config.ts's `test.include` array contains the same two globs, and `test.testTimeout >= 900000`.
    - `npm test` still exits 0 (no test files touched by this task; the existing 5 Phase-1 test files remain green).
  </acceptance_criteria>
  <done>package.json/package-lock.json declare the four exact-pinned deps + @types/pngjs; vitest.config.ts excludes the two heavy test globs; vitest.integration.config.ts exists with the raised timeout; npm test and tsc --noEmit stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend core contracts for lint/test stages + timeout overrides (Stage union + StackSchema fields)</name>
  <files>src/core/events.ts, src/specs/schema.ts, tests/specs.test.ts, tests/core.test.ts</files>
  <read_first>
    - src/core/events.ts — current `Stage = "install" | "build" | "start"`; `StageStartedEvent`/`StageCompletedEvent`/`StageFailedEvent` shapes; the file's own header note "add no new event types without cause".
    - src/specs/schema.ts — current `StackSchema` (`z.strictObject`) fields: template/install/build/start/port/viewport.
    - 02-CONTEXT.md D2-14 ("Lint & test are non-fatal metric stages — they record pass/fail but never block the screenshot"), D2-16 ("Optional lint and test command fields are added to StackSchema... Absent field = stage skipped"), D2-17 ("Per-stage timeouts declared in `stack.yaml` with generous built-in fallbacks: install 5m, build 5m, start/ready 90s, screenshot 30s" — the "declared in stack.yaml" phrasing means these are per-stack OVERRIDE fields, not hardcoded runStack constants; Plan 02-05 reads `stack.<stage>TimeoutMs ?? <fallback>`).
    - tests/specs.test.ts and tests/core.test.ts — existing test conventions for schema/event assertions (match their style, do not introduce a new testing pattern).
  </read_first>
  <behavior>
    - `StackSchema.safeParse({...all required fields, no lint/no test/no timeout overrides}).success === true` (every new field is optional; absent is still valid).
    - `StackSchema.safeParse({...all required fields, lint: "npm run lint", test: "npm test", buildTimeoutMs: 1000}).success === true`.
    - `StackSchema.safeParse({...all required fields, somethingUnknown: true}).success === false` (`.strict()` still rejects extra keys after the new fields are added).
    - `StackSchema.safeParse({...all required fields, buildTimeoutMs: -1}).success === false` (timeout override fields are positive integers, per `z.number().int().positive()`).
    - A `StageStartedEvent`/`StageCompletedEvent`/`StageFailedEvent` literal with `stage: "lint"` or `stage: "test"` type-checks under strict mode exactly like `stage: "install"` does today (no new interface needed).
  </behavior>
  <action>
    In src/core/events.ts, widen the `Stage` type union from `"install" | "build" | "start"` to `"install" | "build" | "lint" | "test" | "start"`. Reuse `StageStartedEvent`/`StageCompletedEvent`/`StageFailedEvent` verbatim for lint/test — do not add any new event interface or new field to these three interfaces; widening an existing union is not "adding a new event type". Dist/ build output size (D2-18) is captured separately as a `meta.json` artifact in Plan 02-05, not as an event field — do not touch `BenchmarkFinishedEvent` in this task.
    In src/specs/schema.ts, add these new optional fields to `StackSchema` immediately after the existing `start` field: `lint: z.string().optional()`, `test: z.string().optional()`, `installTimeoutMs: z.number().int().positive().optional()`, `buildTimeoutMs: z.number().int().positive().optional()`, `lintTimeoutMs: z.number().int().positive().optional()`, `testTimeoutMs: z.number().int().positive().optional()`, `startTimeoutMs: z.number().int().positive().optional()`, `screenshotTimeoutMs: z.number().int().positive().optional()`. These six exist so D2-17's "generous built-in fallbacks" language is honored literally (a per-stack override, falling back to the fixed production default when absent) AND so Plan 02-06's forced-timeout integration tests can set e.g. `buildTimeoutMs: 100` against a deliberately slow command to prove the timeout branch in seconds instead of waiting out the real 5-minute default. Keep `z.strictObject(...)` — unknown/extra keys still reject (D-08). Do not touch `ScenarioSchema` or `ModelSchema`.
    Add a small assertion to tests/specs.test.ts covering the `StackSchema.safeParse` cases in `<behavior>` above (reuse the existing StackSchema test's inline object literal style rather than adding a new fixture file). Add a small assertion to tests/core.test.ts confirming a `StageCompletedEvent` literal with `stage: "lint"` type-checks (compile-time assertion is sufficient; no new runtime behavior to assert beyond the type accepting the value).
  </action>
  <verify>
    <automated>npx vitest run tests/specs.test.ts tests/core.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/core/events.ts's `Stage` type literal includes all five string literals: `"install"`, `"build"`, `"lint"`, `"test"`, `"start"`.
    - src/specs/schema.ts's `StackSchema` object literal contains `lint: z.string().optional()`, `test: z.string().optional()`, and all six `*TimeoutMs: z.number().int().positive().optional()` fields (`installTimeoutMs`, `buildTimeoutMs`, `lintTimeoutMs`, `testTimeoutMs`, `startTimeoutMs`, `screenshotTimeoutMs`).
    - `npx vitest run tests/specs.test.ts` exits 0, including the new lint/test-optional, timeout-override, and strict-rejection assertions.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Stage includes lint/test (no new event variants added); StackSchema accepts optional lint/test string fields and six optional positive-integer timeout-override fields, still rejects unknown keys; specs.test.ts and core.test.ts are green.</done>
</task>

<task type="auto">
  <name>Task 3: Add RenderPort seam to core/ports.ts</name>
  <files>src/core/ports.ts</files>
  <read_first>
    - src/core/ports.ts — current `AgentPort`/`EvaluatorPort`/`StoragePort` and the file's own header comment: "MUST NOT import any concrete runtime dependency... only type-only imports of other core contracts are allowed."
    - 02-CONTEXT.md D2-21 ("add a core port interface only where a test-double needs one, most likely a RenderPort so tests can stub Playwright... D-23 ports-inward direction holds either way").
    - 02-RESEARCH.md "Recommended Project Structure" (`render/renderPort.ts` — but canonical_refs pins this to `src/core/ports.ts`, not a separate file) and the Code Examples' `PageErrorSignal` shape (`consoleErrors`, `uncaughtExceptions`, `failedRequests`).
  </read_first>
  <action>
    Add a `RenderPort` interface to src/core/ports.ts (not a new file — 02-CONTEXT.md's canonical_refs states RenderPort "lives here") with one method: `screenshot(input: RenderInput): Promise&lt;RenderResult&gt;`. Define `RenderInput` as `{ url: string; viewport: { width: number; height: number } }` and `RenderResult` as `{ png: Buffer; consoleErrors: string[]; uncaughtExceptions: string[]; failedRequests: string[] }` — plain data shapes only, zero import of anything from the "playwright" package, keeping this file import-free of concrete deps per its own header comment. `deviceScaleFactor`/`reducedMotion`/browser-channel are NOT input parameters on `RenderInput` — they are fixed platform choices (D2-12) that the concrete implementation (Plan 02-04) hardcodes internally, not something a caller configures per call.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/core/ports.ts exports an interface named exactly `RenderPort` with a `screenshot` method whose parameter type is `RenderInput` and return type is `Promise&lt;RenderResult&gt;`.
    - `RenderResult`'s type literal includes fields `png`, `consoleErrors`, `uncaughtExceptions`, `failedRequests`.
    - `grep -c '"playwright"' src/core/ports.ts` returns 0 (no import of the playwright package in this file).
    - `npx tsc --noEmit` exits 0 (RenderPort compiles standalone with no implementation required yet).
  </acceptance_criteria>
  <done>RenderPort/RenderInput/RenderResult are exported from core/ports.ts, ready for Plan 02-04 to implement and Plan 02-05 to consume through the interface only.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Files (this plan):** `vitest.integration.config.ts` (new); `package.json`, `package-lock.json`, `vitest.config.ts`, `src/core/events.ts`, `src/specs/schema.ts`, `src/core/ports.ts` (all modified, pre-existing from Phase 1).
- **Types/fields added:** `Stage` widened to `"install" | "build" | "lint" | "test" | "start"`; `StackSchema.lint?: string`, `StackSchema.test?: string`; `StackSchema.installTimeoutMs?/buildTimeoutMs?/lintTimeoutMs?/testTimeoutMs?/startTimeoutMs?/screenshotTimeoutMs?: number` (D2-17 per-stage overrides); `RenderPort`, `RenderInput`, `RenderResult` interfaces.
- **Dependencies added:** `playwright@1.61.1`, `execa@9.6.1`, `pixelmatch@7.2.0`, `pngjs@7.0.0` (prod), `@types/pngjs` (dev).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package bytes (playwright, execa, pixelmatch, pngjs, @types/pngjs) enter the project at install time |

ASVS L1 scope: this plan defines contracts + installs dependencies; no untrusted input is processed at runtime here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-SC | Tampering | npm installs (playwright/execa/pixelmatch/pngjs/@types/pngjs) | medium | mitigate | All four runtime packages are CLAUDE.md-locked stack (project-governed decision, not newly-discovered), independently well-known/high-reputation (Microsoft/sindresorhus/mapbox), and version-pinned exact. 02-RESEARCH.md's Package Legitimacy Audit additionally verified the phase's other two new packages (sirv-cli, @angular/cli, used in Plan 02-02) as "Approved" with no postinstall scripts. No `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict anywhere in this phase — no blocking human-verify checkpoint required. |
| T-2-01 | Tampering | src/core/ports.ts RenderPort | low | mitigate | RenderPort is declared as plain data interfaces only, with an explicit acceptance-criteria grep proving no `playwright` import leaks into this file — preserves the D-23 isolation seam (only Plan 02-04's concrete renderer imports playwright). |
</threat_model>

<verification>
- `npm test` green (existing Phase-1 suite, untouched by new heavy test files).
- `npx tsc --noEmit` green.
- `npm ls playwright execa pixelmatch pngjs` shows all four at their exact pinned versions.
</verification>

<success_criteria>
All three tasks' acceptance criteria hold. Plans 02-02, 02-03, and 02-04 can each declare `depends_on: ["02-01"]` and consume the widened `Stage`, the new `StackSchema.lint`/`.test` fields, `RenderPort`, and `vitest.integration.config.ts` without any further edits to these files.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-build-serve-runtime/02-01-SUMMARY.md` when done.
</output>
</content>
