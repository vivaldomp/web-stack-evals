---
phase: 04-agent-runtime-pi-sdk-adapter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specs/schema.ts
  - src/agent/types.ts
  - stacks/angular.yaml
  - tests/fixtures/stacks/angular.yaml
  - tests/specs.test.ts
  - tests/agentInput.test.ts
autonomous: true
requirements: [AGENT-02, AGENT-05]
must_haves:
  truths:
    - "`ScenarioSchema` carries a `budget` object with three ceilings — `maxMinutes`, `maxUsd`, `maxTurns` (D4-01/D4-03) — and an absent `budget` resolves to the documented defaults 20 min / 5.00 USD / 50 turns."
    - "A partial `budget` (e.g. only `maxTurns`) parses, filling the two omitted ceilings from their per-field defaults."
    - "`ScenarioSchema.skills` documents the D4-16 contract: repo-relative `skills/<name>/` paths fed to Pi `DefaultResourceLoader` `additionalSkillPaths`."
    - "`StackSchema` carries a required `preamble` string — the stack-authored environmental grounding prepended verbatim to the scenario prompt (D4-04/D4-05)."
    - "`stacks/angular.yaml` ships a non-empty authored `preamble` that grounds the agent (template present, build/start commands) WITHOUT editorializing the task (D4-04)."
    - "`src/agent/types.ts` exports `AgentInput`, `AgentBudget`, and `AgentModelSpec` — the fully-resolved typed contract the orchestrator hands the adapter (D4-22), with no Pi SDK import."
    - "`AgentInput` covers workspace path, verbatim prompt text, stack preamble, mockup bytes + mimeType, skill paths, resolved model spec, and the three resolved budget ceilings."
  artifacts:
    - "src/specs/schema.ts — `StackSchema.preamble: z.string()` (required)"
    - "src/specs/schema.ts — `ScenarioSchema.budget` = strictObject{ maxMinutes/maxUsd/maxTurns } with per-field defaults + object-level `.default({})`"
    - "src/specs/schema.ts — `ScenarioSchema.skills` doc-comment tightened to the D4-16 contract"
    - "src/agent/types.ts — `AgentBudget` { maxWallClockMs: DurationMs, maxCostUsd: UsdCost, maxTurns: number }"
    - "src/agent/types.ts — `AgentModelSpec` { provider, modelId, thinkingLevel?, temperature }"
    - "src/agent/types.ts — `AgentInput` { runId, workspacePath, promptText, preamble, mockupBytes: Buffer, mockupMimeType, skillPaths, model, budget }"
    - "stacks/angular.yaml + tests/fixtures/stacks/angular.yaml — authored `preamble`"
    - "tests/specs.test.ts — budget-default / partial-budget / missing-preamble assertions"
    - "tests/agentInput.test.ts — AgentInput shape construction test"
  prohibitions:
    - "MUST NOT import `@earendil-works/pi-coding-agent` or any Pi SDK symbol — this plan is pure zod schema + TS types."
    - "MUST NOT modify `src/core/ports.ts`, `src/core/events.ts`, `src/storage/*`, or `src/pipeline/runStack.ts` (owned by wave-1 Plans 04-01/04-02 — file collision)."
    - "MUST NOT re-declare `skills` as a new field — it already exists on `ScenarioSchema`; only its doc-comment is refined."
    - "MUST NOT drop `.strictObject` on either schema — unknown-key rejection stays (existing tests assert it)."
    - "MUST NOT make `preamble` optional or default it to empty — an empty grounding defeats D4-05; the stack authors it."
  key_links:
    - "ScenarioSchema.budget → (Phase 5) orchestrator resolves minutes→ms into AgentInput.budget → adapter ceiling monitor (Plans 04-07/08)"
    - "StackSchema.preamble → orchestrator prepends to scenario prompt → AgentInput.preamble → session.prompt(preamble + prompt)"
    - "AgentInput → the typed argument the adapter narrows `runSession(input: unknown)` against (D4-22)"
---

<objective>
Extend the two spec schemas with the declarative fields Phase 4 forces — a scenario run-budget (three ceilings) and a stack-authored preamble — and define the fully-resolved `AgentInput` contract the orchestrator hands the adapter. Pure zod + TS types; no Pi SDK import.

Purpose: The adapter (Plans 04-07/08) needs a strongly-typed input boundary (D4-22) and the ceilings that bound its run (D4-01/D4-03); each stack needs to declare its own grounding (D4-05). All three are declarative-first (D-07): adding a budget, a preamble, or a skill is a spec edit, not a core change.
Output: `budget` + refined `skills` on `ScenarioSchema`, `preamble` on `StackSchema`, an authored angular preamble, and `src/agent/types.ts` with `AgentInput`/`AgentBudget`/`AgentModelSpec`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-runtime-pi-sdk-adapter/04-CONTEXT.md
@.planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md
@src/specs/schema.ts
@src/specs/types.ts
@src/specs/load.ts
@src/core/units.ts
@tests/specs.test.ts
</context>

<artifacts>
Symbols this plan produces:
- `StackSchema.preamble` — required `z.string()`; stack-authored grounding (D4-04/D4-05).
- `ScenarioSchema.budget` — `z.strictObject({ maxMinutes, maxUsd, maxTurns })` with per-field defaults (`20`, `5`, `50`) and an object-level `.default({})` so an absent budget yields all three defaults (D4-01/D4-03).
- `ScenarioSchema.skills` — existing field, doc-comment tightened to the D4-16 contract (repo-relative `skills/<name>/` paths → Pi `additionalSkillPaths`).
- `AgentBudget` (`maxWallClockMs: DurationMs`, `maxCostUsd: UsdCost`, `maxTurns: number`) — the resolved ceilings (minutes already converted to ms).
- `AgentModelSpec` (`provider`, `modelId`, `thinkingLevel?: "off"|"low"|"medium"|"high"`, `temperature: number`) — D4-18 temperature defaults to 0 at the orchestrator, carried as a resolved field here.
- `AgentInput` (`runId`, `workspacePath`, `promptText`, `preamble`, `mockupBytes: Buffer`, `mockupMimeType: string`, `skillPaths: string[]`, `model: AgentModelSpec`, `budget: AgentBudget`) — the D4-22 pure `AgentPort` boundary.
- Authored `preamble` in `stacks/angular.yaml` + `tests/fixtures/stacks/angular.yaml`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Extend StackSchema (preamble) + ScenarioSchema (budget, skills doc) + author the angular preamble</name>
  <files>src/specs/schema.ts, stacks/angular.yaml, tests/fixtures/stacks/angular.yaml, tests/specs.test.ts</files>
  <read_first>
    - src/specs/schema.ts (full — `StackSchema` and `ScenarioSchema` are `z.strictObject`; `ScenarioSchema.skills` already exists as `z.array(z.string())`)
    - src/specs/load.ts (`parseAndValidate` throws on invalid; `loadStack`/`loadScenario` used by the tests)
    - tests/specs.test.ts (`VALID_STACK` const line 7 has NO preamble; `VALID_SCENARIO` line 71; the `.strict()`-preserved rejection tests; the production `loadStack("stacks/angular.yaml")` assertions)
    - stacks/angular.yaml + tests/fixtures/stacks/angular.yaml (production + fixture stack specs that must gain a preamble to stay valid)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md line 476 (default ceilings `maxMinutes: 20`, `maxUsd: 5.00`, `maxTurns: 50` — ASSUMED, calibrate after first live run) and § AgentInput shape (budget → minutes)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-CONTEXT.md D4-01/D4-03/D4-04/D4-05/D4-16
  </read_first>
  <action>
    In `src/specs/schema.ts`:
    (1) Add a required `preamble: z.string()` field to `StackSchema`. Doc it as the stack-authored environmental grounding (D4-04/D4-05) the platform prepends verbatim to the scenario prompt; each stack describes its own workspace/commands and MUST NOT editorialize the benchmarked task. Keep `StackSchema` a `z.strictObject`.
    (2) Add a `budget` field to `ScenarioSchema`, a `z.strictObject` with three ceilings: `maxMinutes: z.number().positive().default(20)`, `maxUsd: z.number().positive().default(5)`, `maxTurns: z.number().int().positive().default(50)`, and give the whole object `.default({})` so a scenario that omits `budget` resolves to all three defaults (zod substitutes `{}` then fills each field default). Doc it as the D4-01/D4-03 three-ceiling run budget (wall-clock minutes / cumulative USD / turn count, first-to-trip aborts), declared per-scenario so every model/stack facing it gets identical caps; state the defaults inline and note they are ASSUMED pending first-run calibration.
    (3) Tighten the doc-comment on the existing `ScenarioSchema.skills` field to name the D4-16 contract: repo-relative `skills/<name>/` directory paths, committed to the repo and part of the D-10 input fingerprint, passed to Pi `DefaultResourceLoader` `additionalSkillPaths`. Do NOT change its type or add a duplicate field.

    In `stacks/angular.yaml` and `tests/fixtures/stacks/angular.yaml`: add a `preamble` key with a short authored grounding string for the Angular stack — state that an Angular skeleton already exists in the workspace and name the build/start commands, WITHOUT describing or hinting at the design task (D4-04). Keep it a couple of sentences (thin grounding, not a tutorial).

    In `tests/specs.test.ts`: add `preamble` to the `VALID_STACK` const so the existing `StackSchema` describe block still parses. Add four new assertions: (a) `ScenarioSchema.parse(VALID_SCENARIO)` with no `budget` yields `budget` deep-equal `{ maxMinutes: 20, maxUsd: 5, maxTurns: 50 }`; (b) a partial `{ ...VALID_SCENARIO, budget: { maxTurns: 99 } }` yields `maxTurns: 99` with the other two defaulted; (c) `StackSchema.safeParse` of `VALID_STACK` WITHOUT `preamble` returns `success: false`; (d) in the existing `loadStack("stacks/angular.yaml")` production-spec test, assert `stack.preamble` is a non-empty string. Leave the existing lint/test/timeout and `.strict()` tests intact (they now include preamble in `VALID_STACK`).
  </action>
  <acceptance_criteria>
    - `ScenarioSchema.parse` of a scenario with no `budget` key returns `budget` deep-equal `{ maxMinutes: 20, maxUsd: 5, maxTurns: 50 }`.
    - `ScenarioSchema.parse` of `{ ...validScenario, budget: { maxTurns: 99 } }` returns `maxTurns: 99`, `maxMinutes: 20`, `maxUsd: 5`.
    - `ScenarioSchema.safeParse` of a scenario whose `budget.maxUsd` is `-1` returns `success: false`.
    - `StackSchema.safeParse` of a stack object with no `preamble` returns `success: false`; with a string `preamble` returns `success: true`.
    - `loadStack("stacks/angular.yaml").preamble` is a non-empty string.
    - Both `stacks/angular.yaml` and `tests/fixtures/stacks/angular.yaml` contain a `preamble:` key.
    - `npm run typecheck` passes and the full `tests/specs.test.ts` suite is green.
    - `src/specs/schema.ts` contains no import of any `@earendil-works/*` package.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/specs.test.ts && node -e "const c=require('fs').readFileSync('src/specs/schema.ts','utf8'); if(/@earendil-works/.test(c)){console.error('Pi import leaked into schema.ts');process.exit(1)} const y=require('fs').readFileSync('stacks/angular.yaml','utf8'); if(!/^preamble:/m.test(y)){console.error('stacks/angular.yaml missing preamble');process.exit(2)}"</automated>
  </verify>
  <done>Both schemas carry their new declarative fields, the angular stack authors a real preamble (asserted non-empty in the specs suite), an absent scenario budget resolves to the documented defaults, and the spec suite is green with no Pi import.</done>
</task>

<task type="auto">
  <name>Task 2: Define the fully-resolved AgentInput contract in src/agent/types.ts</name>
  <files>src/agent/types.ts, tests/agentInput.test.ts</files>
  <read_first>
    - src/core/units.ts (`DurationMs`, `UsdCost` plain aliases to reuse for the resolved ceilings)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md § "AgentInput shape (D4-22)" (the reference `AgentBudget` / `AgentModelSpec` / `AgentInput` shapes) and § structure (`src/agent/types.ts`)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-CONTEXT.md D4-07 (mockup base64 PNG, image/png), D4-18 (temperature default 0), D4-20 (provider/modelId from models/*.json), D4-22 (pure AgentPort boundary), D4-23 (workspace cwd)
    - src/core/ports.ts (`AgentPort.runSession(input: unknown)` — DO NOT edit here; this plan only defines the type the adapter will narrow against; a later plan wires it)
  </read_first>
  <action>
    Create `src/agent/types.ts` as a pure types module (type-only import of `DurationMs`, `UsdCost` from `../core/units.js`; no runtime import, no Pi SDK import). Define three exported interfaces:
    (1) `AgentBudget` — `maxWallClockMs: DurationMs`, `maxCostUsd: UsdCost`, `maxTurns: number`. Doc it as the RESOLVED ceilings: the orchestrator converts `scenario.yaml` `budget.maxMinutes` to ms (× 60_000) and passes `maxUsd`/`maxTurns` through (D4-01/D4-03).
    (2) `AgentModelSpec` — `provider: string`, `modelId: string`, `thinkingLevel?: "off" | "low" | "medium" | "high"`, `temperature: number`. Doc provider/modelId as from `models/*.json` (D4-20) and `temperature` as D4-18 (default 0 for the v1 row, resolved upstream).
    (3) `AgentInput` — `runId: string`, `workspacePath: string` (the `copyWorkspace` dir `tmp/<runId>/angular/`, D4-23 cwd), `promptText: string` (scenario prompt verbatim, D4-04), `preamble: string` (stack grounding, D4-05), `mockupBytes: Buffer` (raw PNG, D4-07), `mockupMimeType: string` (e.g. `"image/png"`, D4-07), `skillPaths: string[]` (committed `skills/<name>/` dirs, D4-16), `model: AgentModelSpec`, `budget: AgentBudget`. Doc the interface header as the D4-22 fully-resolved input — the orchestrator builds it from the zod-validated stack/scenario/model specs + `copyWorkspace`; the adapter never reaches into spec loaders. Add a one-line note that this file must stay Pi-free so the adapter boundary stays swappable (D-23).

    Create `tests/agentInput.test.ts`: import the types, construct a valid `AgentInput` object literal (a `Buffer.from(...)` for `mockupBytes`, a two-field `budget`, a `model`), and assert the shape is usable at runtime — e.g. `expect(input.skillPaths).toEqual([...])`, `expect(input.budget.maxTurns).toBe(50)`, `expect(Buffer.isBuffer(input.mockupBytes)).toBe(true)`. The construction typechecking under `tsc --noEmit` is the primary gate; the runtime asserts prove the literal is well-formed.
  </action>
  <acceptance_criteria>
    - `src/agent/types.ts` exports `AgentInput`, `AgentBudget`, and `AgentModelSpec`.
    - `AgentBudget.maxWallClockMs` is typed `DurationMs` and `AgentBudget.maxCostUsd` is typed `UsdCost` (imported type-only from `../core/units.js`).
    - `AgentInput` declares all nine fields: `runId`, `workspacePath`, `promptText`, `preamble`, `mockupBytes`, `mockupMimeType`, `skillPaths`, `model`, `budget`.
    - `src/agent/types.ts` contains no import from `@earendil-works/pi-coding-agent`, `@earendil-works/pi-ai`, or any Pi SDK path.
    - `tests/agentInput.test.ts` constructs an `AgentInput` literal and asserts `budget.maxTurns`, `skillPaths`, and `Buffer.isBuffer(mockupBytes)`.
    - `npm run typecheck` passes and `npx vitest run tests/agentInput.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/agentInput.test.ts && node -e "const s=require('fs').readFileSync('src/agent/types.ts','utf8'); for(const t of ['AgentInput','AgentBudget','AgentModelSpec','mockupMimeType','skillPaths','maxWallClockMs']){ if(!s.includes(t)){ console.error('missing',t); process.exit(1);} } if(/@earendil-works|pi-coding-agent|pi-ai/.test(s)){ console.error('Pi import leaked'); process.exit(2);} "</automated>
  </verify>
  <done>`src/agent/types.ts` defines the fully-resolved, Pi-free `AgentInput` boundary (with `AgentBudget`/`AgentModelSpec`); it typechecks and the shape test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML spec author → zod schema | Human-authored `stack.yaml` / `scenario.yaml` values (preamble text, budget ceilings) cross into typed config; malformed input must be rejected before any run starts (D-08). |
| resolved AgentInput → adapter | The typed input the orchestrator hands the adapter; carries workspace path, prompt, mockup bytes, budget — but no secret material (D4-19). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03a | Denial of Service | `ScenarioSchema.budget` (runaway cost/time) | low | mitigate | A scenario omitting `budget` still gets bounded: the object-level `.default({})` + per-field defaults apply the documented ceilings (20 min / 5.00 USD / 50 turns). The three ceilings (D4-01) are the platform's cost/DoS bound on an autonomous agent; `.positive()`/`.int()` reject zero/negative/fractional-turn values that would disable a ceiling. Enforcement itself lands in the adapter (Plans 04-07/08); this plan makes the ceilings unskippable in the schema. |
| T-04-05sc | Tampering | `stack.yaml` / `scenario.yaml` unknown keys | low | mitigate | Both schemas stay `z.strictObject`; unknown keys are rejected (existing `.strict()`-preserved tests). A typo'd budget/preamble key fails fast at load (D-08) rather than silently no-op'ing a ceiling. |
| T-04-22a | Information Disclosure | `AgentInput` fields | low | accept | `AgentInput` carries no secret material — the provider API key stays in the orchestrator's `AuthStorage` (D4-19) and never enters `AgentInput`, the specs, the manifest, or the fingerprint. Types/schema hold prompt text, budget numbers, and public spec fields only. |
</threat_model>

<verification>
- `npm run typecheck` clean (both new/edited files compile under strict nodenext).
- `npx vitest run tests/specs.test.ts tests/agentInput.test.ts` green (budget defaults, partial budget, missing-preamble rejection, AgentInput shape).
- `npm test` stays green (no existing suite regresses from the schema additions — scenario budget is default-filled, so untouched scenario fixtures still parse).
- No `@earendil-works/*` / Pi import appears in `src/specs/schema.ts` or `src/agent/types.ts`.
</verification>

<success_criteria>
- `ScenarioSchema` carries a defaulted three-ceiling `budget` (D4-01/D4-03) and a D4-16-documented `skills` field; `StackSchema` carries a required stack-authored `preamble` (D4-04/D4-05) with the angular stack authoring a real one.
- `src/agent/types.ts` exports the Pi-free, fully-resolved `AgentInput`/`AgentBudget`/`AgentModelSpec` contract (D4-22) covering prompt (AGENT-02) + budget (AGENT-05).
- Typecheck and the schema + AgentInput tests are green; no wave-1 file collision with Plans 04-01/04-02.
</success_criteria>

<output>
Create `.planning/phases/04-agent-runtime-pi-sdk-adapter/04-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
