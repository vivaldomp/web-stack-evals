---
phase: 04-agent-runtime-pi-sdk-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/core/events.ts
autonomous: true
requirements: [AGENT-01, AGENT-04, AGENT-05]
must_haves:
  truths:
    - "`@earendil-works/pi-coding-agent@0.80.3` resolves as an importable dependency."
    - "`src/core/events.ts` exports `SessionStartedEvent`, `FirstTokenEvent`, `UsageEvent` and includes all three in the `AgentEvent` union."
    - "`UsageEvent` carries verbatim input/output/cache-read/cache-write tokens + `costUsd` (D-26 verbatim) + an `aborted` flag (D4-15)."
    - "`AgentEventDraft = Omit<AgentEvent, \"seq\">` is exported for the seq-storage-owned append boundary (D4-26)."
  artifacts:
    - "package.json / package-lock.json with the pinned Pi coding-agent dep"
    - "src/core/events.ts extended union + AgentEventDraft type"
  prohibitions:
    - "MUST NOT install the deprecated `@mariozechner/pi-coding-agent` scope."
    - "MUST NOT use a caret/range version — pin exact `0.80.3` (Pi is fast-moving; lockstep with pi-ai)."
    - "MUST NOT add zod to events.ts — it is a plain discriminated union keyed by `type`."
  key_links:
    - "events.ts `AgentEvent` union → consumed by ports.ts, db.ts, mapEvent.ts, piAgentAdapter.ts"
---

<objective>
Land the one new dependency this phase needs and extend the canonical event union with the minimal typed variants D4-09/D4-10 force, plus the `AgentEventDraft` alias that the D4-26 seq-ownership change (Plan 04-02) and the adapter (Plans 04-07/08) both consume.

Purpose: Every other Phase-4 plan depends on these two contracts existing. The adapter cannot import Pi without the dep; mapEvent/usage/TTFT cannot be typed without the variants.
Output: Pinned `@earendil-works/pi-coding-agent@0.80.3`; three new event interfaces + `AgentEventDraft` in `src/core/events.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-runtime-pi-sdk-adapter/04-CONTEXT.md
@.planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md
@src/core/events.ts
@src/core/units.ts
</context>

<artifacts>
Symbols this plan produces (part of the phase-wide artifact set):
- Dependency: `@earendil-works/pi-coding-agent@0.80.3` (exact pin).
- `SessionStartedEvent` (`type: "session_started"`, `provider`, `modelId`) — t0 anchor, D4-10.
- `FirstTokenEvent` (`type: "first_token"`) — emitted once, D4-10.
- `UsageEvent` (`type: "usage"`, `inputTokens`, `outputTokens`, `cacheReadTokens`, `cacheWriteTokens`, `reasoningTokens?`, `totalTokens`, `costUsd: UsdCost`, `aborted: boolean`) — D4-09/D4-15.
- `AgentEventDraft = Omit<AgentEvent, "seq">` — the seq-storage-owned producer/append type (D4-26).
- All three variants added to the `AgentEvent` union.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Install the pinned Pi coding-agent dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current deps — `@earendil-works/pi-ai@0.80.3` is present; coding-agent is NOT)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md § "Package Legitimacy Audit" (verdict OK / Approved, no postinstall) and § "Standard Stack"
    - .claude/CLAUDE.md § "What NOT to Use" (`@mariozechner/*` is deprecated) and § "Version Compatibility" (keep both Pi packages on the same 0.80.3)
  </read_first>
  <action>
    Run `npm install @earendil-works/pi-coding-agent@0.80.3` (exact pin, no caret — match the existing `@earendil-works/pi-ai@0.80.3` production-dependency style and CLAUDE.md's lockstep-pinning directive). This adds it as a production dependency (the runtime adapter imports it). `@earendil-works/pi-agent-core@0.80.3` arrives transitively — do NOT add it explicitly. The RESEARCH § Package Legitimacy Audit already cleared this package (registry OK, source `github.com/earendil-works/pi`, no preinstall/postinstall script), so no human legitimacy checkpoint is required. Do NOT touch the deprecated `@mariozechner/pi-coding-agent` scope.
  </action>
  <acceptance_criteria>
    - `node -e "console.log(require('@earendil-works/pi-coding-agent/package.json').version)"` prints `0.80.3`.
    - `package.json` `dependencies["@earendil-works/pi-coding-agent"]` is exactly `"0.80.3"` (no `^`/`~`).
    - `package.json` does NOT contain any `@mariozechner/` entry.
    - `node -e "require('@earendil-works/pi-coding-agent')"` exits 0 (module resolves).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const v=require('@earendil-works/pi-coding-agent/package.json').version; if(v!=='0.80.3'){process.exit(1)}; const p=require('./package.json'); if(p.dependencies['@earendil-works/pi-coding-agent']!=='0.80.3'){process.exit(2)}; if(JSON.stringify(p).includes('@mariozechner/')){process.exit(3)}"</automated>
  </verify>
  <done>The exact-pinned coding-agent dep installs cleanly, resolves at runtime, and no deprecated scope is present.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the AgentEvent union with session_started / first_token / usage + AgentEventDraft</name>
  <files>src/core/events.ts</files>
  <read_first>
    - src/core/events.ts (full — `BaseEvent`, `UnknownEvent` passthrough, existing variants, the `AgentEvent` union at the bottom)
    - src/core/units.ts (`EpochMs`, `DurationMs`, `UsdCost` branded aliases to reuse)
    - .planning/phases/04-agent-runtime-pi-sdk-adapter/04-RESEARCH.md § "Proposed new event shapes" (the exact field list) and § "D4-26 seq-ownership migration" (why `seq` becomes storage-assigned)
  </read_first>
  <action>
    Add three plain TS interfaces extending `BaseEvent`, keyed by `type` (no zod — this file is a discriminated union):
    (1) `SessionStartedEvent` — `type: "session_started"`, `provider: string`, `modelId: string`. Doc it as the t0 anchor emitted once on Pi `agent_start` (D4-10).
    (2) `FirstTokenEvent` — `type: "first_token"`. Doc it as emitted once on first streamed assistant text; `TTFT = first_token.ts − session_started.ts`, folded in Phase 5 (D4-10, TEL-03).
    (3) `UsageEvent` — `type: "usage"`, `inputTokens: number`, `outputTokens: number`, `cacheReadTokens: number`, `cacheWriteTokens: number`, `reasoningTokens?: number`, `totalTokens: number`, `costUsd: UsdCost` (import from units.js), `aborted: boolean`. Doc it as one-per-Pi-`turn_end`, verbatim pi-ai `Usage` (never pre-rounded, D-26), also emitted for aborted turns (D4-15).
    Add all three to the `AgentEvent` union. Then add `export type AgentEventDraft = Omit<AgentEvent, "seq">;` with a doc note that `seq` is stamped by storage at append time (D4-26), so producers (adapter, runStack) yield drafts. Update `BaseEvent.seq`'s doc comment to "storage-assigned, monotonic per run (D4-26)". Keep the `import type` for units; do not add runtime imports.
  </action>
  <acceptance_criteria>
    - `src/core/events.ts` exports `SessionStartedEvent`, `FirstTokenEvent`, `UsageEvent`, and `AgentEventDraft`.
    - The `AgentEvent` union type text lists `SessionStartedEvent`, `FirstTokenEvent`, and `UsageEvent`.
    - `UsageEvent.costUsd` is typed `UsdCost` (imported from `./units.js`).
    - `npm run typecheck` passes (no `tsc` errors).
    - No `import ... from "zod"` appears in events.ts.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node -e "const s=require('fs').readFileSync('src/core/events.ts','utf8'); for(const t of ['session_started','first_token','\"usage\"','AgentEventDraft','SessionStartedEvent','FirstTokenEvent','UsageEvent']){ if(!s.includes(t)){ console.error('missing',t); process.exit(1);} } if(/from \"zod\"/.test(s)){process.exit(2)}"</automated>
  </verify>
  <done>The three new variants and `AgentEventDraft` compile and are part of the `AgentEvent` union; events.ts stays zod-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | A third-party package is fetched and its code becomes runnable in-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | `npm install @earendil-works/pi-coding-agent` | high | mitigate | RESEARCH § Package Legitimacy Audit verified the package: current `latest`, source repo `github.com/earendil-works/pi`, NO preinstall/postinstall script (`npm view … scripts` = build/test only). Exact-pin `0.80.3`; no supply-chain execution on install. Verdict OK/Approved — no `[ASSUMED]`/`[SUS]`, so no blocking human checkpoint required. |
| T-04-07a | Info-disclosure | events.ts variants | low | accept | New event shapes carry no secret material; `UsageEvent` holds tokens/cost only. The provider key never enters any event (enforced in Plan 04-06). |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `npm test` still green (no existing test references the new variants yet; nothing regresses).
- Coding-agent dep resolves at runtime.
</verification>

<success_criteria>
- `@earendil-works/pi-coding-agent@0.80.3` installed (exact pin), `@mariozechner/*` absent.
- `SessionStartedEvent`, `FirstTokenEvent`, `UsageEvent`, `AgentEventDraft` exported and unioned; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/04-agent-runtime-pi-sdk-adapter/04-01-SUMMARY.md` when done.
</output>
