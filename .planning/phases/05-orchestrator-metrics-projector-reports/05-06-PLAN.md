---
phase: 05-orchestrator-metrics-projector-reports
plan: 6
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - src/orchestrator/run.ts
  - tests/orchestrator.test.ts
autonomous: true
requirements: [CLI-01]
nyquist_compliant: true

must_haves:
  truths:
    - "`runBenchmark` sequences the whole row end-to-end: load specs → build/persist manifest (run_id) → agent stream into the SAME run log → build/render on the agent-mutated workspace → evaluate+score (completed path only) → updateRunOutcome → projectMetrics → returns a RunResult (SC#1, CLI-01)."
    - "The agent runs BEFORE build/render and build/render operates on the agent-populated workspace via skip-copy (05-01), never the pristine template (agent-first-then-build ordering)."
    - "A scored failure — build_failed / start_failed / timeout / agent_error — never throws: the runs row persists its terminal status via updateRunOutcome (05-03), partial metrics fold via projectMetrics (05-04), and runBenchmark returns scored:true (D5-05)."
    - "Metrics are folded by projectMetrics AFTER the run finishes, never computed inline during the stream (TEL-02, D-24)."
    - "runBenchmark throws ONLY on harness-fatal conditions (unresolvable/invalid spec, missing expected/mockup file, DB open/write error, agent setup throw before any terminal) so the CLI maps return→exit 0, throw→non-zero (D5-08)."
    - "run.ts imports only port/pipeline/eval seams + storage functions (openDb, createStoragePort, storage helpers) + createModels from pi-ai; it does NOT import @earendil-works/pi-coding-agent, does NOT write ad-hoc SQL through better-sqlite3, and does NOT import playwright (D-23)."
    - "injectImage = modelAcceptsImage(model) (05-02) gates mockup injection; when false a `mockup_grounding_skipped` marker event is appended so the report can render the D5-01 caveat (D5-14)."
  artifacts:
    - src/orchestrator/run.ts
    - tests/orchestrator.test.ts
  key_links:
    - "runSession(agentInput, { createSession }) drafts → storage.appendEvent (shared run_id log, agent-first, D4-26)"
    - "buildRender on agent-populated appDir → onLivePage({ generatedPng, page }) → evaluateRun (server-up eval window, 05-01/03-05)"
    - "RunOutcome.status → updateRunOutcome(db, runId, status, failedStage, finishedAt) (05-03) — else the row stays 'pending' forever"
    - "readEvents log → projectMetrics(db, runId) AFTER the terminal (05-04) — never inline (TEL-02/D-24)"
---

<objective>
Build the run orchestrator `src/orchestrator/run.ts` — the single sequencing
function `runBenchmark(args, deps)` that turns three named spec paths into one
stored, scored benchmark row end-to-end (SC#1, CLI-01). It wires the
already-proven Phase 1–4 ports plus the three Phase-5 additive touches
(05-01 skip-copy build/render + server-up eval window, 05-02 image gate,
05-03 terminal-status write + expected-screenshot persistence, 05-04
projector) into the 13-step call sequence from 05-RESEARCH.md §Orchestrator
Seam.

It is deliberately headless: `runBenchmark` returns a structured `RunResult`
and prints nothing. The terminal summary (REPORT-01) and the HTML report
(REPORT-02) are the CLI's job (05-07) and the report renderer's job (05-05).

Purpose: make the whole platform runnable as one command's core — every prior
phase exists behind a port precisely so this function is glue, not new
capability.
Output: `src/orchestrator/run.ts` exporting `runBenchmark` + its
`RunBenchmarkArgs` / `RunBenchmarkDeps` / `RunResult` types, and a failing-first
`tests/orchestrator.test.ts` (Wave-0) that drives the full sequence from a
scripted fake session + faux judge provider with zero paid calls.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-orchestrator-metrics-projector-reports/05-RESEARCH.md
@.planning/phases/05-orchestrator-metrics-projector-reports/05-CONTEXT.md

# Sibling plans this orchestrator depends on — read their PLAN/SUMMARY for the
# EXACT exported names + signatures before wiring (they land in earlier waves):
@.planning/phases/05-orchestrator-metrics-projector-reports/05-01-PLAN.md
@.planning/phases/05-orchestrator-metrics-projector-reports/05-02-PLAN.md
@.planning/phases/05-orchestrator-metrics-projector-reports/05-03-PLAN.md
@.planning/phases/05-orchestrator-metrics-projector-reports/05-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 failing test — tests/orchestrator.test.ts (full sequence, zero paid calls)</name>
  <files>tests/orchestrator.test.ts</files>
  <read_first>
    - tests/evalPipeline.integration.test.ts — the canonical faux-judge wiring: `fauxProvider({ provider: DEFAULT_JUDGE_MODEL.provider, models:[{ id: DEFAULT_JUDGE_MODEL.modelId, input:["text","image"] }] })` + `setResponses([fauxAssistantMessage([fauxToolCall("submit_verdict",{...})])])` + `createModels()` + `models.setProvider(faux.provider)`; and the renderWithPage-on-`file://` fixture pattern (no dev server).
    - tests/_fakes/fakeSession.js — `fakeFactory(script)` → `{ createSession, calls }`; drives the real `runSession` from a scripted `PiEvent[]` with zero network.
    - tests/agentAdapter.test.ts + tests/agentCeilings.test.ts — PiEvent script shapes: `agent_start` (→ session_started), streamed text (→ first_token), `tool_call`, file-mutation tool results (→ file_mutation), `turn_end` (→ usage); and how enough `turn_end`s trip `maxTurns` to end the stream with `benchmark_finished{status:"timeout"}`.
    - tests/fixtures/eval/pngFixtures.ts (`makeExpectedPng`, `makeGeneratedMatchPng`) + tests/fixtures/eval/app.html — reuse for the live-page happy path.
    - src/render/renderWithPage.ts — `renderWithPage({url,viewport})` → `{ png, page, close }` (real page for axe, no dev server).
    - src/storage/db.ts — `openDb(file)` + `readEvents(db,runId)` for post-run assertions.
    - src/pipeline/runStack.ts — `RunOutcome { runId, status, failedStage, screenshotArtifactId }` shape the fake `buildRender` must return.
  </read_first>
  <action>
    Write `tests/orchestrator.test.ts` as the RED scaffold that pins the
    orchestrator contract before it exists. It imports `runBenchmark` and its
    types from `../src/orchestrator/run.js` (the module does not exist yet — the
    suite fails to import / run, which is the expected RED state).

    Point the spec-path args at the existing fixtures
    (`tests/fixtures/scenarios/dashboard/dashboard.yaml`,
    `tests/fixtures/models/deepseek4pro.json`, `stacks/angular.yaml`) so no
    production `models/`/`scenarios/` dirs are required by the test (those are
    05-03's production copies). Use a per-test tmp `dbPath` and `resultsRoot`
    (mkdtempSync) so nothing touches `results/` or `results/bench.sqlite`; clean
    up in afterEach.

    Drive every path through injected deps — no paid calls, no Pi SDK, no dev
    server:
    - `createSession`: from `fakeFactory(script)` (reuse tests/_fakes) so the
      real `runSession` + mapper produce canonical drafts from a scripted
      `PiEvent[]`.
    - `models`: `createModels()` + a `fauxProvider` registered under
      `DEFAULT_JUDGE_MODEL.provider`/`.modelId` returning a scripted
      `submit_verdict` tool call (verbatim eval-pipeline pattern).
    - `buildRender`: a fake matching the orchestrator's `BuildRenderFn` seam
      (see Task 2) that appends the stage + `benchmark_finished` events itself
      and returns a chosen `RunOutcome` — so a build outcome is chosen by the
      test, never by a real Angular build.

    Cover these cases with concrete assertions:
    1. HAPPY (completed): scripted natural agent completion (session_started +
       first_token + ≥1 file_mutation + ≥2 usage, NO agent terminal). Fake
       `buildRender` obtains a real live page+png via `renderWithPage` on the
       `app.html` fixture, invokes `onLivePage({ generatedPng: makeGeneratedMatchPng(), page })`,
       closes it, and returns `RunOutcome{status:"completed"}`. Assert:
       `runBenchmark` resolves (no throw); `result.status === "completed"`,
       `result.scored === true`, `result.compositeScore` is a number in [0,1];
       reopening `dbPath` shows the runs row `status='completed'` (NOT 'pending')
       with a non-null `composite_score`; ≥1 `evaluations` row; projection tables
       (`metrics`/`tool_calls`/`iterations`) are POPULATED for the run; a
       `screenshots role='expected'` row exists (self-contained report data,
       05-03); `result.reportDir` equals `<resultsRoot>/<runId>`.
    2. BUILD_FAILED: same scripted natural agent completion, but fake
       `buildRender` appends `stage_failed{build}` + `benchmark_finished{status:"build_failed",failedStage:"build"}`
       and returns `RunOutcome{status:"build_failed",failedStage:"build",screenshotArtifactId:null}`
       WITHOUT calling `onLivePage`. Assert: no throw; `result.scored === true`;
       `result.status === "build_failed"`; `result.compositeScore === null`;
       runs row `status='build_failed'`, `failed_stage='build'`; zero
       `evaluations` rows; projector still folded the partial cost/token/iteration
       metrics that the agent produced (metrics table non-empty).
    3. TIMEOUT (agent-capped): script enough `turn_end`s (with a low
       `maxTurns` in the scenario budget, or drive the ceiling per
       agentCeilings.test) that `runSession` ends the stream with
       `benchmark_finished{status:"timeout"}`. Assert: `buildRender` is NEVER
       invoked (spy on the fake); no throw; `result.status === "timeout"`;
       `result.scored === true`; runs row `status='timeout'`; cost from the
       reconciliation `usage{aborted:true}` still folded.
    4. HARNESS ERROR: pass an unresolvable `scenarioPath` (or `stackPath`).
       Assert `runBenchmark` REJECTS (throws), and NO runs row / no `reportDir`
       artifacts were produced (harness-fatal → CLI non-zero, D5-08).
    5. IMAGE GATE (D5-01/D5-14): a case where the resolved model does not accept
       image input (drive via a `models`/registry stub or a dep) asserts a
       `mockup_grounding_skipped` `unknown` event is present in `readEvents`
       (the report caveat channel).

    Do NOT assert terminal-printing or HTML here — the orchestrator returns a
    structured result only. Keep the happy path's renderWithPage usage the ONLY
    browser touch; cases 2–4 stay pure (no Chromium).
  </action>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts 2>&1 | grep -Eq "Cannot find module .*orchestrator/run|No test suite|runBenchmark is not|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/orchestrator.test.ts` exists and encodes cases 1–5 with concrete DB/`readEvents`/return-value assertions.
    - The suite fails ONLY because `src/orchestrator/run.ts` is absent (RED), not because of malformed fixtures — every dep is injected, no paid call, no dev server.
    - The happy path is the sole browser touch (renderWithPage on the `app.html` fixture); cases 2–4 run without Chromium.
  </acceptance_criteria>
  <done>`tests/orchestrator.test.ts` exists with cases 1–5 and fails RED only because `src/orchestrator/run.ts` is absent (no paid call, no dev server, happy path the sole Chromium touch).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/orchestrator/run.ts — runBenchmark + seams (GREEN)</name>
  <files>src/orchestrator/run.ts</files>
  <read_first>
    - src/core/ports.ts — `StoragePort` (appendEvent seqless drafts, readEvents, writeArtifact, persistManifest); `AgentPort` shape.
    - src/agent/piAgentAdapter.ts — `runSession(input, { createSession?, now? })` streams drafts and, on natural completion, yields NO terminal (runStack owns it, D4-21); on ceiling/fatal yields `benchmark_finished{timeout|agent_error}`. `SessionFactory`/`RunSessionDeps` types.
    - src/agent/types.ts — `AgentInput` (runId, workspacePath, promptText, preamble, mockupBytes, mockupMimeType, skillPaths, model, budget) + `AgentModelSpec` + `AgentBudget`.
    - src/agent/modelCapabilities.ts (05-02) — `modelAcceptsImage(spec: AgentModelSpec): boolean` (the ONLY image-capability probe; encapsulates the Pi registry read so run.ts never imports pi-coding-agent) + the `AgentInput.injectImage?` field the adapter now honors.
    - src/pipeline/runStack.ts — `RunOutcome { runId, status, failedStage, screenshotArtifactId }`; and the "05-01 build/render entry". That entry is NOT a new export — it IS `runStack(stack, runId, storage, opts?: RunStackOptions)` where `RunStackOptions = { prePopulated?: boolean; onLivePage?: (page, generatedPng: Buffer) => Promise<void> }`. 05-01 adds skip-copy + the `onLivePage` server-up window + emitted `stage:'start'`/`render` events to the EXISTING `runStack`; there is no rename and no `defaultBuildRender`/`buildRender` export to hunt for. Note 05-01's `onLivePage(page, generatedPng)` is POSITIONAL (two args) — `defaultBuildRender` adapts it to 05-06's object `{ generatedPng, page }` internal shape (see Task 2 action).
    - src/pipeline/evaluate.ts — `evaluateRun({ db, runId, repIndex, expectedPng, generatedPng, viewport, page, registry, defaultWeights }) → { compositeScore, weightsUsed, outcomes }` (persists evaluations + writes composite when ≥1 survivor; `page` typed `unknown`, so run.ts passes it through without a playwright import).
    - src/eval/registry.ts — `buildRegistry({ db, models, expectedElements?, judgeModel? }) → EvaluatorPort[]` (pixelmatch+axe+judge always; dom only when expectedElements non-empty). src/eval/judgeEvaluator.ts — `DEFAULT_JUDGE_MODEL`, and that a missing key → judge returns dropped (never throws).
    - src/pipeline/composite.ts — `DEFAULT_EVALUATOR_WEIGHTS`.
    - src/specs/load.ts — `loadStack/loadScenario/loadModel(path)` (throw on invalid = harness-fatal). src/specs/schema.ts / types.ts — `Stack.viewport/preamble/template/port`, `Scenario.prompt/expected.path/viewport/skills/budget{maxMinutes,maxUsd,maxTurns}/expectedElements?/evaluatorWeights?`, `ModelConfig.provider/modelId/params`.
    - src/manifest/manifest.ts — `buildManifest(input) → Manifest`, `persistManifest(db, manifest)` (inserts runs row status='pending'), `VersionStamp` shape; src/manifest/fingerprint.ts — `sha256`.
    - src/storage/db.ts — `openDb(file)`, `readEvents`; src/storage/storagePort.ts — `createStoragePort(db, resultsRoot?)`; src/storage/evaluations.ts — `updateRunComposite`/`linkDiffScreenshot` as the mirror for the 05-03 additions.
    - 05-03-PLAN.md/SUMMARY — the exact names of `updateRunOutcome(db, runId, status, failedStage, finishedAt)` and the persist-expected helper (e.g. `linkExpectedScreenshot`).
    - 05-04-PLAN.md/SUMMARY — `projectMetrics(db, runId)` (src/telemetry) exact signature.
    - src/workspace/copy.ts — `copyWorkspace(template, runId, "tmp") → appDir` (tmp/<runId>/angular).
    - src/core/ids.ts — `newRunId()`. src/core/events.ts — `RunStatus`, `AgentEventDraft`, `UnknownEvent`.
  </read_first>
  <behavior>
    - Given valid fixtures + a scripted natural-completion agent + a completed buildRender, runBenchmark returns { status:"completed", scored:true, compositeScore:number, reportDir } and the runs row is status='completed' with a non-null composite; metrics/tool_calls/iterations are populated only AFTER the terminal.
    - Given a build_failed buildRender, runBenchmark returns { status:"build_failed", scored:true, compositeScore:null } without throwing; updateRunOutcome set the row; projectMetrics folded partial metrics; no evaluation ran.
    - Given an agent stream that ends with benchmark_finished{timeout}, buildRender is never called; runBenchmark returns { status:"timeout", scored:true }.
    - Given an unresolvable spec path (or a missing expected/mockup file, or a DB open failure), runBenchmark throws before returning a RunResult (harness-fatal).
    - Given modelAcceptsImage(model) === false, a `mockup_grounding_skipped` unknown event is appended and AgentInput.injectImage is false.
  </behavior>
  <action>
    Implement `src/orchestrator/run.ts` following the 05-RESEARCH.md §Orchestrator
    13-step sequence. Export:
    - `RunBenchmarkArgs { stackPath: string; modelPath: string; scenarioPath: string }`
    - `RunBenchmarkDeps { createSession?: SessionFactory; buildRender?: BuildRenderFn; models?: Models; dbPath?: string; resultsRoot?: string; now?: () => number }`
    - `RunResult { runId: string; status: RunStatus; compositeScore: number | null; failedStage: string | null; reportDir: string; scored: boolean }`
    - `type OnLivePage = (live: { generatedPng: Buffer; page: unknown }) => Promise<void>`
    - `type BuildRenderFn = (input: { stack: Stack; runId: string; storage: StoragePort; appDir: string; onLivePage: OnLivePage }) => Promise<RunOutcome>`
    - `async function runBenchmark(args: RunBenchmarkArgs, deps?: RunBenchmarkDeps): Promise<RunResult>`

    Defaults (production seams): `dbPath = "results/bench.sqlite"` (D5-15 iv,
    WAL via openDb), `resultsRoot = "results"`, `now = Date.now`, `createSession`
    left undefined (runSession falls back to createPiSession), `models =
    defaultModels()`, `buildRender = defaultBuildRender`.

    `defaultModels()`: `createModels()` then register the real provider for
    `DEFAULT_JUDGE_MODEL.provider` (anthropic) so `buildRegistry`'s
    `models.getModel()` resolves; the key is read from env only at
    `models.complete()` time inside the judge (absent → judge drops, composite
    renormalizes, never a crash — D5-05/Pitfall 5). Confirm the exact pi-ai
    provider-registration call against `@earendil-works/pi-ai` 0.80.3 docs; this
    is the one MEDIUM-confidence seam. pi-ai is allowed under D-23 (importBoundary
    guards only pi-coding-agent).

    `defaultBuildRender`: a ≤5-line adapter over 05-01's build/render entry, which
    is the EXISTING `runStack(stack, runId, storage, opts?: RunStackOptions)` (NOT
    a new export). Call `runStack(stack, runId, storage, { prePopulated: true,
    onLivePage: … })`. Do NOT forward `appDir`: under `prePopulated: true` runStack
    itself derives `appDir = resolve(TMP_ROOT, runId, "angular")` (the same dir
    run.ts pre-populated at step 6), so 05-06 does not pass it in — the seam's
    `appDir` arg is informational for run.ts's own bookkeeping. Bridge the callback
    shapes: 05-01's `onLivePage(page, generatedPng)` is POSITIONAL, but 05-06's
    seam `OnLivePage` is object-shaped `{ generatedPng, page }` — so wrap it,
    passing `(page, generatedPng) => onLivePage({ generatedPng, page })` into
    runStack. Return runStack's `RunOutcome` unchanged. The local
    `BuildRenderFn`/`OnLivePage` shapes isolate run.ts from 05-01's positional
    signature.

    Sequence inside runBenchmark:
    1. HARNESS-FATAL prologue (any throw here propagates → CLI non-zero, D5-08):
       `loadStack(args.stackPath)`, `loadScenario(args.scenarioPath)`,
       `loadModel(args.modelPath)`. Derive `scenarioDir = dirname(args.scenarioPath)`;
       read `expectedPng = readFileSync(join(scenarioDir, scenario.expected.path))`
       and `mockupBytes = readFileSync(join(scenarioDir, "mockup.png"))` (mockup
       is the fixed sibling convention — no yaml field; matches tests/manifest.test.ts).
       `runId = newRunId()`.
    2. `db = openDb(dbPath)`; wrap the REST in try/finally that closes db.
       `storage = createStoragePort(db, resultsRoot)`.
    3. Build `versionStamp` (no playwright/pi-coding-agent import): `node =
       process.version`; `dependencies = { lockfileHash: sha256(readFileSync("package-lock.json")) }`;
       `playwright =` the version string from `node_modules/playwright/package.json`
       read via readFileSync+JSON.parse (a file read, NOT a runtime import);
       `chromium = null` (best-effort v1 — ponytail: real revision later);
       `modelId = model.modelId`; `modelParams = model.params`.
       `buildManifest({ runId, stack, scenario, model, prompt: scenario.prompt, mockup: mockupBytes, expected: expectedPng, skills: undefined, versionStamp })`
       then `persistManifest(db, manifest)` (inserts the runs row status='pending';
       a DB write failure here is harness-fatal → propagates).
    4. Persist the expected screenshot NOW (independent of outcome, so `report`
       shows it even on a failed row): call the 05-03 persist-expected helper
       (e.g. `linkExpectedScreenshot(db, runId, expectedPng, stack.viewport, resultsRoot)`)
       writing a `screenshots role='expected'` artifact (A7 / D5-15 iii).
    5. Resolve the image gate (D5-01/D5-14): build `agentModel: AgentModelSpec =
       { provider: model.provider, modelId: model.modelId, thinkingLevel:
       model.params.thinkingLevel, temperature: (model.params.temperature ?? 0) }`;
       `injectImage = modelAcceptsImage(agentModel)`. If `injectImage === false`,
       `storage.appendEvent({ runId, ts: now(), type:"unknown",
       piType:"mockup_grounding_skipped", raw:{ reason:"model does not accept image input" } })`
       so 05-05 can render the fixed caveat banner.
    6. Seed the disposable workspace: `appDir = copyWorkspace(stack.template,
       runId, "tmp")`. Build `AgentInput = { runId, workspacePath: appDir,
       promptText: scenario.prompt, preamble: stack.preamble, mockupBytes,
       mockupMimeType:"image/png", skillPaths: scenario.skills, model: agentModel,
       budget: { maxWallClockMs: scenario.budget.maxMinutes*60000, maxCostUsd:
       scenario.budget.maxUsd, maxTurns: scenario.budget.maxTurns }, injectImage }`.
    7. AGENT-FIRST stream into the shared log, tracking whether the agent emitted
       its own terminal: `let agentTerminal: RunStatus | null = null;` then
       `for await (const draft of runSession(agentInput, { createSession: deps.createSession, now: deps.now }))
       { storage.appendEvent(draft); if (draft.type === "benchmark_finished") agentTerminal = draft.status; }`.
       A THROW out of runSession (e.g. missing DEEPSEEK_API_KEY, model-not-found,
       skill-load-failure) is a setup failure with no agent terminal → let it
       propagate as harness-fatal (D5-08 "uncaught crash"); do not synthesize a
       scored row. (runSession converts in-run fatals to a benchmark_finished
       terminal itself, D4-14, so the throw path is genuinely setup-only.)
    8. Terminal-status branch (honors D4-21 "one terminal"):
       - If `agentTerminal !== null` (agent capped/errored): the agent already
         emitted the authoritative terminal — do NOT call buildRender (that would
         double-emit). `status = agentTerminal; failedStage = null; compositeScore
         = null` (no generated screenshot, no evaluation).
       - Else (natural completion): `outcome = await buildRender({ stack, runId,
         storage, appDir, onLivePage })` — runStack's contract never rejects, so
         no try needed for scored failures. `status = outcome.status; failedStage
         = outcome.failedStage`. Evaluate ONLY inside `onLivePage` (server-up),
         which buildRender invokes only on the completed+screenshot path: there,
         `registry = buildRegistry({ db, models: deps.models ?? defaultModels(),
         expectedElements: scenario.expectedElements, judgeModel: DEFAULT_JUDGE_MODEL })`;
         `const evalResult = await evaluateRun({ db, runId, repIndex: 0,
         expectedPng, generatedPng: live.generatedPng, viewport: stack.viewport,
         page: live.page, registry, defaultWeights: scenario.evaluatorWeights ??
         DEFAULT_EVALUATOR_WEIGHTS })`; capture `compositeScore = evalResult.compositeScore`.
         Wrap the evaluate call in try/catch so any evaluator throw (e.g. axe on a
         degenerate page) leaves `compositeScore = null` and never crashes the row
         (D5-05); the judge already degrades to dropped on a missing key.
         (repIndex 0 — v1 writes one rep per unique run_id and reports the latest,
         D5-10; ponytail: fingerprint-count rep-keying is the v2 upgrade.)
    9. Terminal-state write (gap #3): `updateRunOutcome(db, runId, status,
       failedStage, now())` (05-03) — else the row stays 'pending' forever. A DB
       write failure here is harness-fatal (propagates).
    10. Fold metrics AFTER the run (TEL-02/D-24): `projectMetrics(db, runId)`
        (05-04) reading the now-complete log — never inline during the stream.
    11. Return `{ runId, status, compositeScore, failedStage, reportDir:
        join(resultsRoot, runId), scored: true }`. Any reached return means a
        scored row persisted → CLI exit 0 (D5-08). Close db in the finally.

    D-23 hygiene (enforce, do not violate): import `openDb`/`createStoragePort`/
    storage helpers + `createModels` from pi-ai + the pipeline/eval/agent/spec/
    manifest/workspace functions ONLY. NO `@earendil-works/pi-coding-agent` import
    (the image probe is encapsulated in 05-02's modelCapabilities). NO direct
    playwright import (the live `page` flows through as `unknown`). NO ad-hoc
    prepared-statement SQL against the raw db handle — every read/write goes
    through the storage/eval helper functions. A bare `import type Database from
    "better-sqlite3"` for the db handle type is the established norm and is allowed.
  </action>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts && npx vitest run tests/importBoundary.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/orchestrator.test.ts` passes all five cases (GREEN).
    - `tests/importBoundary.test.ts` stays green — run.ts is NOT a pi-coding-agent importer; `grep -n "pi-coding-agent" src/orchestrator/run.ts` returns nothing.
    - run.ts contains no `import "playwright"`/`from "playwright"` and no `db.prepare(`/`.exec(` ad-hoc SQL (`grep -vn '^\s*//' src/orchestrator/run.ts | grep -Eq "from \"playwright\"|db\.prepare\(|db\.exec\("` finds nothing).
    - `npx tsc --noEmit` is clean.
    - build_failed and timeout paths return `scored:true` and never throw; the unresolvable-spec path rejects.
  </acceptance_criteria>
  <done>`runBenchmark` returns a scored `RunResult` for every terminal status and throws only on harness-fatal conditions; orchestrator.test.ts + importBoundary.test.ts green and `tsc --noEmit` clean, with no pi-coding-agent/playwright/ad-hoc-SQL in run.ts (D-23).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI spec flags → filesystem paths | `--stack/--model/--scenario` names become spec file paths the orchestrator loads. |
| Agent output → workspace / event log | Untrusted model-generated files land in `tmp/<runId>/`; narration/tool summaries land in the log the report later renders. |
| Env secrets → provider auth | `DEEPSEEK_API_KEY` (agent) / `ANTHROPIC_API_KEY` (judge) enter only their adapter, never the orchestrator. |

## STRIDE Threat Register (ASVS L1 — block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | spec-flag → path resolution | medium | mitigate | Loaders throw on missing/invalid spec (harness-fatal → non-zero, no row). CLI (05-07) constrains flags to the `stacks/`/`models/`/`scenarios/` roots; orchestrator treats received paths as CLI-scoped and does not expand `..`/absolute traversal into writes. |
| T-05-02 | Information disclosure | API keys | high | mitigate | Keys read from `process.env` ONLY inside the agent adapter (D4-19) and the judge; run.ts never reads, logs, or persists a key. `versionStamp`/manifest carry no secret material; the report escapes agent output (05-05). |
| T-05-03 | Tampering / isolation | main project tree vs run workspace | high | mitigate | All agent + build work confined to the disposable `tmp/<runId>/` (copyWorkspace + pipeline teardown, WORK-04); orchestrator writes only under `results/<runId>` via storage helpers. D-23: no direct Pi/better-sqlite3-SQL/playwright imports in run.ts (importBoundary + grep gates). |
| T-05-04 | Denial of service | unbounded agent run | medium | mitigate | The three budget ceilings (maxWallClockMs/maxUsd/maxTurns) resolved from `scenario.budget` and enforced in the adapter; first-to-trip aborts → scored `timeout` row (D5-05), not a hang. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs NO packages (stdlib + already-installed deps only, per 05-RESEARCH Package Legitimacy Audit = empty by construction). Nothing to gate. |
</threat_model>

<verification>
- `npx vitest run tests/orchestrator.test.ts` — all five cases green (SC#1 wiring, D5-05 no-crash, D5-08 exit mapping).
- `npx vitest run tests/importBoundary.test.ts` — run.ts is not a second pi-coding-agent importer (D-23).
- `npx tsc --noEmit` — types clean under strict/verbatimModuleSyntax.
- Grep gates (D-23): `grep -n "pi-coding-agent" src/orchestrator/run.ts` empty; no `from "playwright"`; no `db.prepare(`/`db.exec(` ad-hoc SQL in non-comment lines.
- Full suite `npm test` green after this wave (no regression in projector/report siblings).
</verification>

<success_criteria>
- `runBenchmark` sequences load → manifest/run_id → agent stream → build/render → evaluate/score → updateRunOutcome → projectMetrics and returns a `RunResult` for any scored row (SC#1, CLI-01).
- A build_failed / start_failed / timeout / agent_error run persists its terminal status and folds partial metrics without throwing (D5-05); runBenchmark throws only on harness-fatal conditions (D5-08).
- Metrics are folded by `projectMetrics` after the terminal, never inline (TEL-02/D-24).
- The agent runs before build/render on the agent-populated workspace (agent-first-then-build); the image gate (05-02) drives `injectImage` and the caveat marker (D5-01/D5-14).
- run.ts respects the D-23 import boundary (no pi-coding-agent, no ad-hoc SQL, no playwright).
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator-metrics-projector-reports/05-06-SUMMARY.md` when done.
</output>
