# Phase 5 — Plan Outline

> Chunked-planning outline for **Orchestrator + Metrics Projector + Reports**.
> Decomposition follows the 05-RESEARCH.md Architectural Responsibility Map + the
> 13-step concrete call sequence; each plan aligns to a 05-VALIDATION.md Wave-0
> test file. Waves 1 = the three sanctioned additive prior-phase touches
> (D5-13/14/15), then projector, then report + orchestrator (parallel), then CLI.

| Plan ID | Objective | Wave | Depends On | Requirements |
|---------|-----------|------|------------|--------------|
| 05-01 | Minimal `runStack` seam refactor (D5-13): skip the template copy on an agent-populated workspace, expose a server-up evaluation window (yield the live Playwright `page` before teardown for axe), and emit `stage_started/completed{stage:'start'}` + a render-duration event so startup/render fold like the other stages. Teardown + fatal-stage→`RunOutcome` mapping stay in one place. Wave-0: extend `tests/runStack.integration.test.ts`. | 1 | — | TEL-03, CLI-01 |
| 05-02 | Agent capability probe + image gate (D5-14 / D5-01): new `src/agent/modelCapabilities.ts` `modelAcceptsImage(spec)` (reads the Pi registry `input` field — a 2nd `@earendil-works/pi-coding-agent` importer), optional `AgentInput.injectImage` (default true) honored by the adapter via a one-line ternary on `images`, and `tests/importBoundary.test.ts` allowlist updated in the same task. | 1 | — | CLI-01 |
| 05-03 | Additive storage writes + production specs (D5-15 i/ii/iii): `updateRunOutcome(db,runId,status,failedStage,finishedAt)` mirroring `updateRunComposite` (nothing sets the terminal `runs` row today), persist `expected.png` as a `screenshots role='expected'` artifact for self-contained regen, and create production `models/deepseek4pro.json` + `scenarios/dashboard/dashboard.yaml` (+ `expected.png`) from the fixtures so the D5-02 flags resolve. No schema change (SCHEMA_VERSION stays 1). | 1 | — | CLI-01, REPORT-02 |
| 05-04 | Metrics projector `projectMetrics(db,runId)` in `src/telemetry` — fold the persisted event log into `metrics` / `tool_calls` / `iterations` after the run, never inline (TEL-02): perf (wall/build/ttft/startup/render/cost/tokens) + `backoff_wait_ms` (D5-12, Σ retry end−start), engineering (files/lines), iteration count + correction density (D5-11, `seq`-ordered), tool-calls by type. Wave-0: `tests/projector.test.ts` (golden fixtures + determinism/order-invariance/sum-conservation properties) + `tests/projectionNotInline.test.ts`. | 2 | 05-01 | TEL-02, TEL-03, TEL-04, TEL-05, TEL-06 |
| 05-05 | Self-contained HTML report `renderReport(db,runId): string` in `src/reports` per 05-UI-SPEC — template-literal, base64-inline expected/generated/diff triptych (D5-09), scorecard (composite + 4 sub-score bars), complete folded-metrics table, mockup-grounding caveat banner (D5-01), collapsible agent timeline (D4-12 narration); reads only the DB + artifacts so `report <id>` regenerates offline. Wave-0: `tests/renderReport.test.ts` (self-containment invariant, HTML-escaping, partial-run empty state). | 3 | 05-03, 05-04 | REPORT-02 |
| 05-06 | Run orchestrator `src/orchestrator/run.ts` — sequence load specs → manifest/run_id → capability-gated agent stream → refactored build/render entry (05-01) → evaluate/score → persist `expected.png` + `updateRunOutcome` → `projectMetrics`; scored-outcome-never-crash (D5-05) with exit-code decision logic (0 on any scored row, non-zero only on harness error — D5-08). Wave-0: `tests/orchestrator.test.ts` (injected fake `createSession` + faux judge, incl. build_failed / timeout paths — no paid calls). | 3 | 05-01, 05-02, 05-03, 05-04 | CLI-01 |
| 05-07 | CLI entrypoint `src/cli/index.ts` (`node:util parseArgs`, canonical DB `results/bench.sqlite`) — `run --stack --model --scenario` and `report [<id>] [--latest]` (D5-02/06); terminal summary (REPORT-01 / D5-03: composite + 4 sub-scores + headline) printed after `run` and by `report`; auto-emit `results/<run_id>/report.html` echoing the path (D5-07); `--latest` selection + not-found/empty-DB error copy + exit codes (D5-08). Wave-0: `tests/cli.test.ts`. | 4 | 05-05, 05-06 | REPORT-01, CLI-01, CLI-02 |

**Requirement coverage (all 9 present):** TEL-02 → 05-04 · TEL-03 → 05-01, 05-04 · TEL-04 → 05-04 · TEL-05 → 05-04 · TEL-06 → 05-04 · REPORT-01 → 05-07 · REPORT-02 → 05-03, 05-05 · CLI-01 → 05-01, 05-02, 05-03, 05-06, 05-07 · CLI-02 → 05-07.
